---
title: API ЮТКонтекстТеста
description: "Публичные экспортные методы модуля ЮТКонтекстТеста из ядра YaXUnit."
tags: [yaxunit, api, ЮТКонтекстТеста]
publish_publicly: false
---

# API ЮТКонтекстТеста

Компактный справочник по 3 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКонтекстТеста.Значение

`Функция ЮТКонтекстТеста.Значение(ИмяЗначения) Экспорт`

Возвращает значение контекста

**Параметры:**
- `ИмяЗначения` — Строка

**Возвращает:**
- `Произвольный` — Сохраненное в контексте значение

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКонтекстТеста/Module.bsl:28`.

## ЮТКонтекстТеста.УстановитьЗначение

`Процедура ЮТКонтекстТеста.УстановитьЗначение(ИмяЗначения, Значение) Экспорт`

Установить значение.
Устанавливает значение в контекст тестов.
Установка происходит в соответствии с уровнем исполнения

**Параметры:**
- `ИмяЗначения` — Строка
- `Значение` — Произвольный

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКонтекстТеста/Module.bsl:68`.

## ЮТКонтекстТеста.ТекущийКонтекст

`Функция ЮТКонтекстТеста.ТекущийКонтекст() Экспорт`

Текущий контекст исполнения теста

**Возвращает:**
- Неопределено, Структура, Произвольный -  Текущий контекст

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКонтекстТеста/Module.bsl:99`.
