---
title: API ЮТУтверждения
description: "Публичные экспортные методы модуля ЮТУтверждения из ядра YaXUnit."
tags: [yaxunit, api, ЮТУтверждения]
publish_publicly: false
---

# API ЮТУтверждения

Компактный справочник по 59 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТУтверждения.Что

`Функция ЮТУтверждения.Что(Знач ПроверяемоеЗначение, Знач Сообщение = "") Экспорт`

Инициализирует модуль для проверки утверждений.

* Устанавливает новый контекст утверждений.
* Запоминает проверяемое значение и описание проверки.
* Сбрасывает предыдущие настройки контекста утверждений.

**Параметры:**
- `ПроверяемоеЗначение` — Произвольный — Проверяемое фактическое значение.
- `Сообщение` — Строка — Описание проверки, которое будет выведено при возникновении ошибки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:39`.

## ЮТУтверждения.Метод

`Функция ЮТУтверждения.Метод(Знач ИмяМетода, Знач ПараметрыМетода = Неопределено) Экспорт`

Запоминает описание метода (имя и параметры) для последующей проверки.
Используется для проверки выполнения метода, совместно с методами
см. ВыбрасываетИсключение и см. НеВыбрасываетИсключение.

**Параметры:**
- `ИмяМетода` — Строка — Наименование метода объекта (или его свойство), указанного при инициализации.
- `ПараметрыМетода` — Массив из Произвольный — Параметры, с которыми будет вызываться метод. Можно использовать конструктор параметров см. ЮТМетоды.МассивПараметров.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:70`.

## ЮТУтверждения.Параметр

`Функция ЮТУтверждения.Параметр(Знач ЗначениеПараметра) Экспорт`

Добавляет параметр метода (см. Метод) для последующего вызова метода и проверки.

* Сохраняет переданное значение как параметр для дальнейшего использования при вызове метода.
* Позволяет добавлять параметры последовательно.

**Параметры:**
- `ЗначениеПараметра` — Произвольный — Параметр вызываемого метода.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:101`.

## ЮТУтверждения.ИмеющееПредставление

`Функция ЮТУтверждения.ИмеющееПредставление(Представление) Экспорт`

Запоминает переданное представление объекта для использования в сообщениях об ошибках.

**Параметры:**
- `Представление` — Строка — Текстовое представление проверяемого объекта.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:130`.

## ЮТУтверждения.Свойство

`Функция ЮТУтверждения.Свойство(Знач ИмяСвойства, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет наличие свойства и запоминает его для последующих проверок указанного свойства.

Важно, что следующие проверки будут относиться к этому свойству.
Для переключения на проверки объекта можно воспользоваться методом см. Объект.

**Параметры:**
- `ИмяСвойства` — Строка — Наименование свойства объекта. Возможно обращение к вложенным свойствам через точку, а к элементам коллекций по индексу `[0]`.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:155`.

## ЮТУтверждения.НетСвойства

`Функция ЮТУтверждения.НетСвойства(Знач ИмяСвойства, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет отсутствие свойства основного объекта.
А также сбрасывает ранее установленное проверяемое свойство (см. Свойство).

**Параметры:**
- `ИмяСвойства` — Строка — Наименование свойства объекта. Возможно обращение к вложенным свойствам через точку
- `ОписаниеПроверки` — Строка — Описание конкретной проверки

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:185`.

## ЮТУтверждения.Элемент

`Функция ЮТУтверждения.Элемент(Знач Индекс, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет наличие элемента с указанным индексом и позиционируется на нем для последующих проверок.

Важно, что следующие проверки будут относиться к этому элементу.
Для переключения на проверки объекта можно воспользоваться методом см. Объект.

Это алиас метода см. Свойство

**Параметры:**
- `Индекс` — Число — Индекс элемента коллекции.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:217`.

## ЮТУтверждения.Объект

`Функция ЮТУтверждения.Объект() Экспорт`

Используется после вызова метода см. Свойство, для переключения с ранее указанного свойства на объект.

Этот метод позволяет продолжить проверки на уровне объекта, после того как была выполнена проверка его свойств.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:235`.

## ЮТУтверждения.Равно

`Функция ЮТУтверждения.Равно(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на равенство ожидаемому.

При сравнении также проверяется совпадение типов проверяемого и ожидаемого значения.
Реализовано сравнение не только примитивных значений, но и структур, массивов, таблиц и прочего.
Для сериализуемых объектов проверяется равенство по сериализованной форме.

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что значение (или его свойство) равно этому значению.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:263`.

## ЮТУтверждения.НеРавно

`Функция ЮТУтверждения.НеРавно(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на НЕ равенство ожидаемому.

При сравнении также проверяется совпадение типов проверяемого и ожидаемого значения.
Реализовано сравнение не только примитивных значений, но и структур, массивов, таблиц и прочего.
Для сериализуемых объектов проверяется равенство по сериализованной форме

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что установленное значение (или его свойство) НЕ равно этому значению.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:288`.

## ЮТУтверждения.Больше

`Функция ЮТУтверждения.Больше(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство), оно должно быть больше ожидаемого.

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что установленное значение (или его свойство) больше этого значения.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:309`.

## ЮТУтверждения.БольшеИлиРавно

`Функция ЮТУтверждения.БольшеИлиРавно(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство), оно должно быть больше или равно ожидаемому.

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что установленное значение (или его свойство) больше или равно указанному.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:329`.

## ЮТУтверждения.Меньше

`Функция ЮТУтверждения.Меньше(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство), оно должно быть меньше ожидаемого.

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что установленное значение (или его свойство) меньше этого значения.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:349`.

## ЮТУтверждения.МеньшеИлиРавно

`Функция ЮТУтверждения.МеньшеИлиРавно(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство), оно должно быть меньше или равно ожидаемому.

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что установленное значение (или его свойство) меньше этого значения или равно ему.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:369`.

## ЮТУтверждения.Заполнено

`Функция ЮТУтверждения.Заполнено(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на "заполненность" (обертка для `ЗначениеЗаполнено`).

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:388`.

## ЮТУтверждения.НеЗаполнено

`Функция ЮТУтверждения.НеЗаполнено(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на "не заполненность" (обертка для `ЗначениеЗаполнено`).

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:407`.

## ЮТУтверждения.ИмеетТип

`Функция ЮТУтверждения.ИмеетТип(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на соответствие переданному типу.

**Параметры:**
- `ОжидаемоеЗначение` — Строка, Тип, ОписаниеТипов — Ожидается, что значение (или его свойство) имеет данный тип.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:427`.

## ЮТУтверждения.НеИмеетТип

`Функция ЮТУтверждения.НеИмеетТип(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на несоответствие переданному типу.

**Параметры:**
- `ОжидаемоеЗначение` — Строка, Тип, ОписаниеТипов — Ожидается, что значение (или его свойство) НЕ имеет данный тип.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:447`.

## ЮТУтверждения.Содержит

`Функция ЮТУтверждения.Содержит(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на наличие в нем искомого.

Проверка поддерживается для следующих типов проверяемого значения:

- Строка - по вхождению
- Массив - по наличию равного элемента
- Структура - по наличию равного значения
- Соответствие - по наличию равного значения
- СписокЗначений - по наличию равного значения
- ФиксированныйМассив - по наличию равного значения
- ФиксированнаяСтруктура - по наличию равного значения
- ФиксированноеСоответствие - по наличию равного значения
- Произвольный итерируемый объект для проверки на соответствие предикату

При сравнении также проверяется совпадение типов.

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что значение (или его свойство) содержит указанное значение. - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:489`.

## ЮТУтверждения.НеСодержит

`Функция ЮТУтверждения.НеСодержит(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на наличие в нем искомого.

Проверка поддерживается для следующих типов проверяемого значения:

- Строка - по отсутствию вхождения
- Массив - по отсутствию равного элемента
- Структура - по отсутствию равного значения
- Соответствие - по отсутствию равного значения
- СписокЗначений - по отсутствию равного значения
- ФиксированныйМассив - по отсутствию равного значения
- ФиксированнаяСтруктура - по отсутствию равного значения
- ФиксированноеСоответствие - по отсутствию равного значения
- Произвольный итерируемый объект для проверки на соответствие предикату

При сравнении также проверяется совпадение типов.

**Параметры:**
- `ОжидаемоеЗначение` — Произвольный — Ожидается, что значение (или его свойство) НЕ содержит указанное значение. - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:529`.

## ЮТУтверждения.СодержитСтрокуПоШаблону

`Функция ЮТУтверждения.СодержитСтрокуПоШаблону(Знач Шаблон, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на наличие подстроки, соответствующей регулярному выражению.

**Параметры:**
- `Шаблон` — Строка — Регулярное выражение, по которому ищем подстроку.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:554`.

## ЮТУтверждения.НеСодержитСтрокуПоШаблону

`Функция ЮТУтверждения.НеСодержитСтрокуПоШаблону(Знач Шаблон, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на отсутствие подстроки, соответствующей заданному регулярному выражению.

**Параметры:**
- `Шаблон` — Строка — Регулярное выражение, по которому ищем подстроку.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:574`.

## ЮТУтверждения.ИмеетСвойство

`Функция ЮТУтверждения.ИмеетСвойство(Знач ИмяСвойства, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет наличие свойства у объекта или его свойства.
В отличие от метода см. Свойство, не позиционируется на указанное свойство.

**Параметры:**
- `ИмяСвойства` — Строка — Наименование свойства объекта. Возможно обращение к вложенным свойствам через точку
- `ОписаниеПроверки` — Строка — Описание конкретной проверки

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:596`.

## ЮТУтверждения.НеИмеетСвойства

`Функция ЮТУтверждения.НеИмеетСвойства(Знач ИмяСвойства, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет отсутствие свойства у объекта или его свойства.
В отличие от метода см. НетСвойства, не сбрасывает позиционирование проверки.

**Параметры:**
- `ИмяСвойства` — Строка — Наименование свойства объекта. Возможно обращение к вложенным свойствам через точку
- `ОписаниеПроверки` — Строка — Описание конкретной проверки

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:618`.

## ЮТУтверждения.ВСписке

`Функция ЮТУтверждения.ВСписке(Знач Значения, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет значение (или его свойство), оно должно входить в указанный список.

**Параметры:**
- `Значения` — Массив из Произвольный — Значение для проверки. - СписокЗначений из Произвольный - Значения для проверки.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:643`.

## ЮТУтверждения.МеждуВключаяГраницы

`Функция ЮТУтверждения.МеждуВключаяГраницы(НачалоИнтервала, ОкончаниеИнтервала, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет вхождение установленного значения (или его свойства) в заданный интервал.

Проверяемое значение может находиться на границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения.
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:666`.

## ЮТУтверждения.МеждуИсключаяГраницы

`Функция ЮТУтверждения.МеждуИсключаяГраницы(НачалоИнтервала, ОкончаниеИнтервала, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет вхождение установленного значения (или его свойства) в заданный интервал.

Проверяемое значение не может находиться на границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения.
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:689`.

## ЮТУтверждения.МеждуВключаяНачалоГраницы

`Функция ЮТУтверждения.МеждуВключаяНачалоГраницы(НачалоИнтервала, ОкончаниеИнтервала, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет вхождение установленного значения (или его свойства) в заданный интервал.

Проверяемое значение может находиться на начальной границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения.
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:712`.

## ЮТУтверждения.МеждуВключаяОкончаниеГраницы

`Функция ЮТУтверждения.МеждуВключаяОкончаниеГраницы(НачалоИнтервала, ОкончаниеИнтервала, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет вхождение установленного значения (или его свойства) в заданный интервал.

Проверяемое значение может находиться на конечной границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения.
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:735`.

## ЮТУтверждения.ИмеетСвойстваРавные

`Функция ЮТУтверждения.ИмеетСвойстваРавные(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет наличие свойств и их значения у проверяемого объекта (или его свойства).

**Параметры:**
- `ОжидаемоеЗначение` — Структура — Проверяемый объект должен иметь свойства, указанные в параметре.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:757`.

## ЮТУтверждения.ЭтоИстина

`Функция ЮТУтверждения.ЭтоИстина(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на равенство `Истина`.
При сравнении также проверяется совпадение типов проверяемого и ожидаемого значения.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:781`.

## ЮТУтверждения.ЭтоНеИстина

`Функция ЮТУтверждения.ЭтоНеИстина(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на НЕ равенство `Истина`.
При сравнении также проверяется совпадение типов проверяемого и ожидаемого значения.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:803`.

## ЮТУтверждения.ЭтоЛожь

`Функция ЮТУтверждения.ЭтоЛожь(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на равенство `Ложь`.
При сравнении также проверяется совпадение типов проверяемого и ожидаемого значения.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:825`.

## ЮТУтверждения.ЭтоНеЛожь

`Функция ЮТУтверждения.ЭтоНеЛожь(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на НЕ равенство `Ложь`.
При сравнении также проверяется совпадение типов проверяемого и ожидаемого значения.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:847`.

## ЮТУтверждения.Существует

`Функция ЮТУтверждения.Существует(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на "существование", т.е. отличие от `Null` и `Неопределено`.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:868`.

## ЮТУтверждения.НеСуществует

`Функция ЮТУтверждения.НеСуществует(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на "не существование", т.е. оно равно `Null` или `Неопределено`.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:889`.

## ЮТУтверждения.ЭтоНеопределено

`Функция ЮТУтверждения.ЭтоНеопределено(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на неравенство `Неопределено`.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:910`.

## ЮТУтверждения.ЭтоНеНеопределено

`Функция ЮТУтверждения.ЭтоНеНеопределено(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на не равенство `Неопределено`.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:931`.

## ЮТУтверждения.ЭтоNull

`Функция ЮТУтверждения.ЭтоNull(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на равенство `Null`.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:952`.

## ЮТУтверждения.ЭтоНеNull

`Функция ЮТУтверждения.ЭтоНеNull(Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на не равенство `Null`.

**Параметры:**
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:973`.

## ЮТУтверждения.ИмеетДлину

`Функция ЮТУтверждения.ИмеетДлину(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на длину или размер коллекции.

Проверка поддерживается для следующих типов проверяемого значения:
- Строка
- Массив
- Структура
- Соответствие
- СписокЗначений
- ФиксированныйМассив
- ФиксированнаяСтруктура
- ФиксированноеСоответствие
- ТаблицаЗначений

**Параметры:**
- `ОжидаемоеЗначение` — Число — Ожидается, что значение (или его свойство) имеет указанную длину или размер.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1015`.

## ЮТУтверждения.ИмеетДлинуБольше

`Функция ЮТУтверждения.ИмеетДлинуБольше(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на длину или размер коллекции и определяет, больше ли эта длина или размер указанного значения.

Проверка поддерживается для следующих типов проверяемого значения:
- Строка
- Массив
- Структура
- Соответствие
- СписокЗначений
- ФиксированныйМассив
- ФиксированнаяСтруктура
- ФиксированноеСоответствие
- ТаблицаЗначений

**Параметры:**
- `ОжидаемоеЗначение` — Число — Ожидается, что значение (или его свойство) имеет длину или размер, превышающий это значение.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1065`.

## ЮТУтверждения.ИмеетДлинуМеньше

`Функция ЮТУтверждения.ИмеетДлинуМеньше(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на длину или размер коллекции и определяет, меньше ли эта длина или размер указанного значения.

Проверка поддерживается для следующих типов проверяемого значения:
- Строка
- Массив
- Структура
- Соответствие
- СписокЗначений
- ФиксированныйМассив
- ФиксированнаяСтруктура
- ФиксированноеСоответствие
- ТаблицаЗначений

**Параметры:**
- `ОжидаемоеЗначение` — Число — Ожидается, что значение (или его свойство) имеет длину или размер, меньшее это значение.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1117`.

## ЮТУтверждения.НеИмеетДлину

`Функция ЮТУтверждения.НеИмеетДлину(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойство) на длину или размер коллекции, которые не должны совпадать с указанным значением.

Проверка поддерживается для следующих типов проверяемого значения:
- Строка
- Массив
- Структура
- Соответствие
- СписокЗначений
- ФиксированныйМассив
- ФиксированнаяСтруктура
- ФиксированноеСоответствие
- ТаблицаЗначения

**Параметры:**
- `ОжидаемоеЗначение` — Число — Ожидается, что значение (или его свойство) не имеет указанную длину или размер.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки, позволяющее более точно понять, что именно проверяется.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1167`.

## ЮТУтверждения.ВыбрасываетИсключение

`Функция ЮТУтверждения.ВыбрасываетИсключение(Знач ОжидаемоеЗначение, Знач ОписаниеПроверки = Неопределено, ВТранзакции = Ложь) Экспорт`

Вызывает метод (см. Метод) объекта и проверяет, выбрасывает ли он исключение.
Проверяет, что метод вызывает исключение, а текст исключения содержит (включает) указанный текст.

**Параметры:**
- `ОжидаемоеЗначение` — Строка — Ожидается, что сообщение об ошибке будет содержать (включать) данный текст.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.
- `ВТранзакции` — Булево — Указывает, что вызов метода выполняется в транзакции.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1200`.

## ЮТУтверждения.НеВыбрасываетИсключение

`Функция ЮТУтверждения.НеВыбрасываетИсключение(Знач ОжидаемоеЗначение = Неопределено, Знач ОписаниеПроверки = Неопределено, ВТранзакции = Ложь) Экспорт`

Вызывает метод (см. Метод) объекта и проверяет, выбрасывает ли он исключение.
Проверяет, что метод не вызывает исключение,
или что при его вызове выбрасывается исключение, текст которого не содержит указанный текст.

**Параметры:**
- `ОжидаемоеЗначение` — Строка — Ожидается, что метод выбросит исключение, текст которого НЕ будет содержать (включать) данный текст. - Неопределено - Ожидается, что метод отработает без выброса исключения.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.
- `ВТранзакции` — Булево — Указывает, что вызов метода выполняется в транзакции.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1246`.

## ЮТУтверждения.ИмеетМетод

`Функция ЮТУтверждения.ИмеетМетод(ИмяМетода, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет, что установленное значение (или его свойство) имеет указанный экспортный метод.

**Параметры:**
- `ИмяМетода` — Строка — Имя метода, наличие которого нужно проверить.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1286`.

## ЮТУтверждения.НачинаетсяС

`Функция ЮТУтверждения.НачинаетсяС(СтрокаПоиска, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет, что установленное значение (или его свойство) начинается на указанное значение.

При этом проверяемое значение должно быть строкой.

**Параметры:**
- `СтрокаПоиска` — Строка — Ожидается, что значение начинается с этой строки поиска.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1319`.

## ЮТУтверждения.ЗаканчиваетсяНа

`Функция ЮТУтверждения.ЗаканчиваетсяНа(СтрокаПоиска, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет, что установленное значение (или его свойство) заканчивается на указанное значение.

При этом проверяемое значение должно быть строкой.

**Параметры:**
- `СтрокаПоиска` — Строка — Ожидается, что значение заканчивается на этой строке поиска.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1354`.

## ЮТУтверждения.КаждыйЭлементСодержитСвойство

`Функция ЮТУтверждения.КаждыйЭлементСодержитСвойство(ИмяСвойства, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет каждый элемент установленного значения (или его свойства); все элементы должны иметь указанное свойство.

**Параметры:**
- `ИмяСвойства` — Строка — Имя свойства, наличие которого нужно проверить у каждого элемента.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1387`.

## ЮТУтверждения.КаждыйЭлементСодержитСвойствоСоЗначением

`Функция ЮТУтверждения.КаждыйЭлементСодержитСвойствоСоЗначением(ИмяСвойства, ОжидаемоеЗначение, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет каждый элемент установленного значения (или его свойства);
все элементы должны иметь указанное свойство, которое равно ожидаемому значению.

**Параметры:**
- `ИмяСвойства` — Строка — Имя свойства, наличие которого нужно проверить у каждого элемента.
- `ОжидаемоеЗначение` — Произвольный — Ожидаемое значение свойства для проверки.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1422`.

## ЮТУтверждения.ЛюбойЭлементСодержитСвойство

`Функция ЮТУтверждения.ЛюбойЭлементСодержитСвойство(ИмяСвойства, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет элементы установленного значения (или его свойства);
хотя бы один из них должен иметь указанное свойство.

**Параметры:**
- `ИмяСвойства` — Строка — Имя свойства, наличие которого нужно проверить у элементов.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1465`.

## ЮТУтверждения.ЛюбойЭлементСодержитСвойствоСоЗначением

`Функция ЮТУтверждения.ЛюбойЭлементСодержитСвойствоСоЗначением(ИмяСвойства, ОжидаемоеЗначение, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет элементы установленного значения (или его свойства);
хотя бы один из них должен иметь указанное свойство, которое равно ожидаемому значению.

**Параметры:**
- `ИмяСвойства` — Строка — Имя свойства, наличие которого нужно проверить у элементов.
- `ОжидаемоеЗначение` — Произвольный — Ожидаемое значение свойства для проверки.
- `ОписаниеПроверки` — Строка — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1506`.

## ЮТУтверждения.СоответствуетПредикату

`Функция ЮТУтверждения.СоответствуетПредикату(Предикат, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет установленное значение (или его свойства) на соответствие утверждениям предиката.
См. ЮТест.Предикат.

**Параметры:**
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор утверждений, см. ЮТПредикаты.Получить. - См. ЮТФабрика.ВыражениеПредиката. - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат.
- `ОписаниеПроверки` — Строка, Неопределено — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1565`.

## ЮТУтверждения.КаждыйЭлементСоответствуетПредикату

`Функция ЮТУтверждения.КаждыйЭлементСоответствуетПредикату(Предикат, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет каждый элемент установленного значения (или его свойства);
все элементы должны соответствовать утверждениям предиката.
См. ЮТест.Предикат.

**Параметры:**
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор утверждений, см. ЮТПредикаты.Получить. - См. ЮТФабрика.ВыражениеПредиката. - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат.
- `ОписаниеПроверки` — Строка, Неопределено — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1604`.

## ЮТУтверждения.ЛюбойЭлементСоответствуетПредикату

`Функция ЮТУтверждения.ЛюбойЭлементСоответствуетПредикату(Предикат, ОписаниеПроверки = Неопределено) Экспорт`

Проверяет элементы установленного значения (или его свойства);
хотя бы один из них должен соответствовать утверждениям предиката.
См. ЮТест.Предикат.

**Параметры:**
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор утверждений, см. ЮТПредикаты.Получить. - См. ЮТФабрика.ВыражениеПредиката. - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат.
- `ОписаниеПроверки` — Строка, Неопределено — Описание конкретной проверки.

**Возвращает:**
- CommonModule.ЮТУтверждения - Этот модуль для продолжения формирования утверждений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1646`.

## ЮТУтверждения.СброситьКонтекст

`Процедура ЮТУтверждения.СброситьКонтекст() Экспорт`

Сбросить контекст.
Используется для принудительного сброса контекста утверждения.
Это позволяет очистить состояние текущих проверок и начать новую серию утверждений.

Примечание: После вызова этой процедуры все предыдущие утверждения будут сброшены,
и новые проверки можно будет выполнять с чистого листа.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1685`.

## ЮТУтверждения.ПередКаждымТестом

`Процедура ЮТУтверждения.ПередКаждымТестом(ОписаниеСобытия) Экспорт`

Обработчик события "ПередКаждымТестом"

**Параметры:**
- `ОписаниеСобытия` — см. ЮТФабрикаСлужебный.ОписаниеСобытияИсполненияТестов

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1696`.

## ЮТУтверждения.Контекст

`Функция ЮТУтверждения.Контекст() Экспорт`

Контекст.

**Возвращает:**
- см. НовыйКонтекстУтверждения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТУтверждения/Module.bsl:1712`.
