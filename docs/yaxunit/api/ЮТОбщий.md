---
title: API ЮТОбщий
description: "Публичные экспортные методы модуля ЮТОбщий из ядра YaXUnit."
tags: [yaxunit, api, ЮТОбщий]
publish_publicly: false
---

# API ЮТОбщий

Компактный справочник по 48 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТОбщий.ЗначениеСвойства

`Функция ЮТОбщий.ЗначениеСвойства(Объект, ИмяСвойства) Экспорт`

Возвращает значение свойства объекта по пути.

**Параметры:**
- `Объект` — Произвольный — Объект, свойство которого нужно получить.
- `ИмяСвойства` — Строка — Путь к свойству. Примеры: "ИмяСвойства.ИмяВложенногоСвойства", "[2].ИмяСвойства", "ИмяСвойства[2].ИмяВложенногоСвойства". - Число - Индекс элемента. Возможен выбор элемента с конца, для этого нужно указывать отрицательный номер элемента с конца, например: '-1' - последний элемент, '-2' - предпоследний.

**Возвращает:**
- `Произвольный` — Значение свойства.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:41`.

## ЮТОбщий.ДобавитьКДате

`Функция ЮТОбщий.ДобавитьКДате(Дата, Интервал, ТипИнтервала) Экспорт`

Добавляет к дате указанное значение временных интервалов.

**Параметры:**
- `Дата` — Дата — Исходная дата.
- `Интервал` — Число — Добавляемое значение.
- `ТипИнтервала` — Строка — Тип интервала (год, квартал, месяц, день, час, минута, секунда).

**Возвращает:**
- `Дата` — Результирующая дата.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:95`.

## ЮТОбщий.ПараметрыЗаписи

`Функция ЮТОбщий.ПараметрыЗаписи() Экспорт`

Возвращает параметры записи объекта по умолчанию.

**Возвращает:**
- `Структура` — Параметры записи: * ОбменДаннымиЗагрузка - Булево - Значение опции движка ЮТКонтекст.ОпцииДвижка().ОбменДаннымиЗагрузкаПриСозданииДанных. * ДополнительныеСвойства - Структура - Пустая структура для дополнительных свойств. * РежимЗаписи - Неопределено - Режим записи документа (по умолчанию не установлен). * УникальныйИдентификаторСсылки - Неопределено - Уникальный идентификатор для новой ссылки (по умолчанию не установлен).

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:121`.

## ЮТОбщий.Инкремент

`Функция ЮТОбщий.Инкремент(Значение, Знач Шаг = 1) Экспорт`

Инкрементирует значение.

**Параметры:**
- `Значение` — Число — Исходное значение.
- `Шаг` — Число — Шаг инкремента. По умолчанию 1.

**Возвращает:**
- `Число` — Результат инкремента.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:156`.

## ЮТОбщий.ЧислоВСтроку

`Функция ЮТОбщий.ЧислоВСтроку(Значение) Экспорт`

Преобразует число в строку без разделителей групп разрядов.

**Параметры:**
- `Значение` — Число — Преобразуемое число.

**Возвращает:**
- `Строка` — Строковое представление числа.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:174`.

## ЮТОбщий.ПредставлениеПродолжительности

`Функция ЮТОбщий.ПредставлениеПродолжительности(Знач Продолжительность) Экспорт`

Формирует представление продолжительности в удобном для восприятия виде.

**Параметры:**
- `Продолжительность` — Число — Продолжительность в миллисекундах.

**Возвращает:**
- `Строка` — Представление продолжительности в формате "X.XXX сек".

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:195`.

## ЮТОбщий.ПредставлениеУниверсальнойДата

`Функция ЮТОбщий.ПредставлениеУниверсальнойДата(Знач УниверсальнаяДатаВМиллисекундах = Неопределено, ТолькоВремя = Ложь) Экспорт`

Преобразует универсальную дату в миллисекундах в строковое представление.

**Параметры:**
- `УниверсальнаяДатаВМиллисекундах` — Число — Универсальная дата в миллисекундах.
- `ТолькоВремя` — Булево — Признак вывода только времени.

**Возвращает:**
- `Строка` — Строковое представление даты и времени.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:219`.

## ЮТОбщий.ДанныеТекстовогоФайла

`Функция ЮТОбщий.ДанныеТекстовогоФайла(ИмяФайла) Экспорт`

Читает данные из текстового файла

**Статус:** устарел. Используйте `ЮТФайлы.ДанныеТекстовогоФайла`. Переход объявлен в `24.12`.

**Параметры:**
- `ИмяФайла` — Строка — Путь к файлу

**Возвращает:**
- `Строка` — Содержимое файла

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:247`.

## ЮТОбщий.ЗначениеИзJSON

`Функция ЮТОбщий.ЗначениеИзJSON(СтрокаJSON) Экспорт`

Преобразует JSON строку в значение

**Параметры:**
- `СтрокаJSON` — Строка — JSON строка.

**Возвращает:**
- `Произвольный` — Значение, полученное из JSON.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:265`.

## ЮТОбщий.ПеременнаяСодержитСвойство

`Функция ЮТОбщий.ПеременнаяСодержитСвойство(Переменная, ИмяСвойства) Экспорт`

Проверяет наличие свойства у значения любого типа данных.
Если передано НЕОПРЕДЕЛЕНО, то функция всегда вернет Ложь.

**Параметры:**
- `Переменная` — Произвольный — Переменная любого типа, для которой необходимо проверить наличие свойства.
- `ИмяСвойства` — Строка — Имя искомого свойства.

**Возвращает:**
- `Булево` — Признак наличия свойства у значения.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:296`.

## ЮТОбщий.СвойствоПеременной

`Функция ЮТОбщий.СвойствоПеременной(Переменная, ИмяСвойства, ЗначениеПоУмолчанию = Неопределено) Экспорт`

Возвращает значение свойства переменной любого типа данных.

**Параметры:**
- `Переменная` — Произвольный — Переменная любого типа, для которой необходимо получить значение свойства.
- `ИмяСвойства` — Строка — Имя свойства.
- `ЗначениеПоУмолчанию` — Произвольный — Значение, которое будет возвращено, если свойство отсутствует.

**Возвращает:**
- `Произвольный` — Значение свойства или значение по умолчанию.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:327`.

## ЮТОбщий.СообщитьПользователю

`Процедура ЮТОбщий.СообщитьПользователю(ТекстСообщенияПользователю) Экспорт`

СообщитьПользователю
Формирует и выводит сообщение

**Параметры:**
- `ТекстСообщенияПользователю` — Строка — Текст сообщения.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:348`.

## ЮТОбщий.СтрокаJSON

`Функция ЮТОбщий.СтрокаJSON(Значение, ИспользоватьСериализатор = Истина, Однострочный = Ложь) Экспорт`

Преобразует значение в JSON строку.

**Параметры:**
- `Значение` — Произвольный — Значение для преобразования.
- `ИспользоватьСериализатор` — Булево — Признак использования сериализатора XDTO. По умолчанию Истина.
- `Однострочный` — Булево — Признак формирования однострочного JSON. По умолчанию Ложь.

**Возвращает:**
- `Строка` — JSON строка.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:370`.

## ЮТОбщий.ПредставлениеЗначения

`Функция ЮТОбщий.ПредставлениеЗначения(Значение) Экспорт`

Возвращает строковое представление значения.
Все типы кроме ТаблицыЗначений пытается преобразовать в JSON, если не удалось - использует стандартное преобразование в строку.
Таблицу значений преобразовывает в строку формата MarkDown.

**Параметры:**
- `Значение` — Произвольный — Значение для преобразования.

**Возвращает:**
- `Строка` — Строковое представление значения.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:422`.

## ЮТОбщий.УстановленБезопасныйРежим

`Функция ЮТОбщий.УстановленБезопасныйРежим() Экспорт`

Проверяет, установлен ли безопасный режим

**Возвращает:**
- `Булево` — Признак установленного безопасного режима

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:442`.

## ЮТОбщий.МестноеВремяПоВременнойМетке

`Функция ЮТОбщий.МестноеВремяПоВременнойМетке(Метка) Экспорт`

Преобразует временную метку в местное время

**Параметры:**
- `Метка` — Число — Временная метка в миллисекундах

**Возвращает:**
- `Дата` — Местное время

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:455`.

## ЮТОбщий.ПродолжительностьВСекундах

`Функция ЮТОбщий.ПродолжительностьВСекундах(Продолжительность) Экспорт`

Преобразует продолжительность из миллисекунд в секунды.

**Параметры:**
- `Продолжительность` — Число — Продолжительность в миллисекундах.

**Возвращает:**
- `Число` — Продолжительность в секундах.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:476`.

## ЮТОбщий.Модуль

`Функция ЮТОбщий.Модуль(ИмяМодуля) Экспорт`

Возвращает модуль по его имени.
Выполняет безопасное вычисление имени модуля.

**Параметры:**
- `ИмяМодуля` — Строка — Имя модуля (например, "ОбщегоНазначения", "МойОбщийМодульКлиентСервер").

**Возвращает:**
- `Произвольный` — Объект модуля или Неопределено, если модуль не найден или произошла ошибка при его получении.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:496`.

## ЮТОбщий.Менеджер

`Функция ЮТОбщий.Менеджер(Знач Менеджер) Экспорт`

Возвращает менеджер объекта метаданных.

**Параметры:**
- `Менеджер` — Строка — Имя объекта метаданных (например, "Справочник.Номенклатура", "Документ.РеализацияТоваровУслуг").

**Возвращает:**
- `Произвольный` — Менеджер объекта метаданных. Вызывает исключение, если объект не найден или метод недоступен на клиенте.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:515`.

## ЮТОбщий.ТипСтруктуры

`Функция ЮТОбщий.ТипСтруктуры(Структура) Экспорт`

Возвращает тип структуры из специального реквизита `__type__`.

**Параметры:**
- `Структура` — Структура — Исходная структура, у которой должен быть ключ `__type__`.

**Возвращает:**
- `Строка` — Значение реквизита `__type__` или пустая строка, если реквизит отсутствует.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:545`.

## ЮТОбщий.ЭтаСтруктураИмеетТип

`Функция ЮТОбщий.ЭтаСтруктураИмеетТип(Структура, ИмяТипа) Экспорт`

Проверяет, имеет ли структура указанный тип, сравнивая значение реквизита `__type__`.

**Параметры:**
- `Структура` — Структура — Проверяемая структура.
- `ИмяТипа` — Строка — Имя типа для сравнения.

**Возвращает:**
- `Булево` — Истина, если реквизит `__type__` структуры равен ИмяТипа, иначе Ложь.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:565`.

## ЮТОбщий.УказатьТипСтруктуры

`Процедура ЮТОбщий.УказатьТипСтруктуры(Структура, ИмяТипа) Экспорт`

Устанавливает тип структуры, записывая его в специальный реквизит `__type__`.

**Параметры:**
- `Структура` — Структура — Структура, для которой устанавливается тип.
- `ИмяТипа` — Строка — Имя типа, которое будет присвоено реквизиту `__type__`.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:581`.

## ЮТОбщий.Пауза

`Процедура ЮТОбщий.Пауза(Время) Экспорт`

Приостанавливает поток выполнения на указанное количество секунд

**Параметры:**
- `Время` — Число — Продолжительность паузы в секундах, возможно указывать дробное значение

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:591`.

## ЮТОбщий.ВывестиВКонсоль

`Процедура ЮТОбщий.ВывестиВКонсоль(Знач Сообщение) Экспорт`

Выводит сообщение в консоль (stdout) приложения

**Параметры:**
- `Сообщение` — Строка — Выводимое сообщение

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:611`.

## ЮТОбщий.Макет

`Функция ЮТОбщий.Макет(ИмяМакета) Экспорт`

Возвращает макет или его область.

**Параметры:**
- `ИмяМакета` — Строка — Полное имя макета или области макета. Возможные значения * Общий макет, например, "ОбщийМакет.ЮТМетаданные". * Макет объекта метаданных, например, "Справочник.Товары.ПечатнаяФорма". * Область макета, например, "Справочник.Товары.ПечатнаяФорма.Шапка", "ОбщийМакет.ЮТМетаданные.Заголовок".

**Возвращает:**
- ТабличныйДокумент, ТекстовыйДокумент, ДвоичныеДанные - Макет или его область. Тип зависит от вида макета. Вызывает исключение, если макет не найден.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:646`.

## ЮТОбщий.ДобавитьСтроку

`Функция ЮТОбщий.ДобавитьСтроку(ИсходнаяСтрока, ДополнительнаяСтрока, Разделитель = ";") Экспорт`

Устарела. Метод перенесен в см. ЮТСтроки.ДобавитьСтроку
Конкатенирует строки, разделяя их разделителем

**Статус:** устарел. Используйте `ЮТСтроки.ДобавитьСтроку`. Переход объявлен в `24.03`.

**Параметры:**
- `ИсходнаяСтрока` — Строка	 — Исходная строка
- `ДополнительнаяСтрока` — Строка	 — Добавляемая строка
- `Разделитель` — Строка	 — Строка разделитель, любой набор символов — разделитель между подстроками

**Возвращает:**
- `Строка` — Результат конкатенации строк

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:664`.

## ЮТОбщий.РазложитьСтрокуВМассивПодстрок

`Функция ЮТОбщий.РазложитьСтрокуВМассивПодстрок(Значение, Разделитель = ";") Экспорт`

Устарела. Метод перенесен в см. ЮТСтроки.РазделитьСтроку
Возвращает массив на основании строки

**Статус:** устарел. Используйте `ЮТСтроки.РазделитьСтроку`. Переход объявлен в `24.03`.

**Параметры:**
- `Значение` — Строка — преобразуемая строка
- `Разделитель` — Строка — строка-разделитель

**Возвращает:**
- Массив Из Строка - массив строк

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:681`.

## ЮТОбщий.СформироватьСтрокуСимволов

`Функция ЮТОбщий.СформироватьСтрокуСимволов(Символ, Количество) Экспорт`

Устарела. Метод перенесен в см. ЮТСтроки.СтрокаСимволов
Формирует строку из заданного количества повторяемых символов

**Статус:** устарел. Используйте `ЮТСтроки.СтрокаСимволов`. Переход объявлен в `24.03`.

**Параметры:**
- `Символ` — Строка — Повторяемый символ
- `Количество` — Число — Количество повторений

**Возвращает:**
- `Строка` — Строка повторяемых символов

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:696`.

## ЮТОбщий.ЗначениеСтруктуры

`Функция ЮТОбщий.ЗначениеСтруктуры(ИсходнаяСтруктура, ИмяПоля, ЗначениеПоУмолчанию = Неопределено) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ЗначениеСтруктуры
Возвращает требуемое поле структуры. В случае отсутствия поля возвращает значение по умолчанию

**Статус:** устарел. Используйте `ЮТКоллекции.ЗначениеСтруктуры`. Переход объявлен в `24.03`.

**Параметры:**
- `ИсходнаяСтруктура` — Структура — Исходная структура
- `ИмяПоля` — Строка — Имя поля структуры
- `ЗначениеПоУмолчанию` — Произвольный — Значение, которое будет возвращено, если поля в структуре нет

**Возвращает:**
- `Произвольный` — Значение искомого поля структуры

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:713`.

## ЮТОбщий.ОбъединитьВСтруктуру

`Функция ЮТОбщий.ОбъединитьВСтруктуру(Знач Коллекция1, Коллекция2) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ДополнитьСтруктуру
Функция, объединяющая две коллекции( с типами Структура или Соответствие) в одну структуру, если это возможно

**Статус:** устарел. Используйте `ЮТКоллекции.ДополнитьСтруктуру`. Переход объявлен в `24.03`.

**Параметры:**
- `Коллекция1` — Соответствие из Произвольный - Структура
- `Коллекция2` — Соответствие из Произвольный - Структура

**Возвращает:**
- `Структура` — Результат объединения двух коллекций

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:732`.

## ЮТОбщий.СкопироватьРекурсивно

`Функция ЮТОбщий.СкопироватьРекурсивно(Источник) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.СкопироватьРекурсивно
Создает копию экземпляра указанного объекта.
Примечание:
Функцию нельзя использовать для объектных типов (СправочникОбъект, ДокументОбъект и т.п.).

**Статус:** устарел. Используйте `ЮТКоллекции.СкопироватьРекурсивно`. Переход объявлен в `24.03`.

**Параметры:**
- `Источник` — Произвольный	 — объект, который необходимо скопировать.

**Возвращает:**
- `Произвольный` — копия объекта, переданного в параметре Источник.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:757`.

## ЮТОбщий.СкопироватьСтруктуру

`Функция ЮТОбщий.СкопироватьСтруктуру(Источник) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.СкопироватьСтруктуру
Создает копию значения типа Структура

**Статус:** устарел. Используйте `ЮТКоллекции.СкопироватьСтруктуру`. Переход объявлен в `24.03`.

**Параметры:**
- `Источник` — Структура — копируемая структура

**Возвращает:**
- `Структура` — копия исходной структуры.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:773`.

## ЮТОбщий.СкопироватьСоответствие

`Функция ЮТОбщий.СкопироватьСоответствие(Источник) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.СкопироватьСоответствие
Создает копию значения типа Соответствие.

**Статус:** устарел. Используйте `ЮТКоллекции.СкопироватьСоответствие`. Переход объявлен в `24.03`.

**Параметры:**
- `Источник` — Соответствие из Произвольный — соответствие, копию которого необходимо получить.

**Возвращает:**
- Соответствие Из Произвольный - копия исходного соответствия.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:789`.

## ЮТОбщий.СкопироватьМассив

`Функция ЮТОбщий.СкопироватьМассив(Источник) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.СкопироватьМассив
Создает копию значения типа Массив.

**Статус:** устарел. Используйте `ЮТКоллекции.СкопироватьМассив`. Переход объявлен в `24.03`.

**Параметры:**
- `Источник` — Массив Из Произвольный — массив, копию которого необходимо получить

**Возвращает:**
- Массив Из Произвольный - копия исходного массива.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:805`.

## ЮТОбщий.СкопироватьСписокЗначений

`Функция ЮТОбщий.СкопироватьСписокЗначений(Источник) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.СкопироватьСписокЗначений
Создает копию значения типа СписокЗначений.

**Статус:** устарел. Используйте `ЮТКоллекции.СкопироватьСписокЗначений`. Переход объявлен в `24.03`.

**Параметры:**
- `Источник` — СписокЗначений Из Произвольный — список значений, копию которого необходимо получить

**Возвращает:**
- СписокЗначений Из Произвольный - копия исходного списка значений

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:821`.

## ЮТОбщий.ВыгрузитьЗначения

`Функция ЮТОбщий.ВыгрузитьЗначения(Коллекция, ИмяРеквизита) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ВыгрузитьЗначения

**Статус:** устарел. Используйте `ЮТКоллекции.ВыгрузитьЗначения`. Переход объявлен в `24.03`.

**Параметры:**
- `Коллекция` — Произвольный
- `ИмяРеквизита` — Строка

**Возвращает:**
- Массив из Произвольный - Выгрузить значения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:836`.

## ЮТОбщий.ПересечениеМассивов

`Функция ЮТОбщий.ПересечениеМассивов(Коллекция1, Коллекция2) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ПересечениеМассивов.

**Статус:** устарел. Используйте `ЮТКоллекции.ПересечениеМассивов`. Переход объявлен в `24.03`.

**Параметры:**
- `Коллекция1` — Массив из Произвольный
- `Коллекция2` — Массив из Произвольный

**Возвращает:**
- Массив из Произвольный - Пересечение массивов

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:851`.

## ЮТОбщий.ЗначениеВМассиве

`Функция ЮТОбщий.ЗначениеВМассиве(Значение, Значение2 = "_!%*", Значение3 = "_!%*", Значение4 = "_!%*", Значение5 = "_!%*", Значение6 = "_!%*", Значение7 = "_!%*", Значение8 = "_!%*", Значение9 = "_!%*", Значение10 = "_!%*") Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ЗначениеВМассиве.
Создает массив с переданными значениями.

**Статус:** устарел. Используйте `ЮТКоллекции.ЗначениеВМассиве`. Переход объявлен в `24.03`.

**Параметры:**
- `Значение` — Произвольный — Первое значение для добавления в массив.
- `Значение2` — Произвольный — Второе значение для добавления в массив (опционально).
- `Значение3` — Произвольный — Третье значение для добавления в массив (опционально).
- `Значение4` — Произвольный — Четвертое значение для добавления в массив (опционально).
- `Значение5` — Произвольный — Пятое значение для добавления в массив (опционально).
- `Значение6` — Произвольный — Шестое значение для добавления в массив (опционально).
- `Значение7` — Произвольный — Седьмое значение для добавления в массив (опционально).
- `Значение8` — Произвольный — Восьмое значение для добавления в массив (опционально).
- `Значение9` — Произвольный — Девятое значение для добавления в массив (опционально).
- `Значение10` — Произвольный — Десятое значение для добавления в массив (опционально).

**Возвращает:**
- Массив из Произвольный - Массив, содержащий переданные значения.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:882`.

## ЮТОбщий.ДополнитьМассив

`Процедура ЮТОбщий.ДополнитьМассив(Приемник, Источник) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ДополнитьМассив
Дополняет один массив элементами другого массива.

**Статус:** устарел. Используйте `ЮТКоллекции.ДополнитьМассив`. Переход объявлен в `24.03`.

**Параметры:**
- `Приемник` — Массив из Произвольный — Массив, в который будут добавлены элементы. Этот массив изменяется.
- `Источник` — Массив из Произвольный — Массив, элементы которого будут добавлены в массив-приемник.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:910`.

## ЮТОбщий.ВСоответствие

`Функция ЮТОбщий.ВСоответствие(Коллекция, ИмяПоляКлюча, ИмяПоляЗначения = Неопределено) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ВСоответствие
Возвращает соответствие элементов переданной коллекции, в качестве ключей выступают значения указанного поля элементов коллекции.

**Статус:** устарел. Используйте `ЮТКоллекции.ВСоответствие`. Переход объявлен в `24.03`.

**Параметры:**
- `Коллекция` — Произвольный — значение, для которого определен итератор, и возможно обращение к полям элементов через квадратные скобки.
- `ИмяПоляКлюча` — Строка — имя поля элемента коллекции, которое будет ключом соответствия.
- `ИмяПоляЗначения` — Строка — если указан, значениями результата будут не элементы, а значения соответствующих полей элементов коллекции.

**Возвращает:**
- Соответствие Из Произвольный - полученное соответствие.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:926`.

## ЮТОбщий.ВСтруктуру

`Функция ЮТОбщий.ВСтруктуру(Коллекция, ИмяПоляКлюча, ИмяПоляЗначения = Неопределено) Экспорт`

Устарела. Метод перенесен в см. ЮТКоллекции.ВСтруктуру.
Возвращает структуру элементов переданной коллекции,
в качестве ключей выступают значения указанного поля элементов коллекции.

**Статус:** устарел. Используйте `ЮТКоллекции.ВСтруктуру`. Переход объявлен в `24.03`.

**Параметры:**
- `Коллекция` — Произвольный — Коллекция (например, Массив, ТаблицаЗначений), для элементов которой можно получить значение по имени поля. Итератор коллекции должен быть доступен.
- `ИмяПоляКлюча` — Строка — Имя поля элемента коллекции, значение которого будет использовано в качестве ключа в результирующей Структуре.
- `ИмяПоляЗначения` — Строка — Имя поля элемента коллекции, значение которого будет использовано в качестве значения в результирующей Структуре. Если параметр не указан, в качестве значения используется сам элемент коллекции.

**Возвращает:**
- Структура из Произвольный - Новая структура, где ключами являются значения поля `ИмяПоляКлюча`, а значениями - либо сами элементы коллекции, либо значения поля `ИмяПоляЗначения`.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:947`.

## ЮТОбщий.МетодМодуляСуществует

`Функция ЮТОбщий.МетодМодуляСуществует(ИмяМодуля, ИмяМетода, КоличествоПараметров = 0, Кешировать = Истина) Экспорт`

Устарела. МетодМодуляСуществует
Проверяет существование публичного (экспортного) метода у модуля

**Статус:** устарел.

**Параметры:**
- `ИмяМодуля` — Строка — Имя модуля, метод которого нужно поискать
- `ИмяМетода` — Строка — Имя метода, который ищем
- `КоличествоПараметров` — Число — Количество параметров метода, увы это никак не влияет на проверку
- `Кешировать` — Булево — Признак кеширования результата проверки

**Возвращает:**
- `Булево` — Метод найден

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:965`.

## ЮТОбщий.МетодОбъектаСуществует

`Функция ЮТОбщий.МетодОбъектаСуществует(Объект, ИмяМетода) Экспорт`

Устарела. Следует использовать см. ЮТМетодыСлужебный.МетодОбъектаСуществует.
Проверяет существование публичного (экспортного) метода у объекта.

**Статус:** устарел.

**Параметры:**
- `Объект` — Произвольный — Объект, наличие метода у которого необходимо проверить.
- `ИмяМетода` — Строка — Имя искомого метода.

**Возвращает:**
- `Булево` — Истина, если экспортный метод с указанным именем существует у объекта, иначе Ложь.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:987`.

## ЮТОбщий.ВыполнитьМетод

`Функция ЮТОбщий.ВыполнитьМетод(ПолноеИмяМетода, Параметры = Неопределено, Объект = Неопределено) Экспорт`

Устарела.

**Статус:** устарел.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:995`.

## ЮТОбщий.ВычислитьБезопасно

`Функция ЮТОбщий.ВычислитьБезопасно(Выражение) Экспорт`

Устарела.

**Статус:** устарел.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:1007`.

## ЮТОбщий.ПредставлениеТипа

`Функция ЮТОбщий.ПредставлениеТипа(Тип) Экспорт`

Устарела. Следует использовать см. ЮТТипыДанныхСлужебный.ПредставлениеТипа.
Возвращает строковое представление типа значения.

**Статус:** устарел.

**Параметры:**
- `Тип` — Тип — Тип, для которого необходимо получить строковое представление.

**Возвращает:**
- `Строка` — Строковое представление типа (например, "Число", "Строка", "ДокументСсылка.РеализацияТоваровУслуг").

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:1026`.

## ЮТОбщий.ОписаниеТиповЛюбаяСсылка

`Функция ЮТОбщий.ОписаниеТиповЛюбаяСсылка() Экспорт`

Устарела. Описание типов любая ссылка.

**Статус:** устарел.

**Возвращает:**
- `ОписаниеТипов` — Описание типов любая ссылка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:1037`.

## ЮТОбщий.ЧастиПути

`Функция ЮТОбщий.ЧастиПути(Цепочка) Экспорт`

Разбирает строку пути к свойству на массив частей.
Поддерживает доступ к элементам по индексу (включая отрицательные для массивов) и к свойствам вложенных объектов.

**Параметры:**
- `Цепочка` — Строка, Число — Строка, представляющая путь к свойству (например, "ИмяСвойства.ВложенноеСвойство[0].ЕщеСвойство") или числовой индекс для прямого доступа к элементу массива.

**Возвращает:**
- Массив из Произвольный - Массив, содержащий части пути. Строковые части представляют имена свойств, числовые части - индексы доступа к элементам.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОбщий/Module.bsl:1070`.
