---
title: API ЮТПодражатель
description: "Публичные экспортные методы модуля ЮТПодражатель из ядра YaXUnit."
tags: [yaxunit, api, ЮТПодражатель]
publish_publicly: false
---

# API ЮТПодражатель

Компактный справочник по 5 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТПодражатель.Локализация

`Функция ЮТПодражатель.Локализация() Экспорт`

Локализация.

**Возвращает:**
- `Строка` — Возвращает текущую установленную локализацию

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПодражатель/Module.bsl:28`.

## ЮТПодражатель.УстановитьЛокализацию

`Процедура ЮТПодражатель.УстановитьЛокализацию(КодЯзыка) Экспорт`

Устанавливает локализацию подражателя.
В дальнейшем используется для получения списка словарей с указанной локализацией.

**Параметры:**
- `КодЯзыка` — Строка — Код языка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПодражатель/Module.bsl:42`.

## ЮТПодражатель.Люди

`Функция ЮТПодражатель.Люди() Экспорт`

Возвращает подражателя для генерации данных о людях.

**Возвращает:**
- CommonModule.ЮТПодражатель_Люди - Модуль для генерации данных о людях.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПодражатель/Module.bsl:58`.

## ЮТПодражатель.Компании

`Функция ЮТПодражатель.Компании() Экспорт`

Возвращает подражателя для генерации данных о компаниях.

**Возвращает:**
- CommonModule.ЮТПодражатель_Компании - Модуль для генерации данных о компаниях.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПодражатель/Module.bsl:72`.

## ЮТПодражатель.Банки

`Функция ЮТПодражатель.Банки() Экспорт`

Возвращает подражателя для генерации данных о банках.

**Возвращает:**
- CommonModule.ЮТПодражатель_Банки - Модуль для генерации данных о банках.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПодражатель/Module.bsl:86`.
