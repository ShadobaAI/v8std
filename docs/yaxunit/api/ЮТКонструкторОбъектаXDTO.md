---
title: API ЮТКонструкторОбъектаXDTO
description: "Публичные экспортные методы модуля ЮТКонструкторОбъектаXDTO из ядра YaXUnit."
tags: [yaxunit, api, ЮТКонструкторОбъектаXDTO]
publish_publicly: false
---

# API ЮТКонструкторОбъектаXDTO

Компактный справочник по 11 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКонструкторОбъектаXDTO.Установить

`Функция ЮТКонструкторОбъектаXDTO.Установить(ИмяРеквизита, Значение) Экспорт`

Устанавливает значение свойства объекта XDTO.

**Параметры:**
- `ИмяРеквизита` — Строка — Имя свойства объекта XDTO.
- `Значение` — Произвольный — Значение свойства объекта XDTO.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:44`.

## ЮТКонструкторОбъектаXDTO.УстановитьРеквизиты

`Функция ЮТКонструкторОбъектаXDTO.УстановитьРеквизиты(ЗначенияРеквизитов) Экспорт`

Устанавливает значения свойств объекта XDTO.

**Параметры:**
- `ЗначенияРеквизитов` — Структура — Структура, где ключ — имя свойства, значение — значение свойства.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:64`.

## ЮТКонструкторОбъектаXDTO.Фикция

`Функция ЮТКонструкторОбъектаXDTO.Фикция(ИмяРеквизита) Экспорт`

Устанавливает фиктивное значение для указанного свойства объекта XDTO.

Особенности генерации фиктивных значений:
* Для примитивных типов генерируется случайное значение.
* Для объектных типов создается новый объект XDTO и рекурсивно заполняется фиктивными значениями.
* Для коллекций генерируется случайное количество элементов (на основании типа коллекции)
и каждый элемент заполняется фиктивными значениями.

**Параметры:**
- `ИмяРеквизита` — Строка — Имя свойства объекта XDTO.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:91`.

## ЮТКонструкторОбъектаXDTO.ФикцияРеквизитов

`Функция ЮТКонструкторОбъектаXDTO.ФикцияРеквизитов(ИменаРеквизитов) Экспорт`

Устанавливает фиктивные значения для указанных свойств объекта XDTO.

Особенности генерации фиктивных значений см. ЮТКонструкторОбъектаXDTO.Фикция

**Параметры:**
- `ИменаРеквизитов` — Строка — Имена свойств объекта XDTO, разделенные запятыми. - Массив из Строка - Массив имен свойств объекта XDTO.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:116`.

## ЮТКонструкторОбъектаXDTO.ФикцияОбязательныхПолей

`Функция ЮТКонструкторОбъектаXDTO.ФикцияОбязательныхПолей() Экспорт`

Заполняет обязательные свойства объекта XDTO фиктивными значениями.

Обязательные свойства определяются на основании метаданных XDTO (свойство.ВозможноПустое = Ложь).
Особенности генерации фиктивных значений см. ЮТКонструкторОбъектаXDTO.Фикция

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:143`.

## ЮТКонструкторОбъектаXDTO.ФикцияНезаполненных

`Функция ЮТКонструкторОбъектаXDTO.ФикцияНезаполненных() Экспорт`

Заполняет все незаполненные свойства объекта XDTO фиктивными значениями.

Особенности генерации фиктивных значений см. ЮТКонструкторОбъектаXDTO.Фикция

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:167`.

## ЮТКонструкторОбъектаXDTO.ФикцияНезаполненныхИсключая

`Функция ЮТКонструкторОбъектаXDTO.ФикцияНезаполненныхИсключая(ИменаИсключений) Экспорт`

Заполняет неустановленные реквизиты объекта фиктивными значениями, исключая указанные

На основании типа объекта определяются обязательные поля.
Для них генерируются и устанавливаются фиктивные значение.

* Для примитивных значение генерируется случайное значение
* Для объектных типов создается новый объект
* Для коллекций - генерируется случайно количество случайных элементов (на основании типа)

**Параметры:**
- `ИменаИсключений` — Строка — Имена исключаемых реквизитов объекта или табличной части разделенные запятыми - Массив из Строка - Имена исключаемых реквизитов объекта или табличной части

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:196`.

## ЮТКонструкторОбъектаXDTO.ДанныеОбъекта

`Функция ЮТКонструкторОбъектаXDTO.ДанныеОбъекта() Экспорт`

Возвращает корневой (основной) сконструированный объект XDTO.

**Возвращает:**
- `ОбъектXDTO` — Сконструированный объект XDTO.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:237`.

## ЮТКонструкторОбъектаXDTO.ДобавитьНовый

`Функция ЮТКонструкторОбъектаXDTO.ДобавитьНовый(ИмяРеквизита) Экспорт`

Добавляет новый объект XDTO в свойство-коллекцию текущего объекта XDTO
и переключает контекст конструктора на добавленный объект.

**Параметры:**
- `ИмяРеквизита` — Строка — Имя свойства-коллекции, в которую будет добавлен новый объект.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов. Контекст конструктора переключен на добавленный объект.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:261`.

## ЮТКонструкторОбъектаXDTO.ПерейтиКВладельцу

`Функция ЮТКонструкторОбъектаXDTO.ПерейтиКВладельцу() Экспорт`

Переключает контекст конструктора на родительский объект XDTO в стеке объектов.
Используется после вызова метода ДобавитьНовый для возврата к конструированию родительского объекта.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Текущий конструктор для цепочки вызовов. Контекст конструктора переключен на родительский объект.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:293`.

## ЮТКонструкторОбъектаXDTO.Инициализировать

`Процедура ЮТКонструкторОбъектаXDTO.Инициализировать(ИмяТипаОбъекта, ПространствоИмен, ФабрикаОбъектов = Неопределено) Экспорт`

Инициализирует конструктор для указанного типа объекта XDTO.
Создает корневой объект XDTO и помещает его в стек.

**Параметры:**
- `ИмяТипаОбъекта` — Строка — Имя типа объекта XDTO.
- `ПространствоИмен` — Строка — URI пространства имен типа объекта XDTO.
- `ФабрикаОбъектов` — ФабрикаXDTO — (необязательный) Фабрика XDTO для создания объектов. По умолчанию используется глобальная ФабрикаXDTO.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторОбъектаXDTO/ObjectModule.bsl:317`.
