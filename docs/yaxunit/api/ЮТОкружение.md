---
title: API ЮТОкружение
description: "Публичные экспортные методы модуля ЮТОкружение из ядра YaXUnit."
tags: [yaxunit, api, ЮТОкружение]
publish_publicly: false
---

# API ЮТОкружение

Компактный справочник по 14 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТОкружение.ОписаниеОкружения

`Функция ЮТОкружение.ОписаниеОкружения() Экспорт`

Возвращает информацию об окружении

**Возвращает:**
- см. ЮТФабрика.НовоеОписаниеОкружения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:25`.

## ЮТОкружение.ВерсияПлатформы

`Функция ЮТОкружение.ВерсияПлатформы() Экспорт`

Версия платформы.

**Возвращает:**
- `Строка` — Версия платформы

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:42`.

## ЮТОкружение.ВерсияПлатформыБольшеИлиРавна

`Функция ЮТОкружение.ВерсияПлатформыБольшеИлиРавна(ВерсияДляПроверки) Экспорт`

Проверяет версию платформы (без учета режима совместимости)

**Параметры:**
- `ВерсияДляПроверки` — Строка — Версия, с которой необходимо сравнить версию платформы

**Возвращает:**
- `Булево` — Версия платформы больше или равна указанной

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:55`.

## ЮТОкружение.ВерсияПлатформыМеньше

`Функция ЮТОкружение.ВерсияПлатформыМеньше(ВерсияДляПроверки) Экспорт`

Проверяет версию платформы (без учета режима совместимости)

**Параметры:**
- `ВерсияДляПроверки` — Строка — Версия, с которой необходимо сравнить версию платформы

**Возвращает:**
- `Булево` — Версия платформы меньше указанной

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:68`.

## ЮТОкружение.ИспользуетсяАнглийскийВстроенныйЯзык

`Функция ЮТОкружение.ИспользуетсяАнглийскийВстроенныйЯзык() Экспорт`

Используется английский встроенный язык разработки.

**Возвращает:**
- `Булево` — Используется английский встроенный язык

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:78`.

## ЮТОкружение.ИспользуетсяРусскийВстроенныйЯзык

`Функция ЮТОкружение.ИспользуетсяРусскийВстроенныйЯзык() Экспорт`

Используется русский встроенный язык разработки.

**Возвращает:**
- `Булево` — Используется русский встроенный язык

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:88`.

## ЮТОкружение.ЭтоWindows

`Функция ЮТОкружение.ЭтоWindows() Экспорт`

Проверяет тип текущей операционной системы

**Возвращает:**
- Булево

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:98`.

## ЮТОкружение.ЭтоLinux

`Функция ЮТОкружение.ЭтоLinux() Экспорт`

Проверяет тип текущей операционной системы

**Возвращает:**
- Булево

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:109`.

## ЮТОкружение.ЭтоMac

`Функция ЮТОкружение.ЭтоMac() Экспорт`

Проверяет тип текущей операционной системы

**Возвращает:**
- Булево

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:120`.

## ЮТОкружение.ЛокальИнтерфейса

`Функция ЮТОкружение.ЛокальИнтерфейса() Экспорт`

Возвращает код языка интерфейса.

**Возвращает:**
- `Строка` — Локаль интерфейса

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:131`.

## ЮТОкружение.ЛокальПлатформы

`Функция ЮТОкружение.ЛокальПлатформы() Экспорт`

Возвращает код языка платформы.

**Возвращает:**
- `Строка` — Локаль платформы

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:145`.

## ЮТОкружение.ИспользуетсяАнглийскаяЛокальПлатформы

`Функция ЮТОкружение.ИспользуетсяАнглийскаяЛокальПлатформы() Экспорт`

Используется английская локаль платформы.

**Возвращает:**
- `Булево` — Используется английская локаль платформы

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:155`.

## ЮТОкружение.ИспользуетсяРусскаяЛокальПлатформы

`Функция ЮТОкружение.ИспользуетсяРусскаяЛокальПлатформы() Экспорт`

Используется русская локаль платформы.

**Возвращает:**
- `Булево` — Используется русская локаль платформы

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:165`.

## ЮТОкружение.ЭтоФайловаяБаза

`Функция ЮТОкружение.ЭтоФайловаяБаза() Экспорт`

Используется файловая база.

**Возвращает:**
- `Булево` — Это файловая база

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТОкружение/Module.bsl:175`.
