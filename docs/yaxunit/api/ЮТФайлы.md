---
title: API ЮТФайлы
description: "Публичные экспортные методы модуля ЮТФайлы из ядра YaXUnit."
tags: [yaxunit, api, ЮТФайлы]
publish_publicly: false
---

# API ЮТФайлы

Компактный справочник по 10 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТФайлы.Существует

`Функция ЮТФайлы.Существует(ПутьКФайлу, Обработчик = Неопределено) Экспорт`

Проверяет существование файла

**Параметры:**
- `ПутьКФайлу` — Строка
- `Обработчик` — ОписаниеОповещения — Обработчик асинхронного получения свойства файла. Если обработчик указан, но проверка выполняется асинхронно. - Неопределено - Проверка выполняется синхронно.

**Возвращает:**
- `Булево` — Существует

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:33`.

## ЮТФайлы.ЭтоКаталог

`Функция ЮТФайлы.ЭтоКаталог(ПутьКФайлу, Обработчик = Неопределено) Экспорт`

Проверяет, что по указанному пути находится каталог

**Параметры:**
- `ПутьКФайлу` — Строка
- `Обработчик` — ОписаниеОповещения — Обработчик асинхронного получения свойства файла. Если обработчик указан, но проверка выполняется асинхронно. - Неопределено - Проверка выполняется синхронно.

**Возвращает:**
- `Булево` — Это каталог

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:58`.

## ЮТФайлы.ОбъединитьПути

`Функция ЮТФайлы.ОбъединитьПути(Путь1, Путь2, Путь3 = Неопределено, Путь4 = Неопределено, Путь5 = Неопределено) Экспорт`

Возвращает путь к вложенному элементу

**Параметры:**
- `Путь1` — Строка — базовый путь к каталогу
- `Путь2` — Строка — относительный путь к вложенному элементу
- `Путь3` — Строка — относительный путь к вложенному элементу
- `Путь4` — Строка — относительный путь к вложенному элементу
- `Путь5` — Строка — относительный путь к вложенному элементу

**Возвращает:**
- `Строка` — Объединенный путь

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:86`.

## ЮТФайлы.УдалитьВременныеФайлы

`Процедура ЮТФайлы.УдалитьВременныеФайлы(Файлы) Экспорт`

Удаляет файлы

**Параметры:**
- `Файлы` — Массив из Файл - Массив из Строка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:113`.

## ЮТФайлы.СкопироватьКаталог

`Процедура ЮТФайлы.СкопироватьКаталог(КаталогИсточник, КаталогПриемник) Экспорт`

Выполняет рекурсивное копирование каталога

**Параметры:**
- `КаталогИсточник` — Строка — Каталог источник
- `КаталогПриемник` — Неопределено, Строка — Каталог приемник

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:156`.

## ЮТФайлы.СоздатьКаталогРекурсивно

`Процедура ЮТФайлы.СоздатьКаталогРекурсивно(Путь) Экспорт`

Экспортный метод модуля `ЮТФайлы`.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:176`.

## ЮТФайлы.СоздатьРодительскийКаталог

`Процедура ЮТФайлы.СоздатьРодительскийКаталог(Путь) Экспорт`

Экспортный метод модуля `ЮТФайлы`.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:190`.

## ЮТФайлы.ДанныеТекстовогоФайла

`Функция ЮТФайлы.ДанныеТекстовогоФайла(ИмяФайла) Экспорт`

Возвращает содержимое текстового файла

**Параметры:**
- `ИмяФайла` — Строка — Имя файла

**Возвращает:**
- Строка, Неопределено - Данные текстового файла

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:209`.

## ЮТФайлы.ЗаписатьТекстВФайла

`Процедура ЮТФайлы.ЗаписатьТекстВФайла(ИмяФайла, Данные) Экспорт`

Сохраняет данные в текстовый файла

**Параметры:**
- `ИмяФайла` — Строка — Имя файла
- `Данные` — Строка — Данные

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:229`.

## ЮТФайлы.ПолучитьАтрибутФайла

`Процедура ЮТФайлы.ПолучитьАтрибутФайла(Результат, Параметры) Экспорт`

Экспортный метод модуля `ЮТФайлы`.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТФайлы/Module.bsl:256`.
