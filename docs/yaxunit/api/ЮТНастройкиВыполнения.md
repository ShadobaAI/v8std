---
title: API ЮТНастройкиВыполнения
description: "Публичные экспортные методы модуля ЮТНастройкиВыполнения из ядра YaXUnit."
tags: [yaxunit, api, ЮТНастройкиВыполнения]
publish_publicly: false
---

# API ЮТНастройкиВыполнения

Компактный справочник по 7 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТНастройкиВыполнения.ВТранзакции

`Функция ЮТНастройкиВыполнения.ВТранзакции() Экспорт`

Возвращает значение настройки "ВТранзакции" для текущего исполняемого объекта (тест, набор, модуль)

**Возвращает:**
- Булево

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТНастройкиВыполнения/Module.bsl:27`.

## ЮТНастройкиВыполнения.УдалениеТестовыхДанных

`Функция ЮТНастройкиВыполнения.УдалениеТестовыхДанных() Экспорт`

Возвращает значение настройки "УдалениеТестовыхДанных" для текущего исполняемого объекта (тест, набор, модуль)

**Возвращает:**
- Булево

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТНастройкиВыполнения/Module.bsl:39`.

## ЮТНастройкиВыполнения.Перед

`Функция ЮТНастройкиВыполнения.Перед() Экспорт`

Возвращает значение настройки "Перед" для текущего исполняемого объекта (тест, набор, модуль)
Возвращает имя назначенного обработчика события (имя метода) "Перед".

**Возвращает:**
- Строка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТНастройкиВыполнения/Module.bsl:52`.

## ЮТНастройкиВыполнения.После

`Функция ЮТНастройкиВыполнения.После() Экспорт`

Возвращает значение настройки "После" для текущего исполняемого объекта (тест, набор, модуль)
Возвращает имя назначенного обработчика события (имя метода) "После".

**Возвращает:**
- Строка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТНастройкиВыполнения/Module.bsl:65`.

## ЮТНастройкиВыполнения.ЗначениеНастройкиТеста

`Функция ЮТНастройкиВыполнения.ЗначениеНастройкиТеста(ИмяНастройки, ЗначениеПоУмолчанию, СтрогийУровеньИсполнения = Ложь) Экспорт`

Возвращает значение произвольной настройки для текущего исполняемого объекта (тест, набор, модуль)

**Параметры:**
- `ИмяНастройки` — Строка — Имя настройки, см. ЮТФабрика.ПараметрыИсполненияТеста
- `ЗначениеПоУмолчанию` — Произвольный — Значение по умолчанию
- `СтрогийУровеньИсполнения` — Булево — Признак, стоит ли проверять наличие настройки у родительских элементов.
- `Ложь` — По умолчанию, будет выполнен поиск и получение значения для родителей (набор, модуль), если значения для текущего элемента не установлено.
- `Истина` — Получение настройки только для текущего элемента.

**Возвращает:**
- Произвольный, Неопределено, Булево, Строка - Значение настройки теста

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТНастройкиВыполнения/Module.bsl:85`.

## ЮТНастройкиВыполнения.ГлобальныеНастройкиВыполнения

`Функция ЮТНастройкиВыполнения.ГлобальныеНастройкиВыполнения() Экспорт`

Возвращает глобальные настройки выполнения, указанные в файле настроек запуска, объект`settings`

**Возвращает:**
- Структура, Неопределено - Глобальные настройки

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТНастройкиВыполнения/Module.bsl:132`.

## ЮТНастройкиВыполнения.КаталогПроекта

`Функция ЮТНастройкиВыполнения.КаталогПроекта() Экспорт`

Возвращает путь к каталогу проекта, указанному в файле настроек - `projectPath`

**Возвращает:**
- `Строка` — Каталог проекта

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТНастройкиВыполнения/Module.bsl:143`.
