---
title: API ЮТПредикаты
description: "Публичные экспортные методы модуля ЮТПредикаты из ядра YaXUnit."
tags: [yaxunit, api, ЮТПредикаты]
publish_publicly: false
---

# API ЮТПредикаты

Компактный справочник по 30 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТПредикаты.Реквизит

`Функция ЮТПредикаты.Реквизит(ИмяРеквизита) Экспорт`

Устанавливает имя реквизита, все последующие проверки будут относится к нему.

**Параметры:**
- `ИмяРеквизита` — Строка — Имя реквизита - Число - Индекс коллекции - Произвольный - Ключ соответствия

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:44`.

## ЮТПредикаты.Свойство

`Функция ЮТПредикаты.Свойство(ИмяСвойства) Экспорт`

Устанавливает имя свойства, все последующие проверки будут относится к нему.
Это псевдоним (алиас) для см. Реквизит

**Параметры:**
- `ИмяСвойства` — Строка — Имя реквизита - Число - Индекс коллекции - Произвольный - Ключ соответствия

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:61`.

## ЮТПредикаты.Равно

`Функция ЮТПредикаты.Равно(Значение) Экспорт`

Добавляет предикат, проверяющий равенство объекта (свойства) указанному значению

**Параметры:**
- `Значение` — Произвольный — Значение для сравнения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:84`.

## ЮТПредикаты.НеРавно

`Функция ЮТПредикаты.НеРавно(Значение) Экспорт`

Добавляет предикат, проверяющий не равенство объекта (свойства) указанному значению

**Параметры:**
- `Значение` — Произвольный — Значение для сравнения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:105`.

## ЮТПредикаты.Заполнено

`Функция ЮТПредикаты.Заполнено() Экспорт`

Добавляет предикат, проверяющий заполненность объекта (свойства)

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:123`.

## ЮТПредикаты.НеЗаполнено

`Функция ЮТПредикаты.НеЗаполнено() Экспорт`

Добавляет предикат, проверяющий, что объект (свойств) не заполнено

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:141`.

## ЮТПредикаты.Пусто

`Функция ЮТПредикаты.Пусто() Экспорт`

Добавляет предикат, проверяющий, что объект (свойств) не заполнено

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:159`.

## ЮТПредикаты.Больше

`Функция ЮТПредикаты.Больше(Значение) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (свойства) больше указанного

**Параметры:**
- `Значение` — Произвольный — Значение для сравнения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:179`.

## ЮТПредикаты.БольшеИлиРавно

`Функция ЮТПредикаты.БольшеИлиРавно(Значение) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (свойства) больше или равно указанному

**Параметры:**
- `Значение` — Произвольный — Значение для сравнения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:200`.

## ЮТПредикаты.Меньше

`Функция ЮТПредикаты.Меньше(Значение) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (свойства) меньше указанного

**Параметры:**
- `Значение` — Произвольный — Значение для сравнения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:221`.

## ЮТПредикаты.МеньшеИлиРавно

`Функция ЮТПредикаты.МеньшеИлиРавно(Значение) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (свойства) меньше или равно указанному

**Параметры:**
- `Значение` — Произвольный — Значение для сравнения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:242`.

## ЮТПредикаты.ИмеетТип

`Функция ЮТПредикаты.ИмеетТип(Тип) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (свойства) имеет указанный тип

**Параметры:**
- `Тип` — Тип — Тип для проверки - ОписаниеТипов - Описание типов для проверки - Строка - Имя типа для проверки

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:268`.

## ЮТПредикаты.ИмеетТипОтличныйОт

`Функция ЮТПредикаты.ИмеетТипОтличныйОт(Тип) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (свойства) имеет тип отличный от указанного

**Параметры:**
- `Тип` — Тип — Тип для проверки - ОписаниеТипов - Описание типов для проверки - Строка - Имя типа для проверки

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:291`.

## ЮТПредикаты.ИмеетДлину

`Функция ЮТПредикаты.ИмеетДлину(Значение) Экспорт`

Добавляет предикат, проверяющий, длину/размер значение объекта (свойства) на равенство указанному значению

**Параметры:**
- `Значение` — Число — Ожидаемая длина/размер

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:315`.

## ЮТПредикаты.ИмеетДлинуОтличнуюОт

`Функция ЮТПредикаты.ИмеетДлинуОтличнуюОт(Значение) Экспорт`

Добавляет предикат, проверяющий, длину/размер значение объекта (свойства) на не равенство указанному значению

**Параметры:**
- `Значение` — Число — Ожидаемая длина/размер

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:336`.

## ЮТПредикаты.ИмеетСвойство

`Функция ЮТПредикаты.ИмеетСвойство(ИмяСвойства) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (реквизита) содержит вложенное свойство

**Параметры:**
- `ИмяСвойства` — Строка — Имя свойства для проверки - Число - Индекс коллекции для проверки - Произвольный - Ключ соответствия для проверки

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:362`.

## ЮТПредикаты.НеИмеетСвойства

`Функция ЮТПредикаты.НеИмеетСвойства(ИмяСвойства) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (реквизита) не содержит вложенное свойство

**Параметры:**
- `ИмяСвойства` — Строка — Имя свойства для проверки - Число - Индекс коллекции для проверки - Произвольный - Ключ соответствия для проверки

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:385`.

## ЮТПредикаты.Содержит

`Функция ЮТПредикаты.Содержит(Значение) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (реквизита) содержит указанное значение

**Параметры:**
- `Значение` — Произвольный — Значение для поиска

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:406`.

## ЮТПредикаты.НеСодержит

`Функция ЮТПредикаты.НеСодержит(Значение) Экспорт`

Добавляет предикат, проверяющий, что значение объекта (реквизита) не содержит указанное значение

**Параметры:**
- `Значение` — Произвольный — Значение для поиска

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:427`.

## ЮТПредикаты.СодержитСтрокуПоШаблону

`Функция ЮТПредикаты.СодержитСтрокуПоШаблону(Значение) Экспорт`

Добавляет предикат, проверяющий, что строка соответствует указанному регулярному выражению

**Параметры:**
- `Значение` — Строка — Регулярное выражение для проверки

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:448`.

## ЮТПредикаты.НеСодержитСтрокуПоШаблону

`Функция ЮТПредикаты.НеСодержитСтрокуПоШаблону(Значение) Экспорт`

Добавляет предикат, проверяющий, что строка не соответствует указанному регулярному выражению

**Параметры:**
- `Значение` — Строка — Регулярное выражение для проверки

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:469`.

## ЮТПредикаты.ВСписке

`Функция ЮТПредикаты.ВСписке(Значения) Экспорт`

Добавляет условие, что проверяемое значение (или значение его свойства) входит в список значений

**Параметры:**
- `Значения` — Массив из Произвольный — Значения для проверки - СписокЗначений из Произвольный - Значения для проверки

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:494`.

## ЮТПредикаты.Между

`Функция ЮТПредикаты.Между(НачалоИнтервала, ОкончаниеИнтервала) Экспорт`

Добавляет условие, что проверяемое значение (или значение его свойства) входит в заданный интервал.

Проверяемое значение может находится на границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:515`.

## ЮТПредикаты.МеждуВключаяГраницы

`Функция ЮТПредикаты.МеждуВключаяГраницы(НачалоИнтервала, ОкончаниеИнтервала) Экспорт`

Добавляет условие, что проверяемое значение (или значение его свойства) входит в заданный интервал.

Проверяемое значение может находится на границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:531`.

## ЮТПредикаты.МеждуИсключаяГраницы

`Функция ЮТПредикаты.МеждуИсключаяГраницы(НачалоИнтервала, ОкончаниеИнтервала) Экспорт`

Добавляет условие, что проверяемое значение (или значение его свойства) входит в заданный интервал.

Проверяемое значение не может находится на границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:549`.

## ЮТПредикаты.МеждуВключаяНачалоГраницы

`Функция ЮТПредикаты.МеждуВключаяНачалоГраницы(НачалоИнтервала, ОкончаниеИнтервала) Экспорт`

Добавляет условие, что проверяемое значение (или значение его свойства) входит в заданный интервал.

Проверяемое значение может находится на начальной границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:567`.

## ЮТПредикаты.МеждуВключаяОкончаниеГраницы

`Функция ЮТПредикаты.МеждуВключаяОкончаниеГраницы(НачалоИнтервала, ОкончаниеИнтервала) Экспорт`

Добавляет условие, что проверяемое значение (или значение его свойства) входит в заданный интервал.

Проверяемое значение может находится на конечной границе интервала.

**Параметры:**
- `НачалоИнтервала` — Произвольный — Левая граница интервала. Может иметь любой тип, позволяющий сравнивать значения
- `ОкончаниеИнтервала` — Произвольный — Правая граница. Может иметь любой тип, позволяющий сравнивать значения

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:585`.

## ЮТПредикаты.Получить

`Функция ЮТПредикаты.Получить() Экспорт`

Возвращает набор сформированных утверждений.

Рекомендуется использовать этот метод, если планируется отложенная проверка предикатов. Например, вы хотите сформировать два набору предикатов
и проверять их в зависимости от условия.

Метод копирует настроенный набор утверждений в массив и возвращает его, таким образом сохраняется состояние, которое можно передавать дальше.

**Возвращает:**
- Массив из см. ЮТФабрика.ВыражениеПредиката - Набор предикатов

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:602`.

## ЮТПредикаты.Выражения

`Функция ЮТПредикаты.Выражения() Экспорт`

Возвращает структуру-перечисление с типами выражений предикатов

**Возвращает:**
- `ФиксированнаяСтруктура` — Выражения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:612`.

## ЮТПредикаты.Инициализировать

`Функция ЮТПредикаты.Инициализировать(Условия = Неопределено) Экспорт`

Инициализирует конструктор предиката

**Параметры:**
- `Условия` — Структура, Соответствие из Произвольный — Набор условий, которыми инициализируется предикат
- `Ключ` — Строка — Имя реквизита
- `Значение` — Произвольный — Значение, которому должен быть равен реквизит

**Возвращает:**
- `ОбщийМодуль` — см. ЮТПредикаты.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТПредикаты/Module.bsl:656`.
