---
title: API ЮТHTTPСоединение
description: "Публичные экспортные методы модуля ЮТHTTPСоединение из ядра YaXUnit."
tags: [yaxunit, api, ЮТHTTPСоединение]
publish_publicly: false
---

# API ЮТHTTPСоединение

Компактный справочник по 25 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТHTTPСоединение.CallHTTPMethod

`Функция ЮТHTTPСоединение.CallHTTPMethod(HTTPМетод, HTTPЗапрос, ИмяВыходногоФайла = "") Экспорт`

Отправляет данные на указанный адрес для обработки с использованием указанного HTTP-метода (ENG).

**Параметры:**
- `HTTPМетод` — Строка — Строка, содержащая имя HTTP-метода для запроса (например, "GET", "POST").
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса, содержащий адрес, заголовки и тело запроса.
- `ИмяВыходногоФайла` — Строка — (необязательный) Имя файла, в который будет записано тело ответа. - Неопределено - Тело ответа не будет записано в файл.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:84`.

## ЮТHTTPСоединение.Put

`Функция ЮТHTTPСоединение.Put(HTTPЗапрос) Экспорт`

Отправляет данных на HTTP-сервер с помощью PUT-запроса (ENG).

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:108`.

## ЮТHTTPСоединение.Patch

`Функция ЮТHTTPСоединение.Patch(HTTPЗапрос) Экспорт`

Изменяет данные на сервере при помощи PATCH-запроса (ENG).

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:132`.

## ЮТHTTPСоединение.Post

`Функция ЮТHTTPСоединение.Post(HTTPЗапрос, ИмяВыходногоФайла = "") Экспорт`

Отправляет данных на HTTP-сервер с помощью POST-запроса (ENG).

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.
- `ИмяВыходногоФайла` — Строка — (необязательный) Имя файла, в который будет записано тело ответа. - Неопределено - Тело ответа не будет записано в файл.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:158`.

## ЮТHTTPСоединение.Get

`Функция ЮТHTTPСоединение.Get(HTTPЗапрос, ИмяВыходногоФайла = "") Экспорт`

Получает данные с HTTP-сервера, используя GET-запрос (ENG).

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.
- `ИмяВыходногоФайла` — Строка — (необязательный) Имя файла, в который будет записано тело ответа. - Неопределено - Тело ответа не будет записано в файл.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:186`.

## ЮТHTTPСоединение.Head

`Функция ЮТHTTPСоединение.Head(HTTPЗапрос) Экспорт`

Получает при помощи HEAD-запроса информацию о запрашиваемых данных, содержащуюся в заголовках, не получая сами данные (ENG).

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:214`.

## ЮТHTTPСоединение.Delete

`Функция ЮТHTTPСоединение.Delete(HTTPЗапрос) Экспорт`

Удаляет ресурс на сервере при помощи HTTP-запроса DELETE (ENG).

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:237`.

## ЮТHTTPСоединение.ВызватьHTTPМетод

`Функция ЮТHTTPСоединение.ВызватьHTTPМетод(HTTPМетод, HTTPЗапрос, ИмяВыходногоФайла = "") Экспорт`

Отправляет данные на указанный адрес для обработки с использованием указанного HTTP-метода.

**Параметры:**
- `HTTPМетод` — Строка — Строка, содержащая имя HTTP-метода для запроса (например, "GET", "POST").
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса, содержащий адрес, заголовки и тело запроса.
- `ИмяВыходногоФайла` — Строка — (необязательный) Имя файла, в который будет записано тело ответа. - Неопределено - Тело ответа не будет записано в файл.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:263`.

## ЮТHTTPСоединение.Записать

`Функция ЮТHTTPСоединение.Записать(HTTPЗапрос) Экспорт`

Отправляет данных на HTTP-сервер с помощью PUT-запроса.

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:289`.

## ЮТHTTPСоединение.Изменить

`Функция ЮТHTTPСоединение.Изменить(HTTPЗапрос) Экспорт`

Изменяет данные на сервере при помощи PATCH-запроса.

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:306`.

## ЮТHTTPСоединение.ОтправитьДляОбработки

`Функция ЮТHTTPСоединение.ОтправитьДляОбработки(HTTPЗапрос, ИмяВыходногоФайла = "") Экспорт`

Отправляет данных на HTTP-сервер с помощью POST-запроса.

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.
- `ИмяВыходногоФайла` — Строка — (необязательный) Имя файла, в который будет записано тело ответа. - Неопределено - Тело ответа не будет записано в файл.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:325`.

## ЮТHTTPСоединение.Получить

`Функция ЮТHTTPСоединение.Получить(HTTPЗапрос, ИмяВыходногоФайла = "") Экспорт`

Получает данные с HTTP-сервера, используя GET-запрос.

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.
- `ИмяВыходногоФайла` — Строка — (необязательный) Имя файла, в который будет записано тело ответа. - Неопределено - Тело ответа не будет записано в файл.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:344`.

## ЮТHTTPСоединение.ПолучитьЗаголовки

`Функция ЮТHTTPСоединение.ПолучитьЗаголовки(HTTPЗапрос) Экспорт`

Получает при помощи HEAD-запроса информацию о запрашиваемых данных, содержащуюся в заголовках, не получая сами данные.

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:361`.

## ЮТHTTPСоединение.Удалить

`Функция ЮТHTTPСоединение.Удалить(HTTPЗапрос) Экспорт`

Удаляет ресурс на сервере при помощи HTTP-запроса DELETE.

**Параметры:**
- `HTTPЗапрос` — HTTPЗапрос — Системный объект HTTP-запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок-объект ответа HTTP-сервера на запрос.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:378`.

## ЮТHTTPСоединение.ВыброситьИсключение

`Функция ЮТHTTPСоединение.ВыброситьИсключение(ТекстИсключения) Экспорт`

Указывает, что при выполнении HTTP-запроса необходимо выбросить исключение.

**Параметры:**
- `ТекстИсключения` — Строка — Текст выбрасываемого исключения.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:407`.

## ЮТHTTPСоединение.УстановитьHTTPОтвет

`Функция ЮТHTTPСоединение.УстановитьHTTPОтвет(HTTPОтвет) Экспорт`

Устанавливает возвращаемый HTTP-ответ.

**Параметры:**
- `HTTPОтвет` — ОбработкаОбъект.ЮТHTTPОтвет — Мок-объект HTTP-ответа, который будет возвращен при вызове методов соединения.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:427`.

## ЮТHTTPСоединение.УстановитьЗащищенноеСоединение

`Функция ЮТHTTPСоединение.УстановитьЗащищенноеСоединение(Значение) Экспорт`

Устанавливает защищенное соединение.

**Параметры:**
- `Значение` — ЗащищенноеСоединениеOpenSSL — Объект защищенного соединения для осуществления HTTPS соединения.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:447`.

## ЮТHTTPСоединение.УстановитьАутентификациюОС

`Функция ЮТHTTPСоединение.УстановитьАутентификациюОС(Значение) Экспорт`

Установить использование аутентификации ОС.

**Параметры:**
- `Значение` — Булево — Включает использование аутентификации NTLM или Negotiate на сервере.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:467`.

## ЮТHTTPСоединение.УстановитьПароль

`Функция ЮТHTTPСоединение.УстановитьПароль(Значение) Экспорт`

Устанавливает пароль для аутентификации.

**Параметры:**
- `Значение` — Строка — Пароль пользователя на указанном сервере.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:488`.

## ЮТHTTPСоединение.УстановитьПользователя

`Функция ЮТHTTPСоединение.УстановитьПользователя(Значение) Экспорт`

Устанавливает пользователя для аутентификации.

**Параметры:**
- `Значение` — Строка — Имя пользователя на указанном сервере.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:508`.

## ЮТHTTPСоединение.УстановитьПорт

`Функция ЮТHTTPСоединение.УстановитьПорт(Значение) Экспорт`

Установить порт соединения.

**Параметры:**
- `Значение` — Число — Порт сервера, с которым осуществляется соединение.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:529`.

## ЮТHTTPСоединение.УстановитьПрокси

`Функция ЮТHTTPСоединение.УстановитьПрокси(Значение) Экспорт`

Устанавливает прокси соединения.

**Параметры:**
- `Значение` — ИнтернетПрокси — Прокси, используемый для соединения с сервером.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:551`.

## ЮТHTTPСоединение.УстановитьСервер

`Функция ЮТHTTPСоединение.УстановитьСервер(Значение) Экспорт`

Устанавливает адрес сервера.

**Параметры:**
- `Значение` — Строка — Хост сервера, с которым осуществляется соединение (например, "localhost", "api.example.com").

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:571`.

## ЮТHTTPСоединение.УстановитьТаймаут

`Функция ЮТHTTPСоединение.УстановитьТаймаут(Значение) Экспорт`

Устанавливает таймаут соединения.

**Параметры:**
- `Значение` — Число — Таймаут осуществляемого соединения и операций, в секундах.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:591`.

## ЮТHTTPСоединение.КонструкторПоУмолчанию

`Функция ЮТHTTPСоединение.КонструкторПоУмолчанию(_Сервер, _Порт = 0, _Пользователь = "", _Пароль = "", _Прокси = Неопределено, _Таймаут = 0, _ЗащищенноеСоединение = Неопределено, _ИспользоватьАутентификациюОС = Ложь) Экспорт`

Конструктор по умолчанию.

**Параметры:**
- `_Сервер` — Строка — Хост сервера, с которым осуществляется соединение.
- `_Порт` — Число — (необязательный) Порт сервера, с которым осуществляется соединение (по умолчанию 80 или 443 для HTTPS).
- `_Пользователь` — Строка — (необязательный) Имя пользователя на указанном сервере.
- `_Пароль` — Строка — (необязательный) Пароль пользователя на указанном сервере.
- `_Прокси` — ИнтернетПрокси — (необязательный) Прокси, используемый для соединения с сервером. - Неопределено - Прокси не используется или используется системный.
- `_Таймаут` — Число — (необязательный) Таймаут осуществляемого соединения и операций, в секундах (по умолчанию 0 — бесконечно).
- `_ЗащищенноеСоединение` — ЗащищенноеСоединениеOpenSSL — (необязательный) Объект защищенного соединения для осуществления HTTPS соединения. - Неопределено - Используется незащищенное соединение или системные настройки SSL.
- `_ИспользоватьАутентификациюОС` — Булево — (необязательный) Включает использование аутентификации NTLM или Negotiate на сервере (по умолчанию Ложь).

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСоединение/ObjectModule.bsl:624`.
