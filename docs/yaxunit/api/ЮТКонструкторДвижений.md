---
title: API ЮТКонструкторДвижений
description: "Публичные экспортные методы модуля ЮТКонструкторДвижений из ядра YaXUnit."
tags: [yaxunit, api, ЮТКонструкторДвижений]
publish_publicly: false
---

# API ЮТКонструкторДвижений

Компактный справочник по 13 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКонструкторДвижений.ДобавитьСтроку

`Функция ЮТКонструкторДвижений.ДобавитьСтроку(ЗначенияРеквизитов = Неопределено) Экспорт`

Добавляет новую запись в набор движений.
При необходимости можно выполнить установку реквизитов новой записи.

**Параметры:**
- `ЗначенияРеквизитов` — Структура — Устанавливаемые значения реквизитов новой записи (по умолчанию Неопределено)

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:46`.

## ЮТКонструкторДвижений.Установить

`Функция ЮТКонструкторДвижений.Установить(ИмяРеквизита, Значение) Экспорт`

Устанавливает значение реквизита текущей записи набора

**Параметры:**
- `ИмяРеквизита` — Строка — Имя реквизита записи
- `Значение` — Произвольный — Значение реквизита записи

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:69`.

## ЮТКонструкторДвижений.УстановитьРеквизиты

`Функция ЮТКонструкторДвижений.УстановитьРеквизиты(ЗначенияРеквизитов) Экспорт`

Устанавливает значения реквизитов текущей записи набора.

**Параметры:**
- `ЗначенияРеквизитов` — Структура — Устанавливаемые значения реквизитов. Ключ структуры — имя реквизита, Значение — значение реквизита.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:91`.

## ЮТКонструкторДвижений.Фикция

`Функция ЮТКонструкторДвижений.Фикция(ИмяРеквизита, РеквизитыЗаполнения = Неопределено, ОграничениеТипа = Неопределено) Экспорт`

Генерирует фиктивное значение для указанного реквизита. Для ссылочных типов создает новый объект, для примитивных - случайное значение.

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение.

* Для примитивных значение генерируется случайное значение.
* Для ссылочных типов создается новый объект.
* Для перечислений (в том числе системных) выбирается случайно значение перечисления.

**Параметры:**
- `ИмяРеквизита` — Строка — Имя реквизита записи.
- `РеквизитыЗаполнения` — Структура — Значения реквизитов заполнения создаваемого объекта базы (если реквизит ссылочного типа). - Неопределено - Новый ссылочный объект будет создан с пустыми или фиктивными реквизитами по умолчанию.
- `ОграничениеТипа` — Тип — Используется для ограничения типа составного реквизита, будет сгенерировано значение указанного типа. - ОписаниеТипов - Используется для ограничения типа составного реквизита. - Неопределено - Будет сгенерировано значение на основании типа реквизита, если реквизит составной, то будет использован случайный тип.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:124`.

## ЮТКонструкторДвижений.ФикцияРеквизитов

`Функция ЮТКонструкторДвижений.ФикцияРеквизитов(ИменаРеквизитов) Экспорт`

Генерирует фиктивные значения для нескольких реквизитов

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение.

* Для примитивных значение генерируется случайное значение
* Для ссылочных типов создается новый объект
* Для перечислений (в том числе системных) выбирается случайно значение перечисления

**Параметры:**
- `ИменаРеквизитов` — Строка — Имена реквизитов записи разделенные запятыми - Массив из Строка - Имена реквизитов записи

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:152`.

## ЮТКонструкторДвижений.ФикцияНезаполненных

`Функция ЮТКонструкторДвижений.ФикцияНезаполненных(ИменаРеквизитов) Экспорт`

Генерирует фиктивные значения только для незаполненных реквизитов

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение.

* Для примитивных значение генерируется случайное значение
* Для ссылочных типов создается новый объект
* Для перечислений (в том числе системных) выбирается случайно значение перечисления

**Параметры:**
- `ИменаРеквизитов` — Строка — Имена реквизитов записи разделенные запятыми - Массив из Строка - Имена реквизитов записи

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:182`.

## ЮТКонструкторДвижений.ФикцияНезаполненныхИсключая

`Функция ЮТКонструкторДвижений.ФикцияНезаполненныхИсключая(ИменаИсключений) Экспорт`

Генерирует фиктивные значения для незаполненных реквизитов, исключая указанные

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение.

* Для примитивных значение генерируется случайное значение
* Для ссылочных типов создается новый объект
* Для перечислений (в том числе системных) выбирается случайно значение перечисления

**Параметры:**
- `ИменаИсключений` — Строка — Имена исключаемых реквизитов записи разделенные запятыми - Массив из Строка - Имена исключаемых реквизитов записи

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:212`.

## ЮТКонструкторДвижений.ФикцияОбязательныхПолей

`Функция ЮТКонструкторДвижений.ФикцияОбязательныхПолей() Экспорт`

Автоматически заполняет все обязательные поля регистра фиктивными значениями

На основании метаданных определяются обязательные поля (Проверка заполнения = Выдавать ошибку),
для них генерируются и устанавливаются фиктивные значение.

* Для примитивных значение генерируется случайное значение
* Для ссылочных типов создается новый объект
* Для перечислений (в том числе системных) выбирается случайно значение перечисления

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:235`.

## ЮТКонструкторДвижений.УстановитьДополнительноеСвойство

`Функция ЮТКонструкторДвижений.УстановитьДополнительноеСвойство(ИмяСвойства, Значение = Неопределено) Экспорт`

Устанавливает дополнительное свойство набора записей

**Параметры:**
- `ИмяСвойства` — Строка — Название свойства
- `Значение` — Произвольный — Значение свойства (по умолчанию Неопределено)

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторДвижений - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:256`.

## ЮТКонструкторДвижений.ДанныеСтроки

`Функция ЮТКонструкторДвижений.ДанныеСтроки() Экспорт`

Возвращает данные текущей записи движений

**Возвращает:**
- `Структура` — Значения реквизитов текущей строки

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:279`.

## ЮТКонструкторДвижений.Данные

`Функция ЮТКонструкторДвижений.Данные() Экспорт`

Возвращает все записи движений

**Возвращает:**
- Массив из Структура - Все записи движений

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:297`.

## ЮТКонструкторДвижений.Записать

`Процедура ЮТКонструкторДвижений.Записать(ОбменДаннымиЗагрузка = Неопределено) Экспорт`

Сохраняет сформированные движения документа.

**Параметры:**
- `ОбменДаннымиЗагрузка` — Булево — Если Истина, движения будут записаны с признаком `ОбменДанными.Загрузка = Истина`. - Неопределено - Используется значение по умолчанию (обычно Ложь или из настроек).

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:319`.

## ЮТКонструкторДвижений.Инициализировать

`Процедура ЮТКонструкторДвижений.Инициализировать(Документ, ИмяРегистра) Экспорт`

Инициализирует конструктор для работы с движениями определенного документа и регистра.
Этот метод должен быть вызван после создания экземпляра обработки и перед использованием других методов конструктора.

**Параметры:**
- `Документ` — ДокументОбъект, ДокументСсылка — Документ-регистратор, для которого будут формироваться движения.
- `ИмяРегистра` — Строка — Полное имя регистра, по которому будут формироваться движения (например, "РегистрНакопления.ТоварыНаСкладах").

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторДвижений/ObjectModule.bsl:342`.
