---
title: API ЮТСтроки
description: "Публичные экспортные методы модуля ЮТСтроки из ядра YaXUnit."
tags: [yaxunit, api, ЮТСтроки]
publish_publicly: false
---

# API ЮТСтроки

Компактный справочник по 15 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТСтроки.ДобавитьСтроку

`Функция ЮТСтроки.ДобавитьСтроку(ИсходнаяСтрока, ДополнительнаяСтрока, Разделитель = ";") Экспорт`

Объединяет две строки с помощью указанного разделителя.

Ограничения:
- Метод не изменяет исходную и дополнительную строки, а создает новую строку на их основе.

**Параметры:**
- `ИсходнаяСтрока` — Строка — Строка, к которой будет добавлена дополнительная строка.
- `ДополнительнаяСтрока` — Строка — Строка, которую нужно добавить к исходной строке.
- `Разделитель` — Строка — Символ или набор символов, используемых для разделения исходной и дополнительной строк. По умолчанию используется точка с запятой (";").

**Возвращает:**
- `Строка` — Новая строка, образованная объединением исходной и дополнительной строк с помощью указанного разделителя. Если дополнительная строка пустая, то возвращается исходная строка. Если исходная строка пустая, а дополнительная нет, то возвращается дополнительная строка.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:42`.

## ЮТСтроки.РазделитьСтроку

`Функция ЮТСтроки.РазделитьСтроку(Знач Значение, Разделитель = ";") Экспорт`

Разделяет строку на подстроки с помощью указанного разделителя.

Ограничения:
- Метод не изменяет исходную строку, а создает новый массив строк на ее основе.
- В отличие от СтрРазделить, в качестве разделителя используется вся переданная строка, а не любой из символов, входящих в нее.
- Отсекает незначащие символы, стоящие слева от первого значащего символа в строке,
и пробелы, стоящие справа от последнего значащего символа в строке.

**Параметры:**
- `Значение` — Строка — Строка, которую нужно разделить на подстроки.
- `Разделитель` — Строка — Символ или набор символов, используемых для разделения строки на подстроки. По умолчанию используется точка с запятой (";").

**Возвращает:**
- Массив из Строка - Массив строк, содержащий подстроки, полученные в результате разделения исходной строки. Если исходная строка пустая, то возвращается пустой массив.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:78`.

## ЮТСтроки.СтрокаСимволов

`Функция ЮТСтроки.СтрокаСимволов(Символ, Количество) Экспорт`

Создает строку, состоящую из указанного количества одинаковых символов.

**Параметры:**
- `Символ` — Строка — Символ, из которого будет создана строка.
- `Количество` — Число — Количество символов в создаваемой строке.

**Возвращает:**
- `Строка` — Новая строка, состоящая из указанного количества одинаковых символов.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:107`.

## ЮТСтроки.ДополнитьСправа

`Функция ЮТСтроки.ДополнитьСправа(ИсходнаяСтрока, ОжидаемаяДлина, Символ = " ") Экспорт`

Дополняет строку справа указанным символом до указанной длины.

Ограничения:
- Метод не изменяет исходную строку, а создает новую строку на ее основе.
- Если исходная строка уже длиннее или равна ожидаемой длине, то дополнение не выполняется.

**Параметры:**
- `ИсходнаяСтрока` — Строка — Строка, которую нужно дополнить.
- `ОжидаемаяДлина` — Число — Желаемая длина результирующей строки.
- `Символ` — Строка — Символ, которым будет дополнена строка. По умолчанию используется пробел (" ").

**Возвращает:**
- `Строка` — Новая строка, дополненная справа указанным символом до указанной длины. Если исходная строка уже длиннее или равна ожидаемой длине, то возвращается исходная строка без изменений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:134`.

## ЮТСтроки.РусскиеБуквы

`Функция ЮТСтроки.РусскиеБуквы(НижнийРегистр = Истина, ВерхнийРегистр = Ложь) Экспорт`

Возвращает строку, содержащую все русские буквы в указанном регистре.

**Параметры:**
- `НижнийРегистр` — Булево — Если значение равно `Истина`, то в создаваемой строке будут использованы буквы в нижнем регистре. По умолчанию равно `Истина`.
- `ВерхнийРегистр` — Булево — Если значение равно `Истина`, то в создаваемой строке будут использованы буквы в верхнем регистре. По умолчанию равно `Ложь`.

**Возвращает:**
- `Строка` — Новая строка, содержащая все русские буквы в указанном регистре. Если оба параметра НижнийРегистр и ВерхнийРегистр равны ``Истина`, то в создаваемой строке будут использованы буквы в обоих регистрах.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:166`.

## ЮТСтроки.АнглийскиеБуквы

`Функция ЮТСтроки.АнглийскиеБуквы(НижнийРегистр = Истина, ВерхнийРегистр = Ложь) Экспорт`

Возвращает строку, содержащую все английские буквы в указанном регистре.

**Параметры:**
- `НижнийРегистр` — Булево — Если значение равно `Истина`, то в создаваемой строке будут использованы буквы в нижнем регистре. По умолчанию равно `Истина`.
- `ВерхнийРегистр` — Булево — Если значение равно `Истина`, то в создаваемой строке будут использованы буквы в верхнем регистре. По умолчанию равно `Ложь`.

**Возвращает:**
- `Строка` — Новая строка, содержащая все английские буквы в указанном регистре. Если оба параметра НижнийРегистр и ВерхнийРегистр равны `Истина`, то в создаваемой строке будут использованы буквы в обоих регистрах.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:188`.

## ЮТСтроки.Цифры

`Функция ЮТСтроки.Цифры() Экспорт`

Возвращает строку, содержащую все цифры.

**Возвращает:**
- `Строка` — Новая строка, содержащая все цифры "1234567890".

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:201`.

## ЮТСтроки.СравнитьВерсии

`Функция ЮТСтроки.СравнитьВерсии(Версия1, Версия2) Экспорт`

Сравнивает две версии программного обеспечения.

Ограничения:
- Версии должны быть представлены в формате "X.X.X.X", где X - целое число.

**Параметры:**
- `Версия1` — Строка — Первая версия для сравнения.
- `Версия2` — Строка — Вторая версия для сравнения.

**Возвращает:**
- `Число` — Результат сравнения версий.
- `1` — если Версия1 больше Версия2.
- `0` — если Версия1 равна Версия2; -1 - если Версия1 меньше Версия2;

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:228`.

## ЮТСтроки.ВерсияБольше

`Функция ЮТСтроки.ВерсияБольше(ПроверяемаяВерсия, БазоваяВерсия) Экспорт`

Выполняет сравнение версий

Ограничения:
- Версии должны быть представлены в формате "X.X.X.X", где X - целое число.

**Параметры:**
- `ПроверяемаяВерсия` — Строка — Проверяемая версия
- `БазоваяВерсия` — Строка — Базовая версия, с которой происходит сравнение

**Возвращает:**
- `Булево` — Проверяемая версия больше базовой

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:277`.

## ЮТСтроки.ЭтоУникальныйИдентификаторСтрокой

`Функция ЮТСтроки.ЭтоУникальныйИдентификаторСтрокой(Значение) Экспорт`

Проверяет, что переданное значение является строковым представлением уникального идентификатора

Ограничения:
- Проверяется только формат строки, но не её содержимое
- Строка должна быть в формате "XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX", где X - шестнадцатеричная цифра

**Параметры:**
- `Значение` — Строка — Проверяемая строка

**Возвращает:**
- `Булево` — Истина, если строка соответствует формату UUID

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:299`.

## ЮТСтроки.УникальныеСтроки

`Функция ЮТСтроки.УникальныеСтроки(НеУникальныеСтроки) Экспорт`

Из набора строк формирует набор уникальных строк.
Сравнение происходит без учета регистра.

**Параметры:**
- `НеУникальныеСтроки` — Массив Из Строка

**Возвращает:**
- Массив Из Строка - Уникальные строки

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:343`.

## ЮТСтроки.ЭтоВалидноеИмяПеременной

`Функция ЮТСтроки.ЭтоВалидноеИмяПеременной(ИмяПеременной) Экспорт`

Проверяет переданную строку, является ли она подходщим именем переменной - должна соответствовать требованиям к именам переменных

**Параметры:**
- `ИмяПеременной` — Строка

**Возвращает:**
- `Булево` — Это валидное имя переменной

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:377`.

## ЮТСтроки.ЛокализованноеСообщениеПлатформы

`Функция ЮТСтроки.ЛокализованноеСообщениеПлатформы(СтрокаНаРазныхЯзыках) Экспорт`

Получает строку на языке платформы из набора строк на разных языках платформы.

**Параметры:**
- `СтрокаНаРазныхЯзыках` — Строка — Строки на разных языках, разделенные символом ";" (точка с запятой). Строка на одном языке состоит из кода языка, указанного в метаданных, символа "=" (равно) и собственно строки текста на данном языке в одинарных кавычках, двойных кавычках или без кавычек (когда указывается только один язык).

**Возвращает:**
- `Строка` — Локализованное сообщение платформы

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:405`.

## ЮТСтроки.ЭтоЧисловаяСтрока

`Функция ЮТСтроки.ЭтоЧисловаяСтрока(Знач Строка) Экспорт`

Проверяет, является ли строка числовой

**Параметры:**
- `Строка` — Строка — Проверяемая строка

**Возвращает:**
- `Булево` — Истина, если строка является числовой

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:425`.

## ЮТСтроки.СоответствуетШаблону

`Функция ЮТСтроки.СоответствуетШаблону(Строка, Шаблон) Экспорт`

Проверяет что строк соответствует "простому" шаблону - тексту со звездочками.

**Параметры:**
- `Строка` — Строка — Проверямая строка
- `Шаблон` — Строка — Шаблон

**Возвращает:**
- `Булево` — Соответствует шаблону

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТСтроки/Module.bsl:474`.
