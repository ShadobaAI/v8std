---
title: API ЮТКомпоненты
description: "Публичные экспортные методы модуля ЮТКомпоненты из ядра YaXUnit."
tags: [yaxunit, api, ЮТКомпоненты]
publish_publicly: false
---

# API ЮТКомпоненты

Компактный справочник по 9 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКомпоненты.Пауза

`Функция ЮТКомпоненты.Пауза() Экспорт`

Возвращает внешнюю компоненту, реализующую паузу
Предоставляет метод `Ожидать`, `Sleep`

**Возвращает:**
- ВнешнийОбъект

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:26`.

## ЮТКомпоненты.Консоль

`Функция ЮТКомпоненты.Консоль() Экспорт`

Возвращает внешнюю компоненту, реализующую вывод в консоль
Предоставляет метод `Напечатать`, `Print`

**Возвращает:**
- ВнешнийОбъект

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:37`.

## ЮТКомпоненты.РегулярныеВыражения

`Функция ЮТКомпоненты.РегулярныеВыражения() Экспорт`

Возвращает внешнюю компоненту, реализующую функциональность регулярных выражений
Подробнее с документацией можно ознакомиться по ссылке: https://github.com/alexkmbk/RegEx1CAddin

**Возвращает:**
- ВнешнийОбъект

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:48`.

## ЮТКомпоненты.СоздатьКомпоненту

`Функция ЮТКомпоненты.СоздатьКомпоненту(ОписаниеКомпоненты, Кэшировать = Ложь) Экспорт`

Создает внешнюю компоненту по описанию

**Параметры:**
- `ОписаниеКомпоненты` — см. ОписаниеКомпоненты
- `Кэшировать` — Булево — Кэшировать созданную компоненту

**Возвращает:**
- ВнешнийОбъект

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:74`.

## ЮТКомпоненты.ПодключитьКомпоненту

`Функция ЮТКомпоненты.ПодключитьКомпоненту(ИмяМакета, ИмяКомпоненты, Кэшировать) Экспорт`

Подключает внешнюю компоненту из макета

**Параметры:**
- `ИмяМакета` — Строка — Имя макета, в котором хранится компонента
- `ИмяКомпоненты` — Строка — Имя компоненты
- `Кэшировать` — Булево — Кэшировать создание компоненты

**Возвращает:**
- `Булево` — Успешность подключения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:107`.

## ЮТКомпоненты.ОписаниеКомпонентыСервисногоНазначения

`Функция ЮТКомпоненты.ОписаниеКомпонентыСервисногоНазначения() Экспорт`

Описание компоненты, реализующей паузу, консоль и другие

**Возвращает:**
- см. ОписаниеКомпоненты

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:128`.

## ЮТКомпоненты.ОписаниеКомпонентыРегулярныхВыражений

`Функция ЮТКомпоненты.ОписаниеКомпонентыРегулярныхВыражений() Экспорт`

Описание компоненты реализующей функциональность регулярных выражений.

**Возвращает:**
- см. ОписаниеКомпоненты

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:138`.

## ЮТКомпоненты.ОписаниеКомпонентыВебСокет

`Функция ЮТКомпоненты.ОписаниеКомпонентыВебСокет() Экспорт`

Описание компоненты реализующей функциональность регулярных выражений.

**Возвращает:**
- см. ОписаниеКомпоненты

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:148`.

## ЮТКомпоненты.ОписаниеКомпоненты

`Функция ЮТКомпоненты.ОписаниеКомпоненты(ИмяМакета, ИмяКомпоненты, ИмяКласса) Экспорт`

Описание внешней компоненты.

**Параметры:**
- `ИмяМакета` — Строка — Имя макета, в котором хранится компонента
- `ИмяКомпоненты` — Строка -Имя компоненты
- `ИмяКласса` — Строка — Имя класса, по которому создается компонента

**Возвращает:**
- `Структура` — Описание компоненты: * ИмяМакета - Строка - Имя макета, в котором хранится компонента * ИмяКомпоненты - Строка -Имя компоненты * ИмяКласса - Строка - Имя класса

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКомпоненты/Module.bsl:166`.
