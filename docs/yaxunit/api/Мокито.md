---
title: API Мокито
description: "Публичные экспортные методы модуля Мокито из ядра YaXUnit."
tags: [yaxunit, api, Мокито]
publish_publicly: false
---

# API Мокито

Компактный справочник по 11 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## Мокито.Обучение

`Функция Мокито.Обучение(Объект, СброситьСтарыеНастройки = Истина) Экспорт`

Начинает обучение (настройку) Мокито.
После вызова этого метода следует набор правил для подмены логики работы системы.

**Параметры:**
- `Объект` — Произвольный — Объект, методы которого хотим подменить.
- `СброситьСтарыеНастройки` — Булево — Необходимо удалить старые настройки по объекту. + `Истина` - все предыдущие настройки мокирования объекта будут забыты. + `Ложь` - будет выполнено дообучение объекта.

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:65`.

## Мокито.Прогон

`Процедура Мокито.Прогон(СброситьСтатистику = Истина) Экспорт`

Переводит мокито в режим прогона тестов.

Важно! Вызов этого метода обязателен перед выполнением тестового прогона метода.

**Параметры:**
- `СброситьСтатистику` — Булево — Сбросить статистику прошлых прогонов

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:80`.

## Мокито.Проверить

`Функция Мокито.Проверить(Объект, Описание = Неопределено) Экспорт`

Переводит мокито в режим проверки собранной за прогон статистики вызовов.

**Параметры:**
- `Объект` — Произвольный — Устанавливает проверяемый объект, вызовы методов которого будем проверять.
- `Описание` — Строка — Описание проверки, которое будет выведено при возникновении ошибки

**Возвращает:**
- `ОбщийМодуль` — см. МокитоПроверки

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:98`.

## Мокито.Сбросить

`Процедура Мокито.Сбросить() Экспорт`

Сбрасывает настройки и повторно инициализирует мокито.

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:108`.

## Мокито.ЛюбойПараметр

`Функция Мокито.ЛюбойПараметр() Экспорт`

Возвращает маску параметра. Используется при обучении и проверках для фильтрации входных параметров метода.

Указывает, что в метод может передаваться любой параметр.

**Возвращает:**
- см. МокитоСлужебный.ОписаниеМаскиПараметра

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:121`.

## Мокито.ЧисловойПараметр

`Функция Мокито.ЧисловойПараметр() Экспорт`

Возвращает маску параметра. Используется при обучении и проверках для фильтрации входных параметров метода.

Указывает, что в метод может передаваться числовой параметр.

**Возвращает:**
- см. Мокито.ТипизированныйПараметр

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:133`.

## Мокито.СтроковыйПараметр

`Функция Мокито.СтроковыйПараметр() Экспорт`

Возвращает маску параметра. Используется при обучении и проверках для фильтрации входных параметров метода.

Указывает, что в метод может передаваться строковый параметр

**Возвращает:**
- см. Мокито.ТипизированныйПараметр

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:144`.

## Мокито.ТипизированныйПараметр

`Функция Мокито.ТипизированныйПараметр(Тип) Экспорт`

Возвращает маску параметра. Используется при обучении и проверках для фильтрации входных параметров метода.

Указывает, что в метод может передаваться параметр указанного типа.

**Параметры:**
- `Тип` — Тип — Ограничение типа параметра.

**Возвращает:**
- `Структура` — Описание маски параметра: * Режим - Строка - Тип маски  (значение: `Тип`) * Приоритет - Число - Приоритет маски, используется если значение подпадает под несколько масок  (значение: `10`) * Тип - Тип - Тип, которому должен соответствовать параметр

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:162`.

## Мокито.МассивПараметров

`Функция Мокито.МассивПараметров(Параметр1 = "_!%*", Параметр2 = "_!%*", Параметр3 = "_!%*", Параметр4 = "_!%*", Параметр5 = "_!%*", Параметр6 = "_!%*", Параметр7 = "_!%*", Параметр8 = "_!%*", Параметр9 = "_!%*", Параметр10 = "_!%*") Экспорт`

Формирует массив параметров. Применяется при обучении (настройке) мокито.
Если параметр пропущен, то будет использовать маска см. ЛюбойПараметр

**Параметры:**
- `Параметр1` — Произвольный
- `Параметр2` — Произвольный
- `Параметр3` — Произвольный
- `Параметр4` — Произвольный
- `Параметр5` — Произвольный
- `Параметр6` — Произвольный
- `Параметр7` — Произвольный
- `Параметр8` — Произвольный
- `Параметр9` — Произвольный
- `Параметр10` — Произвольный

**Возвращает:**
- Массив из Произвольный - Массив параметров BSLLS:NumberOfOptionalParams-off BSLLS:NumberOfParams-off BSLLS:DuplicateStringLiteral-off

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:192`.

## Мокито.ПараметрПоУмолчанию

`Функция Мокито.ПараметрПоУмолчанию() Экспорт`

Возвращает идентификатор значения входного параметра по умолчанию.

**Возвращает:**
- Строка

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:232`.

## Мокито.АнализВызова

`Функция Мокито.АнализВызова(Объект, ИмяМетода, ПараметрыМетода, ПрерватьВыполнение) Экспорт`

Устарела. Анализ вызова.

**Статус:** устарел. Используйте `МокитоПерехват.АнализВызова`. Переход объявлен в `24.03`.

**Параметры:**
- `Объект` — Произвольный
- `ИмяМетода` — Произвольный
- `ПараметрыМетода` — Произвольный
- `ПрерватьВыполнение` — Произвольный

**Возвращает:**
- `Произвольный` — Подменный результат работы метода

Источник ядра: `exts/yaxunit/src/CommonModules/Мокито/Module.bsl:256`.
