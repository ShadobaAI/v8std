---
title: API ЮТHTTPОтвет
description: "Публичные экспортные методы модуля ЮТHTTPОтвет из ядра YaXUnit."
tags: [yaxunit, api, ЮТHTTPОтвет]
publish_publicly: false
---

# API ЮТHTTPОтвет

Компактный справочник по 14 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТHTTPОтвет.GetBodyFileName

`Функция ЮТHTTPОтвет.GetBodyFileName() Экспорт`

Получает имя файла, в который было записано тело (ENG)

**Возвращает:**
- - Строка - Имя файла - Неопределено - Если тело не записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:52`.

## ЮТHTTPОтвет.GetBodyAsBinaryData

`Функция ЮТHTTPОтвет.GetBodyAsBinaryData() Экспорт`

Получает тело в виде двоичных данных (ENG)

**Возвращает:**
- - ДвоичныеДанные - Тело в виде двоичных данных - Неопределено - Если тело записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:70`.

## ЮТHTTPОтвет.GetBodyAsStream

`Функция ЮТHTTPОтвет.GetBodyAsStream() Экспорт`

Получает поток для чтения тела (ENG)

**Возвращает:**
- - Поток - Если тело не записывалось в файл - ФайловыйПоток - Если тело записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:87`.

## ЮТHTTPОтвет.GetBodyAsString

`Функция ЮТHTTPОтвет.GetBodyAsString(Кодировка = Неопределено) Экспорт`

Получает тело в виде строки (ENG)

**Параметры:**
- `Кодировка` — КодировкаТекста — Указывает кодировку, в которой должно интерпретироваться тело - Строка - Указывает кодировку, в которой должно интерпретироваться тело

**Возвращает:**
- - Строка - Тело в виде строки - Неопределено - Если тело записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:109`.

## ЮТHTTPОтвет.ПолучитьИмяФайлаТела

`Функция ЮТHTTPОтвет.ПолучитьИмяФайлаТела() Экспорт`

Получает имя файла, в который было записано тело

**Возвращает:**
- - Строка - Имя файла - Неопределено - Если тело не записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:127`.

## ЮТHTTPОтвет.ПолучитьТелоКакДвоичныеДанные

`Функция ЮТHTTPОтвет.ПолучитьТелоКакДвоичныеДанные() Экспорт`

Получает тело в виде двоичных данных

**Возвращает:**
- - ДвоичныеДанные - Тело в виде двоичных данных - Неопределено - Если тело записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:149`.

## ЮТHTTPОтвет.ПолучитьТелоКакПоток

`Функция ЮТHTTPОтвет.ПолучитьТелоКакПоток() Экспорт`

Получает поток для чтения тела

**Возвращает:**
- - Поток - Если тело не записывалось в файл - ФайловыйПоток - Если тело записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:180`.

## ЮТHTTPОтвет.ПолучитьТелоКакСтроку

`Функция ЮТHTTPОтвет.ПолучитьТелоКакСтроку(Кодировка = Неопределено) Экспорт`

Получает тело в виде строки

**Параметры:**
- `Кодировка` — КодировкаТекста — Указывает кодировку, в которой должно интерпретироваться тело - Строка - Указывает кодировку, в которой должно интерпретироваться тело

**Возвращает:**
- - Строка - Тело в виде строки - Неопределено - Если тело записывалось в файл

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:208`.

## ЮТHTTPОтвет.ЗаписатьТелоВФайл

`Функция ЮТHTTPОтвет.ЗаписатьТелоВФайл(Тело = Неопределено, ИмяФайла = Неопределено) Экспорт`

Записывает тело в файл, устанавливает тело и имя файла для чтения тела

**Параметры:**
- `Тело` — Строка — Тело в виде строки - ДвоичныеДанные - Тело в виде двоичных данных - Неопределено - Будет взято тело установленное через метод УстановитьТело()
- `ИмяФайла` — Строка — Имя файла - Неопределено - Будет взято имя установленное через метод УстановитьИмяФайлаТела() или сгенерировано имя временного файла

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:249`.

## ЮТHTTPОтвет.ДобавитьЗаголовки

`Функция ЮТHTTPОтвет.ДобавитьЗаголовки(Значения) Экспорт`

Добавляет заголовки

**Параметры:**
- `Значения` — Соответствие — Коллекция заголовков: * Ключ - Строка - Наименование заголовка * Значение - Строка - Значение заголовка

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:288`.

## ЮТHTTPОтвет.ДобавитьЗаголовок

`Функция ЮТHTTPОтвет.ДобавитьЗаголовок(Ключ, Значение) Экспорт`

Добавляет заголовок

**Параметры:**
- `Ключ` — Строка — Наименование заголовка
- `Значение` — Строка — Значение заголовка

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:311`.

## ЮТHTTPОтвет.УстановитьИмяФайлаТела

`Функция ЮТHTTPОтвет.УстановитьИмяФайлаТела(ИмяФайла) Экспорт`

Устанавливает имя файла для чтения тела

**Параметры:**
- `ИмяФайла` — Строка — Имя файла

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:331`.

## ЮТHTTPОтвет.УстановитьКодСостояния

`Функция ЮТHTTPОтвет.УстановитьКодСостояния(Значение) Экспорт`

Устанавливает код состояния

**Параметры:**
- `Значение` — Число — Код состояния

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:351`.

## ЮТHTTPОтвет.УстановитьТело

`Функция ЮТHTTPОтвет.УстановитьТело(Тело) Экспорт`

Устанавливает тело

**Параметры:**
- `Тело` — Строка — Тело в виде строки - ДвоичныеДанные - Тело в виде двоичных данных

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPОтвет/ObjectModule.bsl:375`.
