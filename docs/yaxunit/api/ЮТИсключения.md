---
title: API ЮТИсключения
description: "Публичные экспортные методы модуля ЮТИсключения из ядра YaXUnit."
tags: [yaxunit, api, ЮТИсключения]
publish_publicly: false
---

# API ЮТИсключения

Компактный справочник по 3 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТИсключения.НекорректныеПараметрыМетода

`Функция ЮТИсключения.НекорректныеПараметрыМетода(ИмяМетода, Пояснение = Неопределено) Экспорт`

Формирует текст исключения о некорректных параметрах вызова метода

**Параметры:**
- `ИмяМетода` — Строка
- `Пояснение` — Строка

**Возвращает:**
- `Строка` — Текст исключения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТИсключения/Module.bsl:29`.

## ЮТИсключения.НеподдерживаемыйПараметрМетода

`Функция ЮТИсключения.НеподдерживаемыйПараметрМетода(ИмяМетода, ЗначениеПараметра) Экспорт`

Формирует текст исключения о неподдерживаемом значении параметра вызова метода

**Параметры:**
- `ИмяМетода` — Строка
- `ЗначениеПараметра` — Произвольный

**Возвращает:**
- `Строка` — Текст исключения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТИсключения/Module.bsl:44`.

## ЮТИсключения.МетодНеДоступен

`Функция ЮТИсключения.МетодНеДоступен(ИмяМетода, ИмяКонтекста = "веб-клиенте") Экспорт`

Формирует текст исключения о недоступности метода в указанном контексте

**Параметры:**
- `ИмяМетода` — Строка
- `ИмяКонтекста` — Строка

**Возвращает:**
- `Строка` — Текст исключения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТИсключения/Module.bsl:58`.
