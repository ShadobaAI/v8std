---
title: API ЮТКонструкторТекста
description: "Публичные экспортные методы модуля ЮТКонструкторТекста из ядра YaXUnit."
tags: [yaxunit, api, ЮТКонструкторТекста]
publish_publicly: false
---

# API ЮТКонструкторТекста

Компактный справочник по 15 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКонструкторТекста.НоваяСтрока

`Функция ЮТКонструкторТекста.НоваяСтрока(Текст = "", НачалоСтроки = "", КонецСтроки = "") Экспорт`

Добавляет новую строку в конец коллекции строк с учетом текущего отступа.

**Параметры:**
- `Текст` — Строка — основное содержимое строки.
- `НачалоСтроки` — Строка — префикс, добавляемый перед текстом (по умолчанию пустая строка).
- `КонецСтроки` — Строка — суффикс, добавляемый после текста (по умолчанию пустая строка).

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:45`.

## ЮТКонструкторТекста.НоваяСтрокаПоШаблону

`Функция ЮТКонструкторТекста.НоваяСтрокаПоШаблону(Шаблон, П1 = "", П2 = "", П3 = "", П4 = "", П5 = "", П6 = "", П7 = "", П8 = "", П9 = "") Экспорт`

Формирует строку по шаблону с подстановкой параметров и добавляет ее в коллекцию.
Автоматически экранирует кавычки в параметрах.

**Параметры:**
- `Шаблон` — Строка — строка с плейсхолдерами (%1, %2, ..., %9) для подстановки параметров.
- `П1` — Произвольный — первый параметр для подстановки (по умолчанию пустая строка).
- `П2` — Произвольный — второй параметр для подстановки (по умолчанию пустая строка).
- `П3` — Произвольный — третий параметр для подстановки (по умолчанию пустая строка).
- `П4` — Произвольный — четвертый параметр для подстановки (по умолчанию пустая строка).
- `П5` — Произвольный — пятый параметр для подстановки (по умолчанию пустая строка).
- `П6` — Произвольный — шестой параметр для подстановки (по умолчанию пустая строка).
- `П7` — Произвольный — седьмой параметр для подстановки (по умолчанию пустая строка).
- `П8` — Произвольный — восьмой параметр для подстановки (по умолчанию пустая строка).
- `П9` — Произвольный — девятый параметр для подстановки (по умолчанию пустая строка).

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:79`.

## ЮТКонструкторТекста.ДобавитьТекст

`Функция ЮТКонструкторТекста.ДобавитьТекст(Текст, РазмерСдвига = 0, РазделительСтрок = Неопределено) Экспорт`

Добавляет текст, разбивая его на строки по разделителю, с применением отступа к каждой строке.

**Параметры:**
- `Текст` — Строка — текст для добавления, который будет разбит на строки.
- `РазмерСдвига` — Число — количество символов отступа для каждой строки (по умолчанию 0).
- `РазделительСтрок` — Строка, Неопределено — разделитель для разбиения текста на строки. Если Неопределено, используется символ перевода строки (Символы.ПС).

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:106`.

## ЮТКонструкторТекста.ВставитьСтроку

`Функция ЮТКонструкторТекста.ВставитьСтроку(Позиция, Текст, НачалоСтроки = "", КонецСтроки = "") Экспорт`

Вставляет строку в указанную позицию коллекции без применения текущего отступа.

**Параметры:**
- `Позиция` — Число — индекс позиции для вставки (начиная с 0).
- `Текст` — Строка — основное содержимое строки.
- `НачалоСтроки` — Строка — префикс, добавляемый перед текстом (по умолчанию пустая строка).
- `КонецСтроки` — Строка — суффикс, добавляемый после текста (по умолчанию пустая строка).

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:137`.

## ЮТКонструкторТекста.ВставитьПервуюСтроку

`Функция ЮТКонструкторТекста.ВставитьПервуюСтроку(Текст, НачалоСтроки = "", КонецСтроки = "") Экспорт`

Вставляет строку в начало коллекции (позиция 0) без применения текущего отступа.

**Параметры:**
- `Текст` — Строка — основное содержимое строки.
- `НачалоСтроки` — Строка — префикс, добавляемый перед текстом (по умолчанию пустая строка).
- `КонецСтроки` — Строка — суффикс, добавляемый после текста (по умолчанию пустая строка).

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:165`.

## ЮТКонструкторТекста.ПереносСтроки

`Функция ЮТКонструкторТекста.ПереносСтроки() Экспорт`

Добавляет символ перевода строки (Символы.ПС) в конец последней строки коллекции.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:183`.

## ЮТКонструкторТекста.УвеличитьОтступ

`Функция ЮТКонструкторТекста.УвеличитьОтступ() Экспорт`

Увеличивает размер текущего отступа на единицу.
Все последующие строки, добавляемые через НоваяСтрока, будут иметь увеличенный отступ.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:205`.

## ЮТКонструкторТекста.УменьшитьОтступ

`Функция ЮТКонструкторТекста.УменьшитьОтступ() Экспорт`

Уменьшает размер текущего отступа на единицу.
Все последующие строки, добавляемые через НоваяСтрока, будут иметь уменьшенный отступ.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:229`.

## ЮТКонструкторТекста.Табуляция

`Функция ЮТКонструкторТекста.Табуляция(Количество = 1) Экспорт`

Добавляет символы табуляции в начало последней строки коллекции.

**Параметры:**
- `Количество` — Число — количество символов табуляции для добавления (по умолчанию 1). Если значение меньше или равно 0, табуляция не добавляется.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:254`.

## ЮТКонструкторТекста.ДобавитьВКонецСтроки

`Функция ЮТКонструкторТекста.ДобавитьВКонецСтроки(Суффикс) Экспорт`

Добавляет суффикс в конец последней строки коллекции.

**Параметры:**
- `Суффикс` — Строка — текст, добавляемый в конец последней строки.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:284`.

## ЮТКонструкторТекста.Разделитель

`Функция ЮТКонструкторТекста.Разделитель(Разделитель) Экспорт`

Устанавливает разделитель для объединения строк при получении итогового текста.
По умолчанию используется символ перевода строки (Символы.ПС).

**Параметры:**
- `Разделитель` — Строка — разделитель, используемый при объединении строк методом Текст().

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:309`.

## ЮТКонструкторТекста.Текст

`Функция ЮТКонструкторТекста.Текст() Экспорт`

Возвращает объединенный текст всех строк коллекции с использованием установленного разделителя.

**Возвращает:**
- `Строка` — объединенный текст всех строк коллекции.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:329`.

## ЮТКонструкторТекста.КоличествоСтрок

`Функция ЮТКонструкторТекста.КоличествоСтрок() Экспорт`

Возвращает количество строк в коллекции.

**Возвращает:**
- `Число` — количество строк в коллекции.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:347`.

## ЮТКонструкторТекста.УстановитьТекст

`Функция ЮТКонструкторТекста.УстановитьТекст(Текст) Экспорт`

Заменяет текущие строки коллекции на строки, полученные из переданного текста.
Текст разбивается на строки с использованием текущего разделителя.

**Параметры:**
- `Текст` — Строка — текст для разбиения на строки и установки в коллекцию.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТекста - текущий объект для поддержки цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:368`.

## ЮТКонструкторТекста.Инициализировать

`Процедура ЮТКонструкторТекста.Инициализировать() Экспорт`

Инициализирует контекст конструктора текста.
Должна быть вызвана перед использованием всех остальных методов.
Создает пустую коллекцию строк, устанавливает разделитель по умолчанию (Символы.ПС)
и символ отступа (Символы.Таб).

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТекста/ObjectModule.bsl:393`.
