---
title: API МокитоПроверки
description: "Публичные экспортные методы модуля МокитоПроверки из ядра YaXUnit."
tags: [yaxunit, api, МокитоПроверки]
publish_publicly: false
---

# API МокитоПроверки

Компактный справочник по 8 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## МокитоПроверки.Проверить

`Функция МокитоПроверки.Проверить(Объект, Описание = Неопределено) Экспорт`

см. Мокито.Проверить

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:22`.

## МокитоПроверки.КоличествоВызовов

`Функция МокитоПроверки.КоличествоВызовов(ИмяМетода = Неопределено, ПараметрыВызова = Неопределено) Экспорт`

Начинает утверждение для проверки количества вызовов метода.

Устанавливает параметры поиска вызовов.

**Параметры:**
- `ИмяМетода` — Строка — Имя метода проверяемого объекта - Произвольный - Вызов метода проверяемого объекта
- `ПараметрыВызова` — Массив из Произвольный — Параметры вызова метода проверяемого объекта, необходим при указании имени метода

**Возвращает:**
- `ОбщийМодуль` — см. МокитоПроверки

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:38`.

## МокитоПроверки.Вызовы

`Функция МокитоПроверки.Вызовы(ИмяМетода = Неопределено, ПараметрыВызова = Неопределено) Экспорт`

Возвращает информацию о вызовах метода.

**Параметры:**
- `ИмяМетода` — Строка — Имя метода проверяемого объекта - Произвольный - Вызов метода проверяемого объекта
- `ПараметрыВызова` — Массив из Произвольный — Параметры вызова метода проверяемого объекта, необходим при указании имени метода

**Возвращает:**
- Массив из см. МокитоСлужебный.СтруктураВызоваМетода - Вызовы метода

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:54`.

## МокитоПроверки.Равно

`Функция МокитоПроверки.Равно(Количество, Знач Описание = Неопределено) Экспорт`

Проверяет вызовы метода (см. КоличествоВызовов).
Их количество должно быть равно переданному значение, иначе будет выброшено исключение.

**Параметры:**
- `Количество` — Число — Ожидаемое количество вызовов
- `Описание` — Строка — Описание проверки, необходимо для идентификации упавшей проверки

**Возвращает:**
- `ОбщийМодуль` — см. МокитоПроверки

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:71`.

## МокитоПроверки.Больше

`Функция МокитоПроверки.Больше(Количество, Знач Описание = Неопределено) Экспорт`

Проверяет вызовы метода (см. КоличествоВызовов).
Их количество должно быть больше переданного значения,, иначе будет выброшено исключение.

**Параметры:**
- `Количество` — Число
- `Описание` — Строка — Описание проверки, необходимо для идентификации упавшей проверки

**Возвращает:**
- `ОбщийМодуль` — см. МокитоПроверки

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:93`.

## МокитоПроверки.Меньше

`Функция МокитоПроверки.Меньше(Количество, Знач Описание = Неопределено) Экспорт`

Проверяет вызовы метода (см. КоличествоВызовов).
Их количество должно быть меньше переданного значения, иначе будет выброшено исключение.

**Параметры:**
- `Количество` — Число
- `Описание` — Строка — Описание проверки, необходимо для идентификации упавшей проверки

**Возвращает:**
- `ОбщийМодуль` — см. МокитоПроверки

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:113`.

## МокитоПроверки.Заполнено

`Функция МокитоПроверки.Заполнено(Знач Описание = Неопределено) Экспорт`

Проверяет, что есть вызовы метода (см. КоличествоВызовов), соответствующие условию.

**Параметры:**
- `Описание` — Строка — Описание проверки, необходимо для идентификации упавшей проверки

**Возвращает:**
- `ОбщийМодуль` — см. МокитоПроверки

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:131`.

## МокитоПроверки.Пусто

`Функция МокитоПроверки.Пусто(Знач Описание = Неопределено) Экспорт`

Проверяет, что нет вызовов метода (см. КоличествоВызовов), соответствующих условию.

**Параметры:**
- `Описание` — Строка — Описание проверки, необходимо для идентификации упавшей проверки

**Возвращает:**
- `ОбщийМодуль` — см. МокитоПроверки

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоПроверки/Module.bsl:149`.
