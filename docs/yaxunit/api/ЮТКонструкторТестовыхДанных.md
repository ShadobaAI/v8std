---
title: API ЮТКонструкторТестовыхДанных
description: "Публичные экспортные методы модуля ЮТКонструкторТестовыхДанных из ядра YaXUnit."
tags: [yaxunit, api, ЮТКонструкторТестовыхДанных]
publish_publicly: false
---

# API ЮТКонструкторТестовыхДанных

Компактный справочник по 19 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКонструкторТестовыхДанных.Установить

`Функция ЮТКонструкторТестовыхДанных.Установить(ИмяРеквизита, Значение) Экспорт`

Устанавливает значение реквизита создаваемого объекта.

Если метод вызывается после вызова см. ТабличнаяЧасть, то устанавливается значение реквизита строки табличной части.

**Параметры:**
- `ИмяРеквизита` — Строка — Имя реквизита объекта или табличной части.
- `Значение` — Произвольный — Значение реквизита объекта или табличной части.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:47`.

## ЮТКонструкторТестовыхДанных.УстановитьРеквизиты

`Функция ЮТКонструкторТестовыхДанных.УстановитьРеквизиты(ЗначенияРеквизитов) Экспорт`

Устанавливает значения реквизитов создаваемого объекта.

Если метод вызывается после вызова см. ТабличнаяЧасть, то устанавливаются значения реквизитов строки табличной части.

**Параметры:**
- `ЗначенияРеквизитов` — Структура — Устанавливаемые значения реквизитов. Ключ структуры — имя реквизита, Значение — значение реквизита.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:71`.

## ЮТКонструкторТестовыхДанных.Фикция

`Функция ЮТКонструкторТестовыхДанных.Фикция(ИмяРеквизита, РеквизитыЗаполнения = Неопределено, ОграничениеТипа = Неопределено) Экспорт`

Устанавливает фиктивное значение реквизита создаваемого объекта.

Если метод вызывается после вызова см. ТабличнаяЧасть, то устанавливается значение реквизита строки табличной части.

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение:
* Для примитивных значение генерируется случайное значение.
* Для ссылочных типов создается новый объект.
* Для перечислений (в том числе системных) выбирается случайно значение перечисления.

**Параметры:**
- `ИмяРеквизита` — Строка — Имя реквизита объекта или табличной части.
- `РеквизитыЗаполнения` — Структура — Значения реквизитов заполнения создаваемого объекта базы. - Неопределено - Новый объект будет создан с пустыми или фиктивными реквизитами по умолчанию.
- `ОграничениеТипа` — Тип — Используется для ограничения типа составного реквизита, будет сгенерировано значение указанного типа. - ОписаниеТипов - Используется для ограничения типа составного реквизита. - Неопределено - Будет сгенерировано значение на основании типа реквизита, если реквизит составной, то будет использован случайный тип.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:106`.

## ЮТКонструкторТестовыхДанных.ФикцияРеквизитов

`Функция ЮТКонструкторТестовыхДанных.ФикцияРеквизитов(ИменаРеквизитов) Экспорт`

Устанавливает фиктивные значения реквизитов создаваемого объекта.

Если метод вызывается после вызова см. ТабличнаяЧасть, то устанавливаются значения реквизитов строки табличной части.

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение:
* Для примитивных значение генерируется случайное значение.
* Для ссылочных типов создается новый объект.
* Для перечислений (в том числе системных) выбирается случайно значение перечисления.

**Параметры:**
- `ИменаРеквизитов` — Строка — Имена реквизитов объекта или табличной части, разделенные запятыми. - Массив из Строка - Массив имен реквизитов объекта или табличной части.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:135`.

## ЮТКонструкторТестовыхДанных.ФикцияНезаполненных

`Функция ЮТКонструкторТестовыхДанных.ФикцияНезаполненных(ИменаРеквизитов) Экспорт`

Устанавливает фиктивные значения незаполненных реквизитов создаваемого объекта.

Если метод вызывается после вызова см. ТабличнаяЧасть, то устанавливаются значения реквизитов строки табличной части.

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение:
* Для примитивных значение генерируется случайное значение.
* Для ссылочных типов создается новый объект.
* Для перечислений (в том числе системных) выбирается случайно значение перечисления.

**Параметры:**
- `ИменаРеквизитов` — Строка — Имена реквизитов объекта или табличной части, разделенные запятыми. - Массив из Строка - Массив имен реквизитов объекта или табличной части.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:165`.

## ЮТКонструкторТестовыхДанных.ФикцияНезаполненныхИсключая

`Функция ЮТКонструкторТестовыхДанных.ФикцияНезаполненныхИсключая(ИменаИсключений) Экспорт`

Устанавливает фиктивные значения незаполненных реквизитов создаваемого объекта, исключая указанные.

Если метод вызывается после вызова см. ТабличнаяЧасть, то устанавливается значения реквизитов строки табличной части.

На основании менеджера и имени реквизита вычисляется его тип, а по типу генерируется фиктивное значение.

* Для примитивных значение генерируется случайное значение
* Для ссылочных типов создается новый объект
* Для перечислений (в том числе системных) выбирается случайно значение перечисления

**Параметры:**
- `ИменаИсключений` — Строка — Имена исключаемых реквизитов объекта или табличной части разделенные запятыми - Массив из Строка - Имена исключаемых реквизитов объекта или табличной части

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:188`.

## ЮТКонструкторТестовыхДанных.ФикцияОбязательныхПолей

`Функция ЮТКонструкторТестовыхДанных.ФикцияОбязательныхПолей() Экспорт`

Заполняет обязательные поля объекта фиктивными значениями.

Если метод вызывается после вызова см. ТабличнаяЧасть, то устанавливаются значения реквизитов строки табличной части.

На основании метаданных определяются обязательные поля (Проверка заполнения = Выдавать ошибку),
для них генерируются и устанавливаются фиктивные значения:
* Для примитивных значение генерируется случайное значение.
* Для ссылочных типов создается новый объект.
* Для перечислений (в том числе системных) выбирается случайно значение перечисления.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:214`.

## ЮТКонструкторТестовыхДанных.ТабличнаяЧасть

`Функция ЮТКонструкторТестовыхДанных.ТабличнаяЧасть(ИмяТабличнойЧасти) Экспорт`

Переключает конструктор на заполнение табличной части объекта.

**Параметры:**
- `ИмяТабличнойЧасти` — Строка — Имя табличной части.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:237`.

## ЮТКонструкторТестовыхДанных.Объект

`Функция ЮТКонструкторТестовыхДанных.Объект() Экспорт`

Переключает конструктор на заполнение объекта (шапки документа/элемента справочника).

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:257`.

## ЮТКонструкторТестовыхДанных.ДобавитьСтроку

`Функция ЮТКонструкторТестовыхДанных.ДобавитьСтроку(ЗначенияРеквизитов = Неопределено) Экспорт`

Добавляет новую строку табличной части объекта.
Актуально после вызова см. ТабличнаяЧасть.
При необходимости можно выполнить установку реквизитов новой строки.

**Параметры:**
- `ЗначенияРеквизитов` — Структура — Устанавливаемые значения реквизитов новой строки. - Неопределено - Строка добавляется с пустыми значениями.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:284`.

## ЮТКонструкторТестовыхДанных.УстановитьДополнительноеСвойство

`Функция ЮТКонструкторТестовыхДанных.УстановитьДополнительноеСвойство(ИмяСвойства, Значение = Неопределено) Экспорт`

Устанавливает дополнительное свойство объекта.
Эти свойства используются при записи объекта через ОбменДанными.Загрузка = Истина
или для специальных случаев платформы.

**Параметры:**
- `ИмяСвойства` — Строка — Имя дополнительного свойства (например, "ОбменДаннымиЗагрузка", "ДополнительныеСвойства").
- `Значение` — Произвольный — Значение дополнительного свойства.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:309`.

## ЮТКонструкторТестовыхДанных.УстановитьСсылкуНового

`Функция ЮТКонструкторТестовыхДанных.УстановитьСсылкуНового(Значение) Экспорт`

Устанавливает предопределенный уникальный идентификатор для создаваемого объекта.
Полезно, когда нужно создать объект с известным заранее GUID.

**Параметры:**
- `Значение` — УникальныйИдентификатор — Уникальный идентификатор для нового объекта. - Строка - Строковое представление уникального идентификатора для нового объекта.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:335`.

## ЮТКонструкторТестовыхДанных.Записать

`Функция ЮТКонструкторТестовыхДанных.Записать(ВернутьОбъект = Ложь, ОбменДаннымиЗагрузка = Неопределено) Экспорт`

Создает новый объект и возвращает его или ссылку.

Важно: после создания объекта данные в конструкторе не очищаются.
Многократный вызов этого метода создаст несколько идентичных объектов (если УстановитьСсылкуНового() не вызывался).
Для создания полностью нового, независимого объекта, необходимо заново инициализировать конструктор
(вызвать `DataProcessors.ЮТКонструкторТестовыхДанных.Создать()` и `Инициализировать()`).

**Параметры:**
- `ВернутьОбъект` — Булево — Если Истина, будет возвращен объект метаданных (например, СправочникОбъект), иначе будет возвращена ссылка (например, СправочникСсылка). По умолчанию Ложь.
- `ОбменДаннымиЗагрузка` — Булево — Если Истина, объект будет записан с `ОбменДанными.Загрузка = Истина`. - Неопределено - Используется значение по умолчанию из настроек конструктора (см. ЮТКонтекст.ОпцииДвижка().ОбменДаннымиЗагрузкаПриСозданииДанных).

**Возвращает:**
- `Произвольный` — Созданный объект или ссылка на него, в зависимости от параметра ВернутьОбъект.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:366`.

## ЮТКонструкторТестовыхДанных.ДобавитьЗапись

`Функция ЮТКонструкторТестовыхДанных.ДобавитьЗапись(ОбменДаннымиЗагрузка = Неопределено) Экспорт`

Записывает новый объект и возвращает конструктор для продолжения цепочки вызовов.
Отличается от см. Записать тем, что не возвращает созданный объект/ссылку, а текущий экземпляр конструктора.

Важно: после создания объекта данные в конструкторе не очищаются.
Многократный вызов этого метода создаст несколько идентичных объектов.
Для создания полностью нового, независимого объекта, необходимо заново инициализировать конструктор.

**Параметры:**
- `ОбменДаннымиЗагрузка` — Булево — Если Истина, объект будет записан с `ОбменДанными.Загрузка = Истина`. - Неопределено - Используется значение по умолчанию из настроек конструктора.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:392`.

## ЮТКонструкторТестовыхДанных.НовыйОбъект

`Функция ЮТКонструкторТестовыхДанных.НовыйОбъект() Экспорт`

Создает и возвращает новый объект метаданных (не записывая его в базу данных).
Полезно для подготовки объекта и его последующей передачи в другие методы или для проверок.

Важно: после вызова этого метода данные в конструкторе не очищаются.
Для создания нового, независимого объекта, необходимо заново инициализировать конструктор.

**Возвращает:**
- `Произвольный` — Созданный объект метаданных (например, СправочникОбъект.Номенклатура).

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:418`.

## ЮТКонструкторТестовыхДанных.Провести

`Функция ЮТКонструкторТестовыхДанных.Провести(ВернутьОбъект = Ложь) Экспорт`

Создает и проводит новый документ. Возвращает объект или ссылку на него.

Важно: после создания и проведения документа данные в конструкторе не очищаются.
Многократный вызов этого метода создаст и проведет несколько идентичных документов.
Для создания нового, независимого документа, необходимо заново инициализировать конструктор.

**Параметры:**
- `ВернутьОбъект` — Булево — Если Истина, будет возвращен проведенный объект документа, иначе будет возвращена ссылка на проведенный документ. По умолчанию Ложь.

**Возвращает:**
- `Произвольный` — Проведенный документ (объект или ссылка) в зависимости от параметра ВернутьОбъект.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:445`.

## ЮТКонструкторТестовыхДанных.ДанныеСтроки

`Функция ЮТКонструкторТестовыхДанных.ДанныеСтроки() Экспорт`

Возвращает данные текущей строки табличной части объекта, если конструктор находится в режиме заполнения ТЧ.

**Возвращает:**
- - Структура - Данные текущей заполняемой строки табличной части. Ключи - имена реквизитов ТЧ, значения - их значения. - Неопределено - Если конструктор не находится в режиме заполнения табличной части (т.е. заполняется шапка объекта).

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:465`.

## ЮТКонструкторТестовыхДанных.ДанныеОбъекта

`Функция ЮТКонструкторТестовыхДанных.ДанныеОбъекта() Экспорт`

Возвращает данные создаваемого объекта (шапки и всех табличных частей).

**Возвращает:**
- `Структура` — Данные объекта. Содержит реквизиты шапки. Если есть табличные части, они представлены как вложенные массивы структур.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:488`.

## ЮТКонструкторТестовыхДанных.Инициализировать

`Процедура ЮТКонструкторТестовыхДанных.Инициализировать(Менеджер) Экспорт`

Экспортный метод модуля `ЮТКонструкторТестовыхДанных`.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТКонструкторТестовыхДанных/ObjectModule.bsl:498`.
