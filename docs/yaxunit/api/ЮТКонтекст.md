---
title: API ЮТКонтекст
description: "Публичные экспортные методы модуля ЮТКонтекст из ядра YaXUnit."
tags: [yaxunit, api, ЮТКонтекст]
publish_publicly: false
---

# API ЮТКонтекст

Компактный справочник по 3 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКонтекст.ЗначениеКонтекста

`Функция ЮТКонтекст.ЗначениеКонтекста(ИмяРеквизита) Экспорт`

ЗначениеКонтекста
Возвращает значение вложенного контекста, вложенного реквизита контекста

**Параметры:**
- `ИмяРеквизита` — Строка — Имя реквизита/вложенного контекста

**Возвращает:**
- - Структура - Значение реквизита/вложенного контекста - Неопределено

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКонтекст/Module.bsl:28`.

## ЮТКонтекст.УстановитьЗначениеКонтекста

`Процедура ЮТКонтекст.УстановитьЗначениеКонтекста(ИмяРеквизита, Значение) Экспорт`

УстановитьЗначениеКонтекста
Устанавливает значение вложенного контекста, вложенного реквизита контекста

**Параметры:**
- `ИмяРеквизита` — Строка — Имя реквизита/вложенного контекста
- `Значение` — Произвольный — Новое значение реквизита/вложенного контекста

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКонтекст/Module.bsl:40`.

## ЮТКонтекст.ОпцииДвижка

`Функция ЮТКонтекст.ОпцииДвижка() Экспорт`

Параметры движка.

**Возвращает:**
- см. ЮТФабрика.ОпцииДвижка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКонтекст/Module.bsl:50`.
