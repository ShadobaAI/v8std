---
title: API ЮТHTTPСервисЗапрос
description: "Публичные экспортные методы модуля ЮТHTTPСервисЗапрос из ядра YaXUnit."
tags: [yaxunit, api, ЮТHTTPСервисЗапрос]
publish_publicly: false
---

# API ЮТHTTPСервисЗапрос

Компактный справочник по 12 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТHTTPСервисЗапрос.ПолучитьТелоКакДвоичныеДанные

`Функция ЮТHTTPСервисЗапрос.ПолучитьТелоКакДвоичныеДанные() Экспорт`

Получает тело HTTP-запроса в виде двоичных данных.
Если тело не установлено, возвращает пустые двоичные данные.
Если тело установлено как строка, оно будет сконвертировано в двоичные данные.

**Возвращает:**
- `ДвоичныеДанные` — Тело HTTP-запроса.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:56`.

## ЮТHTTPСервисЗапрос.ПолучитьТелоКакПоток

`Функция ЮТHTTPСервисЗапрос.ПолучитьТелоКакПоток() Экспорт`

Получает тело HTTP-запроса в виде потока.
Является оберткой над методом ПолучитьТелоКакДвоичныеДанные().ОткрытьПотокДляЧтения().

**Возвращает:**
- `Поток` — Поток для чтения тела HTTP-запроса.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:83`.

## ЮТHTTPСервисЗапрос.ПолучитьТелоКакСтроку

`Функция ЮТHTTPСервисЗапрос.ПолучитьТелоКакСтроку(Кодировка = Неопределено) Экспорт`

Получает тело HTTP-запроса в виде строки.
Если тело не установлено, возвращает пустую строку.
Если тело установлено как двоичные данные, оно будет сконвертировано в строку с указанной или системной кодировкой.

**Параметры:**
- `Кодировка` — КодировкаТекста, Строка — Кодировка, используемая для преобразования двоичных данных в строку. Необязательный. По умолчанию используется системная кодировка.

**Возвращает:**
- `Строка` — Тело HTTP-запроса.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:107`.

## ЮТHTTPСервисЗапрос.УстановитьТелоКакДвоичныеДанные

`Функция ЮТHTTPСервисЗапрос.УстановитьТелоКакДвоичныеДанные(Данные) Экспорт`

Устанавливает тело HTTP-запроса из двоичных данных.

**Параметры:**
- `Данные` — ДвоичныеДанные — Двоичные данные для установки в качестве тела запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:134`.

## ЮТHTTPСервисЗапрос.УстановитьТелоКакСтроку

`Функция ЮТHTTPСервисЗапрос.УстановитьТелоКакСтроку(Строка) Экспорт`

Устанавливает тело HTTP-запроса из строки.

**Параметры:**
- `Строка` — Строка — Строковое значение для установки в качестве тела запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:153`.

## ЮТHTTPСервисЗапрос.УстановитьТелоКакСтрокуJSON

`Функция ЮТHTTPСервисЗапрос.УстановитьТелоКакСтрокуJSON(Данные) Экспорт`

Устанавливает тело HTTP-запроса как строку JSON.
Сериализует переданные данные в строку JSON и устанавливает ее в качестве тела запроса.

**Параметры:**
- `Данные` — Произвольный — Данные для сериализации в JSON.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:174`.

## ЮТHTTPСервисЗапрос.ДобавитьЗаголовок

`Функция ЮТHTTPСервисЗапрос.ДобавитьЗаголовок(ИмяЗаголовка, Значение) Экспорт`

Добавляет заголовок HTTP-запроса.
Если заголовок с таким именем уже существует, его значение будет перезаписано.

**Параметры:**
- `ИмяЗаголовка` — Строка — Имя HTTP-заголовка.
- `Значение` — Строка — Значение HTTP-заголовка.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:200`.

## ЮТHTTPСервисЗапрос.ДобавитьПараметрЗапроса

`Функция ЮТHTTPСервисЗапрос.ДобавитьПараметрЗапроса(ИмяПараметра, Значение) Экспорт`

Добавляет параметр запроса (query parameter).
Параметры запроса обычно используются в URL после знака вопроса (?).
Если параметр с таким именем уже существует, его значение будет перезаписано.

**Параметры:**
- `ИмяПараметра` — Строка — Имя параметра запроса.
- `Значение` — Строка — Значение параметра запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:222`.

## ЮТHTTPСервисЗапрос.ДобавитьПараметрURL

`Функция ЮТHTTPСервисЗапрос.ДобавитьПараметрURL(ИмяПараметра, Значение) Экспорт`

Добавляет параметр URL.
Параметры URL используются для построения динамических URL и обычно являются частью пути.
Если параметр с таким именем уже существует, его значение будет перезаписано.

**Параметры:**
- `ИмяПараметра` — Строка — Имя параметра URL.
- `Значение` — Строка — Значение параметра URL.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:244`.

## ЮТHTTPСервисЗапрос.Метод

`Функция ЮТHTTPСервисЗапрос.Метод(Значение) Экспорт`

Устанавливает HTTP-метод для запроса.
Например, "GET", "POST", "PUT", "DELETE".

**Параметры:**
- `Значение` — Строка — Имя HTTP-метода.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:264`.

## ЮТHTTPСервисЗапрос.БазовыйURL

`Функция ЮТHTTPСервисЗапрос.БазовыйURL(Значение) Экспорт`

Устанавливает базовый URL для запроса.
Например, "http://localhost:8080".

**Параметры:**
- `Значение` — Строка — Базовый URL.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:284`.

## ЮТHTTPСервисЗапрос.ОтносительныйURL

`Функция ЮТHTTPСервисЗапрос.ОтносительныйURL(Значение) Экспорт`

Устанавливает относительный URL для запроса.
Например, "/api/users".

**Параметры:**
- `Значение` — Строка — Относительный URL.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Текущий объект запроса для возможности выстраивания цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТHTTPСервисЗапрос/ObjectModule.bsl:304`.
