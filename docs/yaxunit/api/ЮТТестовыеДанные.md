---
title: API ЮТТестовыеДанные
description: "Публичные экспортные методы модуля ЮТТестовыеДанные из ядра YaXUnit."
tags: [yaxunit, api, ЮТТестовыеДанные]
publish_publicly: false
---

# API ЮТТестовыеДанные

Компактный справочник по 48 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТТестовыеДанные.СоздатьЭлемент

`Функция ЮТТестовыеДанные.СоздатьЭлемент(Менеджер, Наименование = Неопределено, Реквизиты = Неопределено, Знач ПараметрыЗаписи = Неопределено) Экспорт`

Создает новый элемент и возвращает его ссылку.

**Параметры:**
- `Менеджер` — Произвольный — Менеджер справочника/ПВХ и тд.
- `Наименование` — Строка, Неопределено — Наименование элемента
- `Реквизиты` — Структура, Неопределено — Значения реквизитов элемента
- `ПараметрыЗаписи` — см. ЮТОбщий.ПараметрыЗаписи

**Возвращает:**
- `ЛюбаяСсылка` — Ссылка на созданный объект

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:31`.

## ЮТТестовыеДанные.СоздатьДокумент

`Функция ЮТТестовыеДанные.СоздатьДокумент(Знач Менеджер, Реквизиты = Неопределено, Знач ПараметрыЗаписи = Неопределено) Экспорт`

Создает новый документ и возвращает его ссылку.

**Параметры:**
- `Менеджер` — Произвольный — Менеджер справочника/ПВХ и тд.
- `Реквизиты` — Структура, Неопределено — Значения реквизитов элемента
- `ПараметрыЗаписи` — см. ЮТОбщий.ПараметрыЗаписи

**Возвращает:**
- `ДокументСсылка` — Ссылка на созданный объект

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:62`.

## ЮТТестовыеДанные.СоздатьГруппу

`Функция ЮТТестовыеДанные.СоздатьГруппу(Менеджер, Наименование = Неопределено, Реквизиты = Неопределено, Знач ПараметрыЗаписи = Неопределено) Экспорт`

Создает новую группу

**Параметры:**
- `Менеджер` — Произвольный — Менеджер справочника/ПВХ и тд.
- `Наименование` — Строка, Неопределено — Наименование элемента
- `Реквизиты` — Структура, Неопределено — Значения реквизитов элемента
- `ПараметрыЗаписи` — см. ЮТОбщий.ПараметрыЗаписи

**Возвращает:**
- `ЛюбаяСсылка` — Ссылка на созданную группу

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:95`.

## ЮТТестовыеДанные.СлучайноеЧисло

`Функция ЮТТестовыеДанные.СлучайноеЧисло(Минимум = 0, Максимум = Неопределено, ЗнаковПослеЗапятой = 0) Экспорт`

Возвращает случайное число в указанном диапазоне.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Минимум` — Число — Минимальное значение диапазона. По умолчанию равно 0.
- `Максимум` — Число — Максимальное значение диапазона. Если значение не указано, то будет использовано максимальное значение для целых чисел.
- `ЗнаковПослеЗапятой` — Число — Количество знаков после запятой для случайного числа. По умолчанию равно 0.

**Возвращает:**
- `Число` — Случайное число в указанном диапазоне.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:128`.

## ЮТТестовыеДанные.СлучайноеПоложительноеЧисло

`Функция ЮТТестовыеДанные.СлучайноеПоложительноеЧисло(Максимум = Неопределено, ЗнаковПослеЗапятой = 0) Экспорт`

Возвращает случайное положительное число в указанном диапазоне.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Максимум` — Число — Максимальное значение диапазона. Если значение не указано, то будет использовано максимальное значение для целых чисел.
- `ЗнаковПослеЗапятой` — Число — Количество знаков после запятой для случайного числа. По умолчанию равно 0.

**Возвращает:**
- `Число` — Случайное положительное число в указанном диапазоне.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:168`.

## ЮТТестовыеДанные.СлучайноеОтрицательноеЧисло

`Функция ЮТТестовыеДанные.СлучайноеОтрицательноеЧисло(Знач Минимум = Неопределено, ЗнаковПослеЗапятой = 0) Экспорт`

Возвращает случайное отрицательное число в указанном диапазоне.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Минимум` — Число — Минимальное значение диапазона. Если значение не указано, то будет использовано минимальное значение для целых чисел.
- `ЗнаковПослеЗапятой` — Число — Количество знаков после запятой для случайного числа. По умолчанию равно 0.

**Возвращает:**
- `Число` — Случайное отрицательное число в указанном диапазоне.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:191`.

## ЮТТестовыеДанные.СлучайнаяСтрока

`Функция ЮТТестовыеДанные.СлучайнаяСтрока(Знач Длина = 10, Префикс = "", Знач ДопустимыеСимволы = Неопределено) Экспорт`

Возвращает случайную строку указанной длины, состоящую из допустимых символов.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Длина` — Число — Длина возвращаемой строки. По умолчанию равно 10.
- `Префикс` — Строка — Префикс, который будет добавлен к началу возвращаемой строки. По умолчанию равно пустой строке.
- `ДопустимыеСимволы` — Строка — Строка, содержащая допустимые символы для генерации строки. Если значение не указано, то будут использованы все русские и английские буквы в верхнем и нижнем регистре, а также цифры.

**Возвращает:**
- `Строка` — Случайная строка указанной длины, состоящая из допустимых символов.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:220`.

## ЮТТестовыеДанные.СлучайныйИдентификатор

`Функция ЮТТестовыеДанные.СлучайныйИдентификатор(Знач Длина = 10, Знач Префикс = "") Экспорт`

Возвращает случайный идентификатор указанной длины, состоящий из букв и цифр.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Длина` — Число — Длина возвращаемого идентификатора. По умолчанию равно 10.
- `Префикс` — Строка — Префикс, который будет добавлен к началу возвращаемого идентификатора. По умолчанию равно пустой строке.

**Возвращает:**
- `Строка` — Случайный идентификатор указанной длины, состоящий из букв и цифр.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:257`.

## ЮТТестовыеДанные.СлучайнаяДата

`Функция ЮТТестовыеДанные.СлучайнаяДата(Знач Минимум = '00010101', Знач Максимум = '39991231') Экспорт`

Возвращает случайную дату в указанном диапазоне.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Минимум` — Дата — Минимальное значение диапазона. По умолчанию равно 01.01.0001.
- `Максимум` — Дата — Максимальное значение диапазона. По умолчанию равно 31.12.3999.

**Возвращает:**
- `Дата` — Случайная дата в указанном диапазоне.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:289`.

## ЮТТестовыеДанные.СлучайноеВремя

`Функция ЮТТестовыеДанные.СлучайноеВремя() Экспорт`

Возвращает случайное время в формате "Время".

Ограничения:
- Метод недоступен в веб-клиенте.

**Возвращает:**
- `Дата` — Случайное время

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:317`.

## ЮТТестовыеДанные.СлучайнаяДатаВБудущем

`Функция ЮТТестовыеДанные.СлучайнаяДатаВБудущем(Интервал = Неопределено, ТипИнтервала = Неопределено) Экспорт`

Возвращает случайную дату в будущем, начиная с текущей даты и с учетом указанного интервала.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Интервал` — Число — Значение интервала, который будет использован для генерации случайной даты. По умолчанию равно неопределенному значению.
- `ТипИнтервала` — Строка — Тип интервала, который будет использован для генерации случайной даты. Возможные значения * секунда, секунды, секунд * минута, минуты, минут * час, часа, часов * день, дня, дней * неделя, недели, недель * месяц, месяца, месяцев * квартал, квартала, кварталов * год, года, лет

**Возвращает:**
- `Дата` — Случайная дата в будущем, начиная с текущей даты и с учетом указанного интервала.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:358`.

## ЮТТестовыеДанные.СлучайнаяДатаВПрошлом

`Функция ЮТТестовыеДанные.СлучайнаяДатаВПрошлом(Интервал = Неопределено, ТипИнтервала = Неопределено) Экспорт`

Возвращает случайную дату в прошлом, начиная с текущей даты и с учетом указанного интервала.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Интервал` — Число — Значение интервала, который будет использован для генерации случайной даты. По умолчанию равно неопределенному значению.
- `ТипИнтервала` — Строка — Тип интервала, который будет использован для генерации случайной даты. Возможные значения * секунда, секунды, секунд * минута, минуты, минут * час, часа, часов * день, дня, дней * неделя, недели, недель * месяц, месяца, месяцев * квартал, квартала, кварталов * год, года, лет

**Возвращает:**
- `Дата` — Случайная дата в прошлом, начиная с текущей даты и с учетом указанного интервала.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:398`.

## ЮТТестовыеДанные.СлучайнаяДатаПосле

`Функция ЮТТестовыеДанные.СлучайнаяДатаПосле(Дата, Интервал = Неопределено, ТипИнтервала = Неопределено) Экспорт`

Возвращает случайную дату после указанной даты и с учетом указанного интервала.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Дата` — Дата — Начальная дата для генерации случайной даты.
- `Интервал` — Число — Значение интервала, который будет использован для генерации случайной даты. По умолчанию равно неопределенному значению.
- `ТипИнтервала` — Строка — Тип интервала, который будет использован для генерации случайной даты. Возможные значения * секунда, секунды, секунд * минута, минуты, минут * час, часа, часов * день, дня, дней * неделя, недели, недель * месяц, месяца, месяцев * квартал, квартала, кварталов * год, года, лет

**Возвращает:**
- `Дата` — Случайная дата после указанной даты и с учетом указанного интервала.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:439`.

## ЮТТестовыеДанные.СлучайнаяДатаДо

`Функция ЮТТестовыеДанные.СлучайнаяДатаДо(Дата, Интервал = Неопределено, ТипИнтервала = Неопределено) Экспорт`

Возвращает случайную дату до указанной даты и с учетом указанного интервала.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Дата` — Дата — Конечная дата для генерации случайной даты.
- `Интервал` — Число — Значение интервала, который будет использован для генерации случайной даты. По умолчанию равно неопределенному значению.
- `ТипИнтервала` — Строка — Тип интервала, который будет использован для генерации случайной даты. Возможные значения * секунда, секунды, секунд * минута, минуты, минут * час, часа, часов * день, дня, дней * неделя, недели, недель * месяц, месяца, месяцев * квартал, квартала, кварталов * год, года, лет

**Возвращает:**
- `Дата` — Случайная дата до указанной даты и с учетом указанного интервала.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:479`.

## ЮТТестовыеДанные.СлучайныйIPАдрес

`Функция ЮТТестовыеДанные.СлучайныйIPАдрес() Экспорт`

Возвращает случайный IP-адрес.

Ограничения:
- Метод недоступен в веб-клиенте.

**Возвращает:**
- `Строка` — Случайный IP-адрес в формате "XXX.XXX.XXX.XXX".

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:503`.

## ЮТТестовыеДанные.СлучайноеЗначениеИзСписка

`Функция ЮТТестовыеДанные.СлучайноеЗначениеИзСписка(Список) Экспорт`

Возвращает случайное значение из указанного списка.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Список` — Массив из Произвольный — Список значений, из которых будет выбрано случайное значение.

**Возвращает:**
- `Произвольный` — Случайное значение из указанного списка.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:529`.

## ЮТТестовыеДанные.СлучайноеБулево

`Функция ЮТТестовыеДанные.СлучайноеБулево() Экспорт`

Возвращает случайное булево значение (Истина или Ложь).

Ограничения:
- Метод недоступен в веб-клиенте.

**Возвращает:**
- `Булево` — Случайное булево значение (Истина или Ложь).

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:547`.

## ЮТТестовыеДанные.СлучайноеЗначениеПеречисления

`Функция ЮТТестовыеДанные.СлучайноеЗначениеПеречисления(Перечисление) Экспорт`

Возвращает случайное значение перечисления.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Перечисление` — ПеречислениеМенеджер — Менеджер перечисления. - Строка - мя объекта метаданных перечисления.

**Возвращает:**
- `ПеречислениеСсылка` — Случайное значение перечисления.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:568`.

## ЮТТестовыеДанные.СлучайноеПредопределенноеЗначение

`Функция ЮТТестовыеДанные.СлучайноеПредопределенноеЗначение(Менеджер, Отбор = Неопределено) Экспорт`

Возвращает случайное предопределенное значения объекта конфигурации.

**Параметры:**
- `Менеджер` — Строка — Имя менеджера. Примеры: "Справочники.ВидыЦен", "Справочник.ВидыЦен" - Произвольный - Менеджер объекта метаданных. Примеры: Справочники.ВидыЦен
- `Отбор` — Структура, Соответствие из Произвольный — Отбора поиска предопределенных значений (сравнение на равенство)

**Возвращает:**
- `СправочникСсылка` — Случайное предопределенное значение объекта конфигурации.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:594`.

## ЮТТестовыеДанные.СлучайныйНомерТелефона

`Функция ЮТТестовыеДанные.СлучайныйНомерТелефона(КодСтраны = "7") Экспорт`

Возвращает случайный номер телефона в формате "+КодСтраны(XXX)XXX-XX-XX".

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `КодСтраны` — Строка — Код страны по умолчанию "7".

**Возвращает:**
- `Строка` — Случайный номер телефона в формате "+КодСтраны(XXX)XXX-XX-XX".

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:618`.

## ЮТТестовыеДанные.УникальнаяСтрока

`Функция ЮТТестовыеДанные.УникальнаяСтрока(Префикс = "") Экспорт`

Возвращает уникальную строку на основе префикса и уникального идентификатора.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Префикс` — Строка — Префикс уникальной строки. По умолчанию равно пустой строке.

**Возвращает:**
- `Строка` — Уникальная строка на основе префикса и уникального идентификатора.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:649`.

## ЮТТестовыеДанные.НовыйФайл

`Функция ЮТТестовыеДанные.НовыйФайл(Содержимое = Неопределено, ТолькоЧтение = Ложь, Расширение = Неопределено) Экспорт`

Возвращает путь к созданному временному файлу с указанным содержимым и расширением.

**Параметры:**
- `Содержимое` — Неопределено, Строка — Содержимое файла.
- `ТолькоЧтение` — Булево — Если значение равно Истина, то файл будет создан в режиме только для чтения. По умолчанию равно Ложь.
- `Расширение` — Неопределено, Строка — Расширение файла.

**Возвращает:**
- `Строка` — Путь к новому временному файлу с указанным содержимым и расширением.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:669`.

## ЮТТестовыеДанные.НовоеИмяВременногоФайла

`Функция ЮТТестовыеДанные.НовоеИмяВременногоФайла(Расширение = Неопределено) Экспорт`

Возвращает уникальное имя временного файла с указанным расширением.

**Параметры:**
- `Расширение` — Неопределено, Строка — Расширение файла.

**Возвращает:**
- `Строка` — Уникальное имя временного файла с указанным расширением.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:701`.

## ЮТТестовыеДанные.ТаблицаMarkDown

`Функция ЮТТестовыеДанные.ТаблицаMarkDown(Строки) Экспорт`

Возвращает массив структур, содержащих данные из таблицы Markdown.

**Параметры:**
- `Строки` — Строка — Строка в формате Markdown, содержащая таблицу.

**Возвращает:**
- Массив из Структура - Массив структур, содержащих данные из таблицы Markdown.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:721`.

## ЮТТестовыеДанные.СтруктураMarkDown

`Функция ЮТТестовыеДанные.СтруктураMarkDown(Ключ, Строки) Экспорт`

Возвращает структуру, содержащую данные из таблицы Markdown, где ключом является значение указанного столбца.

Ограничения:
- Метод недоступен в веб-клиенте.

**Параметры:**
- `Ключ` — Строка — Название столбца, значение которого будет использовано в качестве ключа в возвращаемой структуре.
- `Строки` — Строка — Строка в формате Markdown, содержащая таблицу.

**Возвращает:**
- `Структура` — Структура, содержащая данные из таблицы Markdown, где ключом является значение указанного столбца.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:801`.

## ЮТТестовыеДанные.ВариантыПараметров

`Функция ЮТТестовыеДанные.ВариантыПараметров(ЗначенияПоУмолчанию, ЗначенияПараметров) Экспорт`

Формирует массив различных комбинаций параметров

Предназначено для формирования таблицы возможных значений параметров для краш теста метода.

**Параметры:**
- `ЗначенияПоУмолчанию` — Структура — Значения параметров по умолчанию.
- `ЗначенияПараметров` — Структура — Массивы значений для каждого параметра.

**Возвращает:**
- Массив из Структура - Варианты параметров.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:827`.

## ЮТТестовыеДанные.КонструкторОбъекта

`Функция ЮТТестовыеДанные.КонструкторОбъекта(Менеджер) Экспорт`

Возвращает конструктор создания тестовых данных для указанного объекта метаданных.

Конструктор имеет ряд особенностей:

* Создание объекта происходит при вызове методов `Записать` и `Провести`, а создание реквизитов происходит во время вызова методов установки.
* При использовании на клиенте все значения должны быть сериализуемыми.

**Параметры:**
- `Менеджер` — Строка — Имя менеджера объекта метаданных. Примеры: "Справочники.Товары", "Документы.ПриходТоваров". - Произвольный - Менеджер объекта метаданных. Примеры: Справочники.Товары, Документы.ПриходТоваров

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор создания тестовых данных для указанного объекта метаданных.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:866`.

## ЮТТестовыеДанные.КонструкторДвижений

`Функция ЮТТестовыеДанные.КонструкторДвижений(Документ, ИмяРегистра) Экспорт`

Возвращает конструктор создания движений для указанного документа и регистра.

Конструктор имеет ряд особенностей:

* Создание движений происходит при вызове метода `Записать`, а заполнение реквизитов движений осуществляется через методы установки.
* При использовании на клиенте все значения должны быть сериализуемыми.

**Параметры:**
- `Документ` — ДокументОбъект, ДокументСсылка — Документ, для которого создаются движения.
- `ИмяРегистра` — Строка — Имя регистра, движения которого необходимо создать.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторТестовыхДанных - Конструктор для создания движений по указанному документу и регистру.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:893`.

## ЮТТестовыеДанные.КонструкторОбъектаXDTO

`Функция ЮТТестовыеДанные.КонструкторОбъектаXDTO(ИмяТипа, ПространствоИмен, Фабрика = Неопределено) Экспорт`

Возвращает конструктор создания объекта XDTO.

**Параметры:**
- `ИмяТипа` — Строка — Имя типа объекта XDTO.
- `ПространствоИмен` — Строка — Пространство имен типа объекта XDTO.
- `Фабрика` — ФабрикаXDTO — Фабрика, используемая для создания объектов XDTO. По умолчанию используется стандартная фабрика XDTO.

**Возвращает:**
- ОбработкаОбъект.ЮТКонструкторОбъектаXDTO - Конструктор объекта XDTO.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:922`.

## ЮТТестовыеДанные.Удалить

`Процедура ЮТТестовыеДанные.Удалить(Ссылки, Привилегированно = Ложь) Экспорт`

Удаляет переданные объекта

**Параметры:**
- `Ссылки` — Массив из ЛюбаяСсылка — Массив ссылок на объекты, которые необходимо удалить.
- `Привилегированно` — Булево — Выполнить удаление в привилегированном режиме (без учета прав на объекты)

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:937`.

## ЮТТестовыеДанные.ДобавитьКУдалению

`Процедура ЮТТестовыеДанные.ДобавитьКУдалению(Ссылки) Экспорт`

Добавляет к удалению переданные объекты.

**Параметры:**
- `Ссылки` — Массив из ЛюбаяСсылка — Массив ссылок на объекты, которые необходимо добавить к удалению

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:955`.

## ЮТТестовыеДанные.Подражатель

`Функция ЮТТестовыеДанные.Подражатель() Экспорт`

Возвращает объект подражателя для формирования осмысленных тестовых данных

**Возвращает:**
- `ОбщийМодуль` — см. Подражатель

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:975`.

## ЮТТестовыеДанные.ЗагрузитьИзМакета

`Функция ЮТТестовыеДанные.ЗагрузитьИзМакета(Макет, ОписанияТипов, КэшЗначений = Неопределено, ЗаменяемыеЗначения = Неопределено, ПараметрыСозданияОбъектов = Неопределено) Экспорт`

Возвращает таблицу значений из табличного документа

**Параметры:**
- `Макет` — ТабличныйДокумент, ТекстовыйДокумент — Исходный табличный документ - Строка - Имя макета  или данные в формате Markdown. Возможные значения имети макета: + ОбщийМакет.Макет - Общий макет "Макет" + ОбщийМакет.Макет.Область1 - Область "Область1" общего макета "Макет" + Обработка.ТестовыеДанные.МакетДанных - Макет обработки (также можно указать имя области)
- `ОписанияТипов` — Соответствие из ОписаниеТипов — Соответствие имен колонок таблицы к типам значений
- `КэшЗначений` — Соответствие из Произвольный — Соответствие для хранения создаваемых значений
- `ЗаменяемыеЗначения` — Соответствие из Произвольный — Значения, использующиеся для замены
- `ПараметрыСозданияОбъектов` — см. ЮТФабрика.ПараметрыСозданияОбъектов

**Возвращает:**
- - ТаблицаЗначений - Для сервера, данные загруженные из макета - Массив из Структура - Для клиента, данные загруженные из макета

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:996`.

## ЮТТестовыеДанные.ПараметрыЗагрузкиМакетов

`Функция ЮТТестовыеДанные.ПараметрыЗагрузкиМакетов() Экспорт`

Параметры загрузки макетов.

**Возвращает:**
- `Структура` — Параметры загрузки макетов: * КэшЗначений - Соответствие из Произвольный - Соответствие для хранения создаваемых значений * ЗаменяемыеЗначения - Соответствие из Произвольный - Значения, использующиеся для замены * ПараметрыСозданияОбъектов - см. ЮТФабрика.ПараметрыСозданияОбъектов * ПараметрыЗаписи - см. ЮТОбщий.ПараметрыЗаписи

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1046`.

## ЮТТестовыеДанные.ЗагрузитьДвиженияИзМакета

`Процедура ЮТТестовыеДанные.ЗагрузитьДвиженияИзМакета(Документ, ИмяРегистра, Макет, ОбщиеРеквизиты = Неопределено, ПараметрыЗагрузкиМакетов = Неопределено) Экспорт`

Загрузить данные из макета в движения документа по ссылке.

**Параметры:**
- `Документ` — ДокументСсылка
- `ИмяРегистра` — Строка — Короткое или полное имя регистра движений
- `Макет` — ТабличныйДокумент, ТекстовыйДокумент — Исходный табличный документ - Строка - Имя макета  или данные в формате Markdown. Возможные значения имети макета: + ОбщийМакет.Макет - Общий макет "Макет" + ОбщийМакет.Макет.Область1 - Область "Область1" общего макета "Макет" + Обработка.ТестовыеДанные.МакетДанных - Макет обработки (также можно указать имя области)
- `ОбщиеРеквизиты` — Структура — Общие реквизиты записей движений
- `ПараметрыЗагрузкиМакетов` — см. ПараметрыЗагрузкиМакетов

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1070`.

## ЮТТестовыеДанные.ЗагрузитьИзМакетаВТаблицу

`Процедура ЮТТестовыеДанные.ЗагрузитьИзМакетаВТаблицу(ИмяТаблицы, Макет, ОбщиеРеквизиты = Неопределено, ПараметрыЗагрузкиМакетов = Неопределено) Экспорт`

Загружает данные из макета в информационную базу

**Параметры:**
- `ИмяТаблицы` — Строка — Имя объекта метаданных, к который необходимо ипортировать данные
- `Макет` — ТабличныйДокумент, ТекстовыйДокумент — Исходный табличный документ - Строка - Имя макета  или данные в формате Markdown. Возможные значения имети макета: + ОбщийМакет.Макет - Общий макет "Макет" + ОбщийМакет.Макет.Область1 - Область "Область1" общего макета "Макет" + Обработка.ТестовыеДанные.МакетДанных - Макет обработки (также можно указать имя области)
- `ОбщиеРеквизиты` — Структура — Общие реквизиты записей движений
- `ПараметрыЗагрузкиМакетов` — см. ПараметрыЗагрузкиМакетов

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1103`.

## ЮТТестовыеДанные.HTTPСервисЗапрос

`Функция ЮТТестовыеДанные.HTTPСервисЗапрос() Экспорт`

Возвращает мок для `HTTPСервисЗапрос`.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСервисЗапрос - Мок

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1127`.

## ЮТТестовыеДанные.HTTPСоединение

`Функция ЮТТестовыеДанные.HTTPСоединение() Экспорт`

Возвращает мок для `HTTPСоединение`.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPСоединение - Мок

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1141`.

## ЮТТестовыеДанные.HTTPОтвет

`Функция ЮТТестовыеДанные.HTTPОтвет() Экспорт`

Возвращает мок для `HTTPОтвет`.

**Возвращает:**
- ОбработкаОбъект.ЮТHTTPОтвет - Мок

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1151`.

## ЮТТестовыеДанные.ADORecordSet

`Функция ЮТТестовыеДанные.ADORecordSet(Колонки, Описание = Неопределено) Экспорт`

Возвращает мок для ADO.RecordSet.

**Параметры:**
- `Колонки` — Строка — Имена колонок набора данных, разделенные запятой
- `Описание` — Строка — Описание, полезно для отладки и проверки

**Возвращает:**
- ОбработкаОбъект.ЮТRecordSet - Мок ADO.RecordSet

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1167`.

## ЮТТестовыеДанные.УстановитьФоновуюБлокировку

`Процедура ЮТТестовыеДанные.УстановитьФоновуюБлокировку(Блокировка, Длительность = 30) Экспорт`

Установить фоновую блокировку.

**Параметры:**
- `Блокировка` — БлокировкаДанных
- `Длительность` — Число — Длительность блокировки в секундах

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1182`.

## ЮТТестовыеДанные.УстановитьЗначениеРеквизита

`Процедура ЮТТестовыеДанные.УстановитьЗначениеРеквизита(Ссылка, ИмяРеквизита, ЗначениеРеквизита, ПараметрыЗаписи = Неопределено) Экспорт`

Устанавливает значение реквизита ссылки

**Параметры:**
- `Ссылка` — ЛюбаяСсылка
- `ИмяРеквизита` — Строка
- `ЗначениеРеквизита` — Произвольный
- `ПараметрыЗаписи` — см. ЮТОбщий.ПараметрыЗаписи

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1201`.

## ЮТТестовыеДанные.УстановитьЗначенияРеквизитов

`Процедура ЮТТестовыеДанные.УстановитьЗначенияРеквизитов(Ссылка, ЗначенияРеквизитов, ПараметрыЗаписи = Неопределено) Экспорт`

Устанавливает значения реквизитов ссылки.

**Параметры:**
- `Ссылка` — ЛюбаяСсылка —  Ссылка
- `ЗначенияРеквизитов` — Структура, Соответствие из Произвольный —  Значения реквизитов
- `ПараметрыЗаписи` — см. ЮТОбщий.ПараметрыЗаписи

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1215`.

## ЮТТестовыеДанные.Фикция

`Функция ЮТТестовыеДанные.Фикция(ОписаниеТипа, РеквизитыЗаполнения = Неопределено) Экспорт`

Генерирует новое значение указанного типа.
Если `ОписаниеТипа` содержит несколько типов, то выбирается случайный из них.

**Параметры:**
- `ОписаниеТипа` — ОписаниеТипов, Тип — Тип значения генерируемого значения
- `РеквизитыЗаполнения` — Структура — Значения реквизитов заполнения создаваемого объекта базы - Неопределено

**Возвращает:**
- `Произвольный` — Сгенерированное значение указанного типа

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1234`.

## ЮТТестовыеДанные.СообщениеСервисаИнтеграции

`Функция ЮТТестовыеДанные.СообщениеСервисаИнтеграции() Экспорт`

Возвращает мок для `СообщениеСервисаИнтеграции`.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Мок

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1245`.

## ЮТТестовыеДанные.СсылкаПоРеквизиту

`Функция ЮТТестовыеДанные.СсылкаПоРеквизиту(МетаданныеОбъекта, ИмяРеквизита, ЗначениеРеквизита) Экспорт`

Ищет и возвращает ссылку на объект по значению его реквизита.

**Параметры:**
- `МетаданныеОбъекта` — ОбъектМетаданных — Явное указание объекта метаданных. - Произвольный - менеджер, например, Справочники.Номенклатура. - Строка -  Полное имя объекта метаданных или менеджера, например, "Справочник.Номенклатура". - Тип, ОписаниеТипов - Тип искомого объекта
- `ИмяРеквизита` — Строка       — Имя реквизита, по которому выполняется поиск.
- `ЗначениеРеквизита` — Произвольный — Значение реквизита для поиска.

**Возвращает:**
- ЛюбаяСсылка, Неопределено - Ссылка на найденный объект или Неопределено, если объект не найден.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1274`.

## ЮТТестовыеДанные.СлучайнаяСсылка

`Функция ЮТТестовыеДанные.СлучайнаяСсылка(МетаданныеОбъекта, Отборы = Неопределено) Экспорт`

Возвращает случайную ссылку на объект из информационной базы,
удовлетворяющую заданным условиям отбора.

**Параметры:**
- `МетаданныеОбъекта` — ОбъектМетаданных — Явное указание объекта метаданных. - Произвольный - менеджер, например, Справочники.Номенклатура. - Строка -  Полное имя объекта метаданных или менеджера, например, "Справочник.Номенклатура". - Тип, ОписаниеТипов - Тип искомого объекта
- `Отборы` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Структура, Соответствие - Простой отбор по полям "ключ-значение". - Неопределено - Отбор не используется.

**Возвращает:**
- ЛюбаяСсылка, Неопределено - Случайная ссылка на найденный объект или Неопределено, если ничего не найдено.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1310`.

## ЮТТестовыеДанные.Ссылка

`Функция ЮТТестовыеДанные.Ссылка(МетаданныеОбъекта, Значение, Знач Отборы = Неопределено) Экспорт`

Ищет и возвращает ссылку на объект по значению,
которое проверяется по стандартным реквизитам (Код, Наименование) и основному представлению.

**Параметры:**
- `МетаданныеОбъекта` — ОбъектМетаданных — Явное указание объекта метаданных. - Произвольный - менеджер, например, Справочники.Номенклатура. - Строка -  Полное имя объекта метаданных или менеджера, например, "Справочник.Номенклатура". - Тип, ОписаниеТипов - Тип искомого объекта
- `Значение` — Строка, Число, УникальныйИдентификатор — Значение для поиска.
- `Отборы` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Структура, Соответствие - Простой отбор по полям "ключ-значение". - Неопределено - Отбор не используется.

**Возвращает:**
- ЛюбаяСсылка, Неопределено - Ссылка на найденный объект или Неопределено, если объект не найден.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТТестовыеДанные/Module.bsl:1352`.
