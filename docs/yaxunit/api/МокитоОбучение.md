---
title: API МокитоОбучение
description: "Публичные экспортные методы модуля МокитоОбучение из ядра YaXUnit."
tags: [yaxunit, api, МокитоОбучение]
publish_publicly: false
---

# API МокитоОбучение

Компактный справочник по 8 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## МокитоОбучение.Обучение

`Функция МокитоОбучение.Обучение(Объект, СброситьСтарыеНастройки = Истина) Экспорт`

Начинает обучение мокито для регистрации правил подмены вызовов методов.

**Параметры:**
- `Объект` — Произвольный — Обучаемый объект, с методами которого хотим работать.
- `СброситьСтарыеНастройки` — Булево — Необходимо удалить старые настройки по объекту. + `Истина` - все предыдущие настройки мокирования объекта будут забыты. + `Ложь` - будет выполнено дообучение объекта.

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:36`.

## МокитоОбучение.Наблюдать

`Функция МокитоОбучение.Наблюдать(ИмяМетода, ПараметрыВызова = Неопределено) Экспорт`

Включает наблюдение за вызовами метода, при необходимости можно настроить фильтр по параметрам вызова.

**Параметры:**
- `ИмяМетода` — Строка — Имя метода обучаемого объекта, см. Обучение. - Произвольный - Вызов метода обучаемого объекта.
- `ПараметрыВызова` — Массив из Произвольный — Параметры вызова метода обучаемого объекта. Варианты вызова: Мокито.Обучение(Произвольный).Когда(Строка, Массив); // Вызов через указание имени метода и параметров Мокито.Обучение(Произвольный).Когда(Произвольный); // Вызов через явное выполнение наблюдаемого метода

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:60`.

## МокитоОбучение.Когда

`Функция МокитоОбучение.Когда(ИмяМетода, ПараметрыВызова = Неопределено) Экспорт`

Задает условие "подмены" поведения метода.

**Параметры:**
- `ИмяМетода` — Строка — Имя метода обучаемого объекта. - Произвольный - Вызов метода обучаемого объекта.
- `ПараметрыВызова` — Массив из Произвольный — Параметры вызова метода обучаемого объекта. Варианты вызова: Мокито.Обучение(Произвольный).Когда(Строка, Массив); // Вызов через указание имени метода и параметров Мокито.Обучение(Произвольный).Когда(Произвольный); // Вызов через явное выполнение наблюдаемого метода

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение.

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:83`.

## МокитоОбучение.Вернуть

`Функция МокитоОбучение.Вернуть(Результат) Экспорт`

Указывает, что при соблюдении условий (см. Когда) метод должен вернуть указанный результат.

При этом сам метод не исполняется.

**Параметры:**
- `Результат` — Произвольный — Результат, который должен вернуть метод.

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:98`.

## МокитоОбучение.ВыброситьИсключение

`Функция МокитоОбучение.ВыброситьИсключение(ТекстИсключения) Экспорт`

Указывает, что при соблюдении условий (см. Когда) метод должен выбросить исключение.

При этом сам метод не исполняется.

**Параметры:**
- `ТекстИсключения` — Строка

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:115`.

## МокитоОбучение.Пропустить

`Функция МокитоОбучение.Пропустить() Экспорт`

Указывает, что при соблюдении условий (см. Когда) метод не должен выполняться, его вызов пропускается.

Если это функция, то будет возвращено `Неопределено`.

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:129`.

## МокитоОбучение.ВыполнитьМетод

`Функция МокитоОбучение.ВыполнитьМетод() Экспорт`

Указывает, что при соблюдении условий (см. Когда) метод должен выполняться.
Используется для случаев, когда необходимо задать исключения для другого правила на этом методе.

**Возвращает:**
- `ОбщийМодуль` — см. МокитоОбучение

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:149`.

## МокитоОбучение.Прогон

`Процедура МокитоОбучение.Прогон() Экспорт`

Переводит мокито в режим прогона тестов.

Вызов этого метода обязателен перед выполнением тестового прогона метода.

Источник ядра: `exts/yaxunit/src/CommonModules/МокитоОбучение/Module.bsl:160`.
