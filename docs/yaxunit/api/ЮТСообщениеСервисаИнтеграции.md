---
title: API ЮТСообщениеСервисаИнтеграции
description: "Публичные экспортные методы модуля ЮТСообщениеСервисаИнтеграции из ядра YaXUnit."
tags: [yaxunit, api, ЮТСообщениеСервисаИнтеграции]
publish_publicly: false
---

# API ЮТСообщениеСервисаИнтеграции

Компактный справочник по 12 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТСообщениеСервисаИнтеграции.ПолучитьТелоКакПоток

`Функция ЮТСообщениеСервисаИнтеграции.ПолучитьТелоКакПоток() Экспорт`

Возвращает тело как поток.

**Возвращает:**
- `Поток` — Тело сообщения в виде потока данных.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:59`.

## ЮТСообщениеСервисаИнтеграции.УстановитьТелоКакДвоичныеДанные

`Функция ЮТСообщениеСервисаИнтеграции.УстановитьТелоКакДвоичныеДанные(Данные) Экспорт`

Устанавливает тело как двоичные данные.
При получении тела в другом формате происходит конвертация.

**Параметры:**
- `Данные` — ДвоичныеДанные — Двоичные данные для установки в качестве тела сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:79`.

## ЮТСообщениеСервисаИнтеграции.УстановитьТелоКакСтроку

`Функция ЮТСообщениеСервисаИнтеграции.УстановитьТелоКакСтроку(Строка) Экспорт`

Устанавливает тело как строку.
При получении тела в другом формате происходит конвертация.

**Параметры:**
- `Строка` — Строка — Строковые данные для установки в качестве тела сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:99`.

## ЮТСообщениеСервисаИнтеграции.УстановитьТелоКакСтрокуJSON

`Функция ЮТСообщениеСервисаИнтеграции.УстановитьТелоКакСтрокуJSON(Данные) Экспорт`

Устанавливает тело как строку JSON.
Сериализует переданный объект в строку JSON и сохраняет в тело.
При получении тела в другом формате происходит конвертация.

**Параметры:**
- `Данные` — Произвольный — Данные для сериализации в JSON и установки в качестве тела сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:121`.

## ЮТСообщениеСервисаИнтеграции.ДобавитьПараметр

`Функция ЮТСообщениеСервисаИнтеграции.ДобавитьПараметр(ИмяПараметра, Значение) Экспорт`

Добавляет параметр.

**Параметры:**
- `ИмяПараметра` — Строка — Имя добавляемого параметра.
- `Значение` — Строка — Значение добавляемого параметра.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:146`.

## ЮТСообщениеСервисаИнтеграции.ДатаОтправки

`Функция ЮТСообщениеСервисаИнтеграции.ДатаОтправки(Значение) Экспорт`

Устанавливает дату отправки.

**Параметры:**
- `Значение` — Дата — Устанавливаемая дата отправки сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:165`.

## ЮТСообщениеСервисаИнтеграции.ДатаУстаревания

`Функция ЮТСообщениеСервисаИнтеграции.ДатаУстаревания(Значение) Экспорт`

Устанавливает дату устаревания.

**Параметры:**
- `Значение` — Дата — Устанавливаемая дата устаревания сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:184`.

## ЮТСообщениеСервисаИнтеграции.Идентификатор

`Функция ЮТСообщениеСервисаИнтеграции.Идентификатор(Значение) Экспорт`

Устанавливает идентификатор сообщения.

**Параметры:**
- `Значение` — УникальныйИдентификатор — Устанавливаемый идентификатор сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:204`.

## ЮТСообщениеСервисаИнтеграции.ИдентификаторСообщенияЗапроса

`Функция ЮТСообщениеСервисаИнтеграции.ИдентификаторСообщенияЗапроса(Значение) Экспорт`

Устанавливает идентификатор сообщения запроса.
Используется для связывания ответного сообщения с исходным запросом.

**Параметры:**
- `Значение` — УникальныйИдентификатор — Устанавливаемый идентификатор сообщения запроса.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:225`.

## ЮТСообщениеСервисаИнтеграции.КодОтправителя

`Функция ЮТСообщениеСервисаИнтеграции.КодОтправителя(Значение) Экспорт`

Устанавливает код отправителя.

**Параметры:**
- `Значение` — Строка — Устанавливаемый код отправителя сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:244`.

## ЮТСообщениеСервисаИнтеграции.КодПолучателя

`Функция ЮТСообщениеСервисаИнтеграции.КодПолучателя(Значение) Экспорт`

Устанавливает код получателя.

**Параметры:**
- `Значение` — Строка — Устанавливаемый код получателя сообщения.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:263`.

## ЮТСообщениеСервисаИнтеграции.РазмерТела

`Функция ЮТСообщениеСервисаИнтеграции.РазмерТела(Значение) Экспорт`

Устанавливает размер тела.

**Параметры:**
- `Значение` — Число — Устанавливаемый размер тела сообщения в байтах.

**Возвращает:**
- ОбработкаОбъект.ЮТСообщениеСервисаИнтеграции - Текущий объект для цепочки вызовов.

Источник ядра: `exts/yaxunit/src/DataProcessors/ЮТСообщениеСервисаИнтеграции/ObjectModule.bsl:284`.
