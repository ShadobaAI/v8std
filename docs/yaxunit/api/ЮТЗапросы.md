---
title: API ЮТЗапросы
description: "Публичные экспортные методы модуля ЮТЗапросы из ядра YaXUnit."
tags: [yaxunit, api, ЮТЗапросы]
publish_publicly: false
---

# API ЮТЗапросы

Компактный справочник по 14 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТЗапросы.ЗначенияРеквизитов

`Функция ЮТЗапросы.ЗначенияРеквизитов(Ссылка, ИменаРеквизитов) Экспорт`

Возвращает значения реквизитов ссылки

**Параметры:**
- `Ссылка` — ЛюбаяСсылка
- `ИменаРеквизитов` — Строка — Имена получаемых реквизитов, разделенные запятой. Важно, нельзя указывать реквизиты через точку.

**Возвращает:**
- Структура Из Произвольный - Значения реквизитов ссылки

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:35`.

## ЮТЗапросы.ЗначениеРеквизита

`Функция ЮТЗапросы.ЗначениеРеквизита(Ссылка, ИмяРеквизита) Экспорт`

Возвращает значение реквизита ссылки

**Параметры:**
- `Ссылка` — ЛюбаяСсылка
- `ИмяРеквизита` — Строка — Имя получаемого реквизита, можно указать путь к вложенному реквизиту через точку

**Возвращает:**
- `Произвольный` — Значение реквизита ссылки

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:54`.

## ЮТЗапросы.Запись

`Функция ЮТЗапросы.Запись(ИмяТаблицы, Предикат) Экспорт`

Возвращает первую запись таблицы соответствующую условиям

**Параметры:**
- `ИмяТаблицы` — Строка — Имя таблицы базы
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Неопределено - Проверит, что таблица не пустая

**Возвращает:**
- Структура, Неопределено - Содержит все данные записи, включая табличный части

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:74`.

## ЮТЗапросы.СсылкаПоУсловиям

`Функция ЮТЗапросы.СсылкаПоУсловиям(ИмяТаблицы, Предикат) Экспорт`

Возвращает первую ссылку на объект таблицы соответствующую условиям

**Параметры:**
- `ИмяТаблицы` — Строка — Имя таблицы базы
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Неопределено - Проверит, что таблица не пустая

**Возвращает:**
- `ЛюбаяСсылка` — Ссылка на объект

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:96`.

## ЮТЗапросы.Записи

`Функция ЮТЗапросы.Записи(ИмяТаблицы, Предикат) Экспорт`

Возвращает записи таблицы соответствующую условиям

**Параметры:**
- `ИмяТаблицы` — Строка — Имя таблицы базы
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Неопределено - Проверит, что таблица не пустая

**Возвращает:**
- Массив из Структура - Найденные записи, включая табличный части

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:124`.

## ЮТЗапросы.ЗначенияРеквизитовЗаписи

`Функция ЮТЗапросы.ЗначенияРеквизитовЗаписи(ИмяТаблицы, Предикат, ИменаРеквизитов) Экспорт`

Возвращает значения реквизитов первой записи таблицы, соответствующей условиям

**Параметры:**
- `ИмяТаблицы` — Строка
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат
- `ИменаРеквизитов` — Строка — Имена получаемых реквизитов

**Возвращает:**
- `Произвольный` — Значение реквизита записи

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:147`.

## ЮТЗапросы.ЗначениеРеквизитаЗаписи

`Функция ЮТЗапросы.ЗначениеРеквизитаЗаписи(ИмяТаблицы, Предикат, ИмяРеквизита) Экспорт`

Возвращает значение реквизита первой записи таблицы, соответствующей условиям

**Параметры:**
- `ИмяТаблицы` — Строка
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат
- `ИмяРеквизита` — Строка — Имя получаемого реквизита

**Возвращает:**
- Структура Из Произвольный - Значения реквизитов записи

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:170`.

## ЮТЗапросы.ТаблицаСодержитЗаписи

`Функция ЮТЗапросы.ТаблицаСодержитЗаписи(ИмяТаблицы, Предикат = Неопределено) Экспорт`

Вернет признак содержит ли таблица записи удовлетворяющие переданным условиям

**Параметры:**
- `ИмяТаблицы` — Строка — Имя таблицы базы
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Неопределено - Проверит, что таблица не пустая

**Возвращает:**
- `Булево` — Таблица содержит записи

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:192`.

## ЮТЗапросы.КоличествоЗаписей

`Функция ЮТЗапросы.КоличествоЗаписей(ИмяТаблицы, Предикат = Неопределено) Экспорт`

Возвращает количество записей таблицы удовлетворяющих переданным условиям

**Параметры:**
- `ИмяТаблицы` — Строка — Имя таблицы базы
- `Предикат` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Неопределено - Проверит, что таблица не пустая

**Возвращает:**
- `Число` — Количество записей

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:211`.

## ЮТЗапросы.РезультатЗапроса

`Функция ЮТЗапросы.РезультатЗапроса(ОписаниеЗапроса) Экспорт`

Возвращает результат выполнения простого запроса.

**Параметры:**
- `ОписаниеЗапроса` — см. ОписаниеЗапроса

**Возвращает:**
- - ТаблицаЗначений - Результат запроса для сервера - Массив из Структура - Результат запроса для клиента

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:230`.

## ЮТЗапросы.РезультатПустой

`Функция ЮТЗапросы.РезультатПустой(ОписаниеЗапроса) Экспорт`

Определяет, есть ли в результате записи

**Параметры:**
- `ОписаниеЗапроса` — см. ОписаниеЗапроса

**Возвращает:**
- `Булево` — Результат пустой

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:247`.

## ЮТЗапросы.ОписаниеЗапроса

`Функция ЮТЗапросы.ОписаниеЗапроса() Экспорт`

Описание простого запроса

**Возвращает:**
- `Структура` — Описание запроса: * ИмяТаблицы - Строка - Имя таблицы, из которой нужно получить данные * ВыбираемыеПоля - Массив из Строка - Выражения выбираемых полей * КоличествоЗаписей - Число, Неопределено - Ограничение количества выбираемых записей * Условия - Массив из Строка - Коллекция выражений условий, которые будут объединены через `И` * ЗначенияПараметров - Структура - Набор параметров запроса * Порядок - Массив из Строка - Поля сортировки

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:281`.

## ЮТЗапросы.ДвиженияДокумента

`Функция ЮТЗапросы.ДвиженияДокумента(Документ, Знач ИмяРегистра) Экспорт`

Функция получает список движений по указанному документу и регистру.
Если документ не делает движений по указанному регистру, функция вызывает исключение.
Результатом работы функции является список записей регистра, отсортированных по номеру строки.

**Параметры:**
- `Документ` — ДокументСсылка
- `ИмяРегистра` — Строка — Короткое или полное имя регистра движений

**Возвращает:**
- - Массив из Структура - Движения документа для клиента - ТаблицаЗначений - Движения документа для сервера

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:307`.

## ЮТЗапросы.СлучайнаяЗапись

`Функция ЮТЗапросы.СлучайнаяЗапись(МетаданныеОбъекта, Знач Отборы = Неопределено) Экспорт`

Возвращает случайную ссылку на объект из информационной базы,
удовлетворяющую заданным условиям отбора.

**Параметры:**
- `МетаданныеОбъекта` — ОбъектМетаданных — Явное указание объекта метаданных. - Произвольный - менеджер, например, Справочники.Номенклатура. - Строка -  Полное имя объекта метаданных или менеджера, например, "Справочник.Номенклатура". - Тип, ОписаниеТипов - Тип искомого объекта
- `Отборы` — Массив из см. ЮТФабрика.ВыражениеПредиката — Набор условий, см. ЮТПредикаты.Получить - см. ЮТФабрика.ВыражениеПредиката - ОбщийМодуль - Модуль настройки предикатов, см. ЮТест.Предикат - Структура, Соответствие - Простой отбор по полям "ключ-значение". - Неопределено - Отбор не используется.

**Возвращает:**
- `ЛюбаяСсылка` — Случайная ссылка
- `Структура` — Набор ключей для регистра

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТЗапросы/Module.bsl:348`.
