---
title: API ЮТКоллекции
description: "Публичные экспортные методы модуля ЮТКоллекции из ядра YaXUnit."
tags: [yaxunit, api, ЮТКоллекции]
publish_publicly: false
---

# API ЮТКоллекции

Компактный справочник по 22 экспортным методам. Сигнатуры получены из ядра YaXUnit ревизии `23fd2db738dbb26b7b43bdcf0c35fd2c263a3899`.

## ЮТКоллекции.ЗначениеСтруктуры

`Функция ЮТКоллекции.ЗначениеСтруктуры(ИсходнаяСтруктура, ИмяПоля, ЗначениеПоУмолчанию = Неопределено) Экспорт`

Возвращает требуемое поле структуры. В случае отсутствия поля возвращает значение по умолчанию

**Параметры:**
- `ИсходнаяСтруктура` — Структура — Исходная структура
- `ИмяПоля` — Строка — Имя поля структуры
- `ЗначениеПоУмолчанию` — Произвольный — Значение, которое будет возвращено, если поля в структуре нет

**Возвращает:**
- `Произвольный` — Значение искомого поля структуры

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:30`.

## ЮТКоллекции.ЗначениеСоответствия

`Функция ЮТКоллекции.ЗначениеСоответствия(ИсходноеСоответствие, Ключ, ЗначениеПоУмолчанию = Неопределено) Экспорт`

Возвращает требуемое значение сответствия по ключу. В случае отсутствия ключа возвращает значение по умолчанию

**Параметры:**
- `ИсходноеСоответствие` — Соответствие — Исходное соответствие
- `Ключ` — Произвольный — Ключ поиска
- `ЗначениеПоУмолчанию` — Произвольный — Значение, которое будет возвращено, если ключа в соответствии нет

**Возвращает:**
- `Произвольный` — Значение ключа соответствия

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:55`.

## ЮТКоллекции.ДополнитьСтруктуру

`Процедура ЮТКоллекции.ДополнитьСтруктуру(Приемник, Источник) Экспорт`

Добавляет в приемник данные из источника, при совпадении ключей значения заменяются

**Параметры:**
- `Приемник` — Структура
- `Источник` — Соответствие из Произвольный - Структура

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:77`.

## ЮТКоллекции.ДополнитьМассив

`Процедура ЮТКоллекции.ДополнитьМассив(Приемник, Источник) Экспорт`

Добавляет в приемник все элементы источника

**Параметры:**
- `Приемник` — Массив из Произвольный
- `Источник` — Массив из Произвольный

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:90`.

## ЮТКоллекции.ОбъединитьМассивы

`Функция ЮТКоллекции.ОбъединитьМассивы(Коллекция1, Коллекция2, Коллекция3 = Неопределено, Коллекция4 = Неопределено) Экспорт`

Создает новый массив путем объединения входных.

**Параметры:**
- `Коллекция1` — Массив Из Произвольный
- `Коллекция2` — Массив Из Произвольный
- `Коллекция3` — Массив Из Произвольный
- `Коллекция4` — Массив Из Произвольный

**Возвращает:**
- Массив из Произвольный - Результат объединения

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:124`.

## ЮТКоллекции.СкопироватьРекурсивно

`Функция ЮТКоллекции.СкопироватьРекурсивно(Источник) Экспорт`

Создает копию экземпляра указанного объекта.
Примечание:
Функцию нельзя использовать для объектных типов (СправочникОбъект, ДокументОбъект и т.п.).

**Параметры:**
- `Источник` — Произвольный	 — объект, который необходимо скопировать.

**Возвращает:**
- `Произвольный` — копия объекта, переданного в параметре Источник.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:188`.

## ЮТКоллекции.СкопироватьСтруктуру

`Функция ЮТКоллекции.СкопироватьСтруктуру(Источник, Рекурсивно = Истина) Экспорт`

Создает копию структуры

**Параметры:**
- `Источник` — Структура, ФиксированнаяСтруктура — копируемая структура
- `Рекурсивно` — Булево — Создание "глубокой" копии, с копирование вложенных элементов

**Возвращает:**
- `Структура` — копия исходной структуры.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:234`.

## ЮТКоллекции.СкопироватьСоответствие

`Функция ЮТКоллекции.СкопироватьСоответствие(Источник, Рекурсивно = Истина) Экспорт`

Создает копию соответствия

**Параметры:**
- `Источник` — Соответствие из Произвольный - ФиксированноеСоответствие из Произвольный
- `Рекурсивно` — Булево — Создание "глубокой" копии, с копирование вложенных элементов

**Возвращает:**
- Соответствие Из Произвольный - копия исходного соответствия.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:256`.

## ЮТКоллекции.СкопироватьМассив

`Функция ЮТКоллекции.СкопироватьМассив(Источник, Рекурсивно = Истина) Экспорт`

Создает копию массива

**Параметры:**
- `Источник` — Массив Из Произвольный - ФиксированныйМассив из Произвольный
- `Рекурсивно` — Булево — Создание "глубокой" копии, с копирование вложенных элементов

**Возвращает:**
- Массив Из Произвольный - копия исходного массива.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:285`.

## ЮТКоллекции.СкопироватьСписокЗначений

`Функция ЮТКоллекции.СкопироватьСписокЗначений(Источник, Рекурсивно = Истина) Экспорт`

Создает копию списка значений

**Параметры:**
- `Источник` — СписокЗначений Из Произвольный
- `Рекурсивно` — Булево — Создание "глубокой" копии, с копирование вложенных элементов

**Возвращает:**
- СписокЗначений Из Произвольный

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:311`.

## ЮТКоллекции.ВыгрузитьЗначения

`Функция ЮТКоллекции.ВыгрузитьЗначения(Коллекция, ИмяРеквизита) Экспорт`

Выгружает значения "колонки" коллекции элементов.

**Параметры:**
- `Коллекция` — Произвольный — Итерируемая коллекция
- `ИмяРеквизита` — Строка — Имя реквизита элементов коллекции

**Возвращает:**
- Массив из Произвольный

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:334`.

## ЮТКоллекции.ПересечениеМассивов

`Функция ЮТКоллекции.ПересечениеМассивов(Коллекция1, Коллекция2) Экспорт`

Вычисляет двух массивов, коллекцию содержащую общие элементы.

**Параметры:**
- `Коллекция1` — Массив из Произвольный
- `Коллекция2` — Массив из Произвольный

**Возвращает:**
- Массив из Произвольный - Пересечение массивов

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:354`.

## ЮТКоллекции.ЗначениеВМассиве

`Функция ЮТКоллекции.ЗначениеВМассиве(Значение1, Значение2 = "_!%*", Значение3 = "_!%*", Значение4 = "_!%*", Значение5 = "_!%*", Значение6 = "_!%*", Значение7 = "_!%*", Значение8 = "_!%*", Значение9 = "_!%*", Значение10 = "_!%*") Экспорт`

Создает массив с переданными значениями

**Параметры:**
- `Значение1` — Произвольный
- `Значение2` — Произвольный
- `Значение3` — Произвольный
- `Значение4` — Произвольный
- `Значение5` — Произвольный
- `Значение6` — Произвольный
- `Значение7` — Произвольный
- `Значение8` — Произвольный
- `Значение9` — Произвольный
- `Значение10` — Произвольный

**Возвращает:**
- Массив из Произвольный - Массив переданных значений BSLLS:NumberOfOptionalParams-off BSLLS:NumberOfParams-off BSLLS:DuplicateStringLiteral-off

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:390`.

## ЮТКоллекции.ВСоответствие

`Функция ЮТКоллекции.ВСоответствие(Коллекция, ИмяПоляКлюча, ИмяПоляЗначения = Неопределено) Экспорт`

Возвращает соответствие элементов переданной коллекции, в качестве ключей выступают значения указанного поля элементов коллекции.

**Параметры:**
- `Коллекция` — Произвольный — значение, для которого определен итератор, и возможно обращение к полям элементов через квадратные скобки.
- `ИмяПоляКлюча` — Строка — имя поля элемента коллекции, которое будет ключом соответствия.
- `ИмяПоляЗначения` — Строка — если указан, значениями результата будут не элементы, а значения соответствующих полей элементов коллекции.

**Возвращает:**
- Соответствие Из Произвольный - полученное соответствие.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:426`.

## ЮТКоллекции.ВСтруктуру

`Функция ЮТКоллекции.ВСтруктуру(Коллекция, ИмяПоляКлюча, ИмяПоляЗначения = Неопределено) Экспорт`

Возвращает структуру элементов переданной коллекции, в качестве ключей выступают значения указанного поля элементов коллекции.

**Параметры:**
- `Коллекция` — Произвольный — значение, для которого определен итератор, и возможно обращение к полям элементов через квадратные скобки.
- `ИмяПоляКлюча` — Строка — имя поля элемента коллекции, которое будет ключом соответствия.
- `ИмяПоляЗначения` — Строка — если указан, значениями результата будут не элементы, а значения соответствующих полей элементов коллекции.

**Возвращает:**
- Структура Из Произвольный - полученная структура.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:450`.

## ЮТКоллекции.ПредставлениеМассива

`Функция ЮТКоллекции.ПредставлениеМассива(Данные) Экспорт`

Формирует текстовое представление массива.
Пример результата - "[1, 2, 3]"

**Параметры:**
- `Данные` — Массив из Строка

**Возвращает:**
- Строка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:474`.

## ЮТКоллекции.МассивВСтруктуру

`Функция ЮТКоллекции.МассивВСтруктуру(Значения, ЗначениеСтруктуры = Неопределено) Экспорт`

Создает структуру из массива.
Ключами будут элементы массива, а значением каждого элемента структуры - параметр `ЗначениеСтруктуры`

**Параметры:**
- `Значения` — Массив из Строка
- `ЗначениеСтруктуры` — Произвольный

**Возвращает:**
- Структура

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:489`.

## ЮТКоллекции.ОбратныйПорядок

`Функция ЮТКоллекции.ОбратныйПорядок(Значения) Экспорт`

Возвращает массив с обратной последовательностью элементов

**Параметры:**
- `Значения` — Массив Из Произвольный

**Возвращает:**
- Массив Из Произвольный - Перевернутый массив

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:512`.

## ЮТКоллекции.МассивСтруктурИзТаблицы

`Функция ЮТКоллекции.МассивСтруктурИзТаблицы(Таблица) Экспорт`

Преобразует таблицу в массив структур

**Параметры:**
- `Таблица` — ТаблицаЗначений

**Возвращает:**
- Массив из Структура - Массив структур из таблицы

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:537`.

## ЮТКоллекции.МассивСтруктурВТаблицу

`Функция ЮТКоллекции.МассивСтруктурВТаблицу(МассивСтруктур) Экспорт`

Преобразует массив структур в таблицу значений

**Параметры:**
- `МассивСтруктур` — Массив из Структура

**Возвращает:**
- ТаблицаЗначений

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:563`.

## ЮТКоллекции.ПредставлениеТаблицы

`Функция ЮТКоллекции.ПредставлениеТаблицы(ТаблицаЗначений) Экспорт`

Преобразовывает таблицу значений в строку

**Параметры:**
- `ТаблицаЗначений` — ТаблицаЗначений

**Возвращает:**
- Строка

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:592`.

## ЮТКоллекции.СтрокаТаблицыЗначенийВСтруктуру

`Функция ЮТКоллекции.СтрокаТаблицыЗначенийВСтруктуру(СтрокаТаблицыЗначений) Экспорт`

Преобразует строку таблицы значений в структуру.
Свойства структуры и их значения совпадают с колонками переданной строки.

**Параметры:**
- `СтрокаТаблицыЗначений` — СтрокаТаблицыЗначений

**Возвращает:**
- `Структура` — преобразованная строка таблицы значений.

Источник ядра: `exts/yaxunit/src/CommonModules/ЮТКоллекции/Module.bsl:620`.
